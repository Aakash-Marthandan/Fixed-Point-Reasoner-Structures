# Night A — analysis-time labels registered BEFORE the verdict (2026-09-05 ~20:15Z; Fable, parallel science session)

**Status:** labels for the analysis pass, written while Night A runs (Mumbai v6e-16, cap 2026-09-06T08:25Z). NO accuracy value of any Night A arm has been read. Nothing in `tools/analyze_finalA.py` (selftest 32/32, byte-untouched) or in the ledger's registration entries (§5 2026-09-05 LAUNCH REGISTRATION + amendments 1–3) is changed by this note: registration is pre-data and the arms are already banking. Every item below is a READING label the verdict report and the ledger §5 verdict entry must carry. Source of each item: the read of the ledger, `Plan_2026-09-05_FinalPhase.md`, `tools/chain_final.sh`, `tools/analyze_finalA.py`, `src/qhrrn2/dec_cell.py`, and the reference-decoder rider that landed 2026-09-05 18:10Z.

## L1. A1 / A5 anchor rows run ONE segment, not four (a stale plan table)
- `tools/chain_final.sh` and the ledger registration agree: A1 = X0 `--fpa-k 1 --fpa-eps 0.2 --fpa-frac 0.25`; A5 = DEC + the same + `--trm-ri-sigma 1.0`. `Plan_2026-09-05_FinalPhase.md` §3 (Night A table) still reads `--fpa-k 4 --fpa-eps 0.2` for A1 — stale; the registration (ledger + chain) is the authority.
- Consequence: the field-loop anchor is one outer segment (21 stack passes) from the corrupted solution on a quarter of each batch, a weaker anchor than the k=4 final-map rollouts that gave our cell its ≤ .1 % converged-wrong draws (H-45). R-A-1's LIFT letters are read "at k = 1"; the 55 % credence on SELECTOR-LIFT stands; a LIFT-FLAT does not close the FPA question on the field loop (k = 4 is the Night B follow-up if A1 reads FLAT).

## L2. The noise floor carries a selection-cadence confound
- FLOOR = max(|cold16(A0) − .8603|, .005). A0 banks grids at the 2k monitor cadence (`--grid-every $MON`); sportC1's X0 banked grids every 5k with monitors every 2k, so its selection ran at 10k resolution (the sportC2 instrument note). The pair therefore differs in selection resolution as well as seed.
- Label: the floor mixes seed noise with selection resolution; if |A0 − X0| ≤ .5 pp the .005 clamp governs and the confound is moot; if larger, the report states the two cadences beside the floor.

## L3. R-A-0's letter attributes a joint effect to the symmetry (the registration-gate lesson, third instance)
- A3 (DEC-w384, no digit aug) differs from X0 in TWO ways: (i) the nine-field equivariant state with its mean field coupling (`dec_cell._block`: each field reads the MEAN of the other eight fields' tokens per cell — a first-order DeepSets aggregate; digit identity is unrepresentable by construction) in place of the field cell's entangled 512-dim token whose channel MLP mixes all digits; (ii) no digit augmentation. Exact S9 is [P-C] by construction (`tests/test_final.py::test_dec_exact_s9`), so a low A3 cannot mean "the symmetry failed"; it can mean the coupling operator is weaker than the field's token (the X2 precedent: an operator substitution in this loop cost 47 pp while the loop's commit-at-step-1 dynamics survived).
- Label: R-A-0's letters (EXACT-S9-REPLACES-AUG / PARTIAL / FAILS) are read as JOINT symmetry × operator letters; the verdict names the confound. The disentangling arm for Night B if A3 lands in [50, 75]: the DEC with field-identity embeddings + digit augmentation (S9 deliberately broken) — recovery convicts the loss of digit identity in the mean coupling; no recovery convicts the operator's structure (then a set-attention coupling over the nine fields, still equivariant, is the next one-variable arm). A4 (DEC + digit aug) does NOT disentangle this (PM-A-4: data-order noise on an equivariant model).

## L4. Reference-decoder rider (landed 18:10Z): the classical ladder moved — no rule reads it
- `runs/analysis/reference_decoders.{txt,json,npz}`: 500 rating-stratified scan puzzles (seed 20260905; band n 74 / 116 / 154 / 122 / 34; givens median 25), BP 40 iterations undamped with exact all-different factors, BPD = guided decimation WITHOUT backtracking. Peeling 12.2 / BP 17.2 / BPD 21.6 % solved; search-class yield (rating > 0) 0.0 / 3.5 / 8.2 %; BP converged on 18.6 % of the slice; BPD ended in contradiction on 392/500.
- Scratch alignment (descriptive, unregistered; 15 lines over the banked scan records, identical idx) of the learned cells on the SAME 500: cold W0 34.6 · R1 37.8 · R2 33.0 · R3 45.6 · R4 41.0 · R0 38.2 · B0c 42.4 · B1c 44.0 · X1 23.8 · X2 42.8 · X0 92.2; search-class yield natives 24–39 % (R3 38.5) vs BPD 8.2, X0 90.8; BPD's solved set is 77–94 % contained in each native's solved set; stall-set Jaccard (map vs BP) natives .61–.76, X0 .09.
- Consequence for the paper's Figure 1 ladder (plan §5): the plan's ordering "peeling < BP < our natives ≈ 40 < BPD < field" is WRONG on the slice — the order is peeling 12 < BP 17 < BPD 22 < natives 33–46 < field 92 < ML 100. The "learned cell ≈ BP class" claim survives only as "the natives' stall sets contain BP's stall sets (Jaccard .6–.8) while the natives solve 3–5× the search-class puzzles that guided decimation without backtracking solves". Caveats before any figure: BP's 18.6 % convergence at 40 iterations; no backtracking in BPD (a DFS/backtracking reference is the honest upper classical rung); the slice is rating-stratified, not the natural distribution (the natural-20k peeling reference is 11.1 %). Reading for the DEC thesis: commitment WITHOUT a trained revision signal is weak on this channel (BPD 8 %, R3's hard rows partial); the field's loop commits AND revises under deep supervision with re-init as backtracking — the premise stands, the classical rungs move.

## L5. No DEC arm at or below X0's inference compute (amendment 2 removed w256)
- The core DEC arms moved from w256 (1.44M) to w384 (2.78M); A6 = w512. At matched parameters the nine-field state costs ≈ 9 × (w/512)² on the channel MLP and ≈ 9 × w/512 on the token mixer per stack pass — ≈ 5–7 × X0's MACs at w384, more at w512 (measured pace 1.32 vs 26.7 it/s on 4 chips). The plan's accuracy-per-MAC curve (§9 item 2) now has no point at or below X0's compute. The MAC-count instrument (owed since 09-02) and a w192/w128 DEC arm are Night B registration items; every Night A cross-system row carries its parameter count AND its inference compute, labeled inferred until the MAC instrument lands.

## L6. Minor labels
- A0 runs `--seed 1` in the chain; the analyzer header and campaign env call it "X0 seed 2" (ordinal naming: the second seed of the pair). Harmless; the report says "seed 1 vs sportC1's seed 0".
- A5's battery and A6's parity arm are second-night items (HANDOFF STATUS 19:23Z); R-A-3 OBJECTIVES and R-A-6 WIDTH read NO-DATA at the first analysis pass unless the second night runs — the verdict entry says so rather than reading a partial.
- The wide arms' scans are 5k × k32 (labeled by amendment 2): their SPURIOUS, t1r and ρ columns are NOT comparable to the X0-class 20k × k128 columns beyond the analyzer's per-class gates; the report keeps them in separate columns.

**Discipline note.** Items L1–L3 and L5 are registration-quality corrections discovered by reading, before any result; they belong to the next registration's pre-mortem (Night B) and to the verdict's labels, never to a post-hoc edit of the Night A rules. Item L4 is a descriptive rider read; its numbers enter the paper only through the analysis pass with the caveats above.
