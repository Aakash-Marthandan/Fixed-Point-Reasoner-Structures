# FRONTIER POD RUN — registration (2026-09-07; locked before any number is read)

**What:** the field's public TRM-class Sudoku-Extreme checkpoints read through OUR evaluator at the full protocol on one v6e-8, plus pass two of Night A. **Design:** `Plan_2026-09-07_Instrument_Suite.md` §4 (routes, battery, ops) and §6 (builds B1, B2, B4 landed this day). **Chain:** `tools/chain_frontier.sh` (eval-only; harness `tools/harness_frontier.sh`). **Port:** `tools/field_ckpts/port_field_ckpt.py` → `runs/field_ckpts/ported/<tag>/{ckpt_latest.pkl, port_check.json}`; test `tests/test_port_field.py`. **Evaluator flags:** `tools/eval_sudoku_extreme.py` `--record-by-step --record-q --z0-mode {gauss,trunc,perturb} --z0-eps --z0-device --seg-noise-beta --zero-prefix`; test `tests/test_eval_frontier_flags.py`. **Analyzer:** descriptive (`tools/analyze_frontier.py`, to be written during the run: the field study's tables at the full protocol + our records; NO champion rule, no letters — the predictions below are scored, nothing is decided by them). **Bucket:** `gs://qhrrn2-rescue/frontier` (ckpts/, sets/, evals/, live/, `frontier_final.tgz`).

## 1. Models, routes and provenance

| tag | checkpoint (as released) | port check (fp32, 64 strat puzzles, D16): step-1 max \|Δlogit\| / logit scale / D16 exact agreement | headline weights | lambda / beta | cold start |
|---|---|---|---|---|---|
| `trmpub` | alphaXiv/trm-model-sudoku `step_32550_sudoku_epoch50k` (TRM-MLP, 5,037,058 params) | 1.66e-3 / 19.5 / 95.3 % | the released weights (`state` = `state_ema`) | 0 / 0 | their H_init / L_init buffers |
| `trmpub60k` | alphaXiv `step_39060_sudoku_epoch_60k` (the second released grid) | 1.46e-3 / 21.7 / 98.4 % | same | 0 / 0 | theirs |
| `cgar` | Kaleemullah/trm-cgar-sudoku `pytorch_model.bin` (curriculum-trained TRM-MLP) | 8.0e-4 / 18.8 / 96.9 % | same | 0 / 0 | theirs |
| `eqr` | locuslab/EqR-model sudoku-extreme (TRM-MLP + Langevin loop) | 8.2e-4 / 17.2 / 96.9 % (noise 0, the same fixed buffers on both sides) | `state_ema` = the EMA shadow (headline); `state` = the raw model (the alt row) | .05 (their λ_ .95 on F) / .01 in the config; noise applied ONLY under `--seg-noise-beta` | ONE trunc-normal(std 1) draw at seed 20260907 (their buffers are non-persistent: no cold start exists; labeled) |
| A3 A7 A5 A4 A8 | Night A's val-selected DEC grids (42k, 22k, 28k, 10k, 12k; EMA) | — | EMA | — | the DEC's seeded buffers |

The port is the Mac study's verified conversion (I12/P14: relative 1e-4 at step one; ×10 per outer step on chaotic rows with the torch-fp64 control growing identically) extended with the buffers carried in the parameters (`trm_cell.init_states` reads `H_init`/`L_init` when present; our own arms are unchanged: `test_eval_frontier_flags` pins the seeded default). Every summary carries the `port` block (tag, source, note, seed).

## 2. Protocol (binding; identical to Night A's field-cell arms)

Sets: FULL 422,786; SCAN20k (subsample seed 20260822); SUB5k = the seeded 5,000; STRAT512 (seed 20260821); EqR's TRAIN-1k (`sets/train_eqr_sx.npz`, its seed-42 builder rows). Draws: per-(puzzle, draw) seeded (mi_seed 4242), N(0, 1) on the carried latent = the standing column; EqR's truncated-normal reset = the labeled variant (`--z0-mode trunc`). Depth: D16 headline, D64 depth row, D128 on 20k, D256 on 5k; every cold pass records the exact bit per step (R4) and the D16 fulls the halting logits per step (R10). Numerics: the field's bf16 matmul (`JAX_DEFAULT_MATMUL_PRECISION=default`) on every field-class eval; the fp32 control (`highest`) on SCAN20k D16. Weights: `--ema` on every row but EqR's raw alt row. Selection column: "as released" (the field selects its best test checkpoint; we do not re-select). Compute column: 15.2 GMAC per outer step per puzzle for every TRM-MLP row (= X0's; the DEC 70.1). Training-regime column: from the model cards / configs (§1 of the field report).

## 3. The job list (the chain's static order = science per hour under spot churn)

1. **The DEC scan canary** `scan_A3`: 5k × k32 t64 (the amended wide-arm protocol) at `--batch 128` under the stall watchdog (20 min without a shard log / partial write → py-spy dump → kill → STALLED); on a stall the recipe steps to `--z0-device` (draws generated on the device: no host→device transfer of the B × (2, 9, 81, 384) carry — the deadlock's first suspect), then `--batch 64 --z0-device`; the completing recipe is banked (`RECIPE-DEC`) and used by every later DEC scan; all three failing = `A3_SCAN_DEADLOCK`, labeled, and the chain proceeds.
2. **Headline rows per model** (trmpub, eqr, cgar in that order): full D16 (+ bits + q) · full D64 (+ bits) · SCAN20k × k128 t64.
3. `scan_A7`, `scan_A5` (the champion pair and the assembled-objectives arm).
4. **Riders per model:** STRAT512 × k256 D16 screen + unverified majority · D128 on 20k · D256 on 5k · explosion census · stall calibration · init radius (ε ∈ {.03, .1, .3, 1, 3}: one perturbed draw per puzzle on STRAT512 at D16) · prefix zeroed (20k D16) · fp32 control (20k D16).
5. **EqR extras:** the raw-weights alt row (full D16) · the truncated reset scan (20k × k32) · the released Langevin protocol (per-pass noise .5: 20k D16, 20k D64, 5k × k32 with the trunc reset) · the training-noise row (.01; 20k D16) · its own train-1k at D16.
6. `trmpub60k`: full D16 (+ bits + q) and 20k D64.
7. `scan_A4`, `scan_A8`; then the DEC D128 rows on 20k for A3, A7, A5.
8. Completion: `frontier_final.tgz` + the compile cache banked; self-teardown on COMPLETE; INCOMPLETE leaves the missing markers named and a relaunch resumes.

Walls (from Night A's measured paces on an 8): ≈ 106 min per model, EqR extras ≈ 33, trmpub60k ≈ 11, DEC scans ≈ 17 min each, D128 rows ≈ 4 min each → ≈ 8.5 h + compile/markers; **budget ≈ $60–75 on a US v6e-8 (+ weather; Mumbai ≈ $75–90)**; cap = launch + 18 h (`runs/tpu_deadline.txt`); `WALL` 17 h.

## 4. Predictions (locked; credence in parentheses; scored descriptively, nothing decided)

Reproduction (FULL test, cold, as released): **P1** trmpub D16 ∈ [78, 80], D64 ∈ [82.5, 84.5] (75 %); **P2** cgar D16 ∈ [85.5, 86.7], D64 ∈ [91, 92.5] (70 %); **P3** eqr (EMA, noise 0, fixed buffers) D16 ∈ [86, 87.5], D64 ∈ [92.5, 93.7] (70 %); **P4** trmpub60k within ±1.5 pp of trmpub at D16 (60 %); **P5** the pod's bf16 rates on the 20k subset within .5 pp of the Mac's fp32 torch rows on the identical puzzles, per-puzzle agreement ≥ 94 % (70 %), and the fp32 control within .3 pp of the bf16 row with agreement 94–97 % (70 %).
Depth: **P6** D128 adds +1–2 pp over D64 on trmpub, +0.5–1.5 on cgar and eqr; D256 adds ≤ +0.5 over D128 on all three (55 %); **P7** depth regressions (solved at some step, unsolved at the last) ≤ .1 % of puzzles on every model at every depth (70 %).
Decoder class (R1 on the FULL D64 records): **P8** cgar and eqr DECIMATING (no g50 in 17–35; yields ≥ 88 %), trmpub MIXED with g50 ∈ [17, 21] and yield 78–83 % (60 %); **P9** first-exact median 1 on all three with p90 ≤ 12 (75 %).
Selector law (SCAN20k × k128): **P10** spurious rate eqr ≤ 1 %, trmpub 2–5 %, cgar 20–30 % (60 %); **P11** b1 within 2 pp of cold on trmpub and eqr, cgar's b1 15–25 pp under its cold (65 %); **P12** verified@128 ≥ 97 % on all three, ≥ 99 % on eqr and cgar (65 %); **P13** t1r@128 / verified@128 ≥ .98 on eqr, .95–.99 on trmpub, ≤ .85 on cgar (60 %).
Halting head (R10 on the D16 fulls): **P14** AUC(q_halt at step 16, exact) ≥ .995 on all three (75 %); **P15** the emulated ACT-halting row (exact at the first step where q_halt > q_continue) within 1 pp of the D16 cold on every model (55 %).
EqR protocol: **P16** noise .5 costs 8–11 pp at D16 and 2.5–4 at D64 on the 20k vs noise 0 (70 %); noise .01 within .5 pp of noise 0 (70 %); **P17** the trunc-reset scan's b1 within 1.5 pp of the gauss scan's b1 and its spurious rate ≤ 1 % (65 %); **P18** the raw weights read 1–8 pp under the EMA at D16 (60 %); **P19** EqR's own train-1k at D16 ∈ [79, 85], within 3 pp of its test rate (no memorization) (70 %).
Attractors: **P20** init radius at D16 relative to ε = 0: eqr ≥ 95 % of cold at ε = 3; trmpub ≥ 90 %; cgar ≤ 88 % at ε = 3 with a gain ≥ +1 pp at ε = .3 (60 %); **P21** prefix zeroed: |Δ| ≤ 1.5 pp on trmpub and cgar, −4 to −7 pp on eqr (65 %); **P22** explosion census 0.00 % and the calibration gap (top-5 correct at stalls) .45–.60 on all three (70 %). Screens (STRAT512 k256 D16): **P23** unverified majority@256 eqr ≥ 90, cgar 85–90, trmpub 78–85 (55 %).
Pass two (the DEC scans, 5k × k32 t64): **P24** the canary completes at `--batch 128` without a stall (50 %; if it stalls, `--z0-device` completes (35 %)); **P25** spurious rates A3 2–6 %, A7 ≥ 30 %, A5 ≤ .5 %, A4 1–4 %, A8 ≤ .5 % (55 %); **P26** verified@32 ≥ 99 % on every DEC arm and b1 within 2 pp of cold on all five (70 %); **P27** DEC D128 (20k): A3 +0.3–1.0 pp over its D64 rate, A7 and A5 +0.2–0.6 (55 %), regressions ≤ .05 %.

## 5. Labels and limits (pre-registered)

The pod rows are our evaluator's numbers on the ported weights: the comparator table cites them as "reproduction (our port)" beside the paper numbers and the Mac's torch rows; the port's ≈ 5 % per-puzzle disagreement on chaotic rows (the fp64 control) is the numerics floor for every paired statement. EqR's "cold" is one labeled reset draw (its B = 1 column is the per-puzzle reset = the scan's b1). HRM is not on this pod (no JAX port; the torch harness needs a GPU VM and the project's global GPU quota is 0): its rows stay the Mac's reduced-protocol rows, upgraded on the Mac to k32 draws (SUB5k D16) and k8 at D64 in the follow-on queue; the cross-route row (torch vs JAX on trmpub) is the Mac's SCAN20k torch run vs this pod's 20k subset. Orbit (C8 `--digit-perm`), retention/ε-ladder (C5 `--init-latent`) and the PTRM protocol (C11f) are NOT in this battery (builds B1c/e/g not made; the Mac's retention/orbit rows stand). The DEC scans are the amended wide-arm protocol (5k × k32), never in a column with the 20k × k128 scans. Nothing here is a champion rule; the champion night's registration follows the freethink.

## 6. Ops

`tools/campaign.env` → `pod.sh supervise` (one spot v6e-8, US zones first, Mumbai last; the deadline knob bumped to launch + 18 h; the launchd watchdog loaded; the Mac on AC); the chain restores the live bank at every start and banks every 5 min; the ops phase reads no accuracy values; completion = `CHAIN-FRONTIER-COMPLETE` + `frontier_final.tgz` in the bucket, then fleet zero verified per zone. Mac follow-on (the $0 queue): the four ported checkpoints' C1/C2 dynamics rows through `suite_ckpt.py` (n 128, t 64) and HRM's Metal upgrade.
