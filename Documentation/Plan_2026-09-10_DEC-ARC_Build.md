# The last $400: one DEC-ARC build on a v6e-8, with the pending instruments and understanding gaps closed first (plan, 2026-09-10; NOT a registration — the registration follows the build, the adversarial review and the pilot)

**The PI's framing (2026-09-10):** $400 left; one DEC-ARC build "based on all we know" to see how it does on ARC and make the Sudoku-vs-ARC section concrete; paper padding with what is left and on the Mac; every pending instrument and understanding gap addressed first because there is a single shot; a v6e-8. **This document:** the recommendation with the alternatives priced (§0), what the build is and what exists in code (§1), the night's arms and battery (§2), the pending instruments and gaps with what each decides for the build (§3), the prediction stubs (§4), the calendar to the Sep 16 freeze (§5), the decisions the PI owns (§6), and the fallback that keeps the shot from being lost (§7). Rates: v6e-8 US $6.82/h, Mumbai-8 ≈ $8/h (ops memory). Every wall below is inferred from the champion night's paces until the pilot measures the DEC-ARC's own.

## 0. Recommendation and the priced alternatives

**Recommendation: the DEC-ARC night with the native control inside it, preceded by a 2–3 h pilot, with the champion's full-set D64 row as a rider that runs only if the reserve is untouched.** It is the one use of the money that produces new science for §6 (the same cell read on both domains through the same instruments) and paper 2's first object, and it carries the d96 scale-matching as one of its arms instead of as a separate run.

| use | what it buys | pod-hours (inferred) | cost | verdict |
|---|---|---|---|---|
| **A. DEC-ARC night** (4 arms in two rounds on the two hosts + batteries; §2) | the DEC on ARC: the depth law, the selector law's value, the failure texture, basin existence, the exact symmetry and the public-eval number on ONE cell across both domains; the native d96 control = the d96 rung's scale-matched arm | pilot 2–3 h; night ≈ 20–24 h | ≈ $20 + $140–165 (US) / $160–190 (Mumbai) | **do** |
| B. the ARC-d96 rung of the original recipe with the back-port (freethink A-5 iii) | scale-matches D6 / Law 4's fourth width on the native cell; no new code | ≈ 10–14 h | ≈ $70–100 | folded into A as the control arm; the full rung is the FALLBACK (§7) |
| C. the full-set Sudoku evals (`Plan_2026-09-10_FullSet_Evals.md`) | headline-table padding on the field's set for C2 / C4; no new science | ≈ 17–23 h per row on 4 chips | ≈ $290 for the plan | only the C2 D64 row, as A's rider, ≈ $60–115 |
| D. the field's ARC TRM ported into our evaluator (A-5 iv) | their basins per task through our suite | Mac build days, no pod | $0 | after the freeze; the harness rows already carry §6 |
| reserve | preemption, one relaunch, the Mumbai rate if the US is dry | — | ≈ $80–100 | keep |

Budget: pilot ≈ $20 + night ≈ $165 + reserve ≈ $100 = $285 committed; the rider ≈ $60–115 spends the remainder only if the night runs clean. Drop order under churn: the rider, then the fourth arm (the plain DEC-ARC), never the pilot, the seed pair or the control.

## 1. The build: what the DEC-ARC is, what exists, what is new

**The design, derived from the laws (freethink A-4, sharpened by the ARC pass).**

1. **The state:** ten color fields on the canvas, z_H / z_L of shape (F = 10, S = 900 or 1024, w), every parameter shared over the field axis, so a permutation of the colors permutes the state and the logits exactly (S10; the background is one of the ten and is distinguished only through the role embedding, which is what leaves S9 on the non-background colors physical). Exact color symmetry replaces the field's colour-permutation augmentation, as the S9 state replaced the digit orbit on Sudoku (−65 pp without it there; Law 6). D4 stays an augmentation (the corpus's orbit expansion; the eight views at evaluation are the consistency vote of §2).
2. **The role embedding per (field, cell):** the INPUT grid's color at this cell is this field / another field / the cell is outside the input's size — the Sudoku "given" roles generalized; the input is context, not a partial answer, so no cell is clamped.
3. **The readout per cell:** ten color logits from the field tokens (one shared vector per field) plus a VOID logit from the S10-invariant cell mean; the output size is the crop of the non-void region (the field's crop rule). The halting head reads the field-and-cell mean as on Sudoku; the commit head (C6's, τ ≥ 1 = inert, bit-identical by test) rides as the abstention instrument.
4. **The conditioning:** the demonstration pairs in the same ten-field representation, run through the shared block and pooled per field over cells and pairs into (F, w) task tokens added to every cell's field token — color-specific rules survive exactness because the tokens permute with the colors — plus one invariant token for the void decision. NO puzzle identity (the memorization channel measured at 26 pp on the field's model) and NO test-time fit (the natives' arm-A fit stays their protocol column, labeled D7; the DEC-ARC is read cold, with draws, and with the dihedral vote).
5. **The token mixer:** attention over the S cells per field (shared over fields; RoPE-2D or a learned position table, S10-safe), because the DEC's token-mixing SwiGLU over S = 81 cells does not scale to S = 900 (that is why the field's ARC model is TRM-Attention). The field coupling (the mean of the other nine fields at the cell) and the channel SwiGLU are the Sudoku DEC's.
6. **The loop and the objectives:** the champion loop (2 layers, H 3 × L 6, λ .05, β .01, 16 segments with ACT, deep supervision, stablemax, EMA .999, wd 1.0, lr 1e-4 after warmup) with RI σ 1 on the latent and FPA k 1 ε .2 frac .25 on the embedded output (the anchor rows = basin holding, which on ARC is the binding instrument), no NI. The FPA start at ε = 0 is the DEC's handed-truth RETENTION instrument (the field cells have no answer register, so retention is read by starting the loop at the embedded truth) and the ε ladder is its basin radius — the two ARC-era instruments come back on the DEC through the mechanism that trains for them.
7. **Width and compute (the MAC gate; `tools/mac_count.py`, 2026-09-10):** MEASURED per outer step per example (XLA's cost analysis of the evaluator's own step map): the Sudoku DEC-w192 14.82 GMAC, the DEC-w384 49.95, the set-attention arm 53.09, the field's ported TRM-MLP 12.87 (so the w192 is 1.15× the field's Sudoku arithmetic, the w384 3.9×), the native rg cell at d64 on the ARC canvas 3.44. ANALYTIC for the DEC-ARC design (per token per block: attention 4w² + 2Sw, SwiGLU 12w², coupling 2w²; 9,000 tokens; 2 layers × (H 3 × L 6 + 3) block passes per segment; the same formula gives the field's ARC-1 TRM-Attention 141 GMAC per segment at hidden 512 over 916 tokens): w128 199 GMAC per segment = 1.41× the field's ARC cell, w160 283 = 2.0×, w192 381 = 2.7×, w256 620 = 4.4×; parameters ≈ 18w² per block × 2 ≈ 0.6 M (w128) / 0.9 M (w160) / 1.3 M (w192) against the field's 6.83 M non-embedding + the 449 M-entry identity table. **Base width w160** (the 2× point, ≈ 0.9 M parameters, no table; w192 is 2.7× and w128 the 1.4× floor); the pilot's pace decides whether 160 holds or 128 is taken (§5). HBM (inferred from the champion's 729-token examples at batch 768 on 4 chips with remat): 9,000-token examples → batch ≈ 64–96 per host.
8. **The data and the selection:** the pretrain13 corpus (re-arc 20 per family + the ARC training split minus dev-30 + ConceptARC minus val-hard, the family holdout for the rg gate, orbit 4), so the cell is the only change against the natives; the 2k monitor = per-output exact on the val slice with dev-30 as the second key, earliest tie, the raw monitor as the tie-breaker; the onset instrument = the monitor's plateau end; 30k steps with the registered extension (+10k once if the selected grid is in the last 4k).

**What exists (reused, not rewritten):** `qhrrn2.dec_cell` (the field-shared block stack, the coupling, the role embedding, the halting mean, embed_answer for the FPA anchor, the commit head), `qhrrn2.trm_cell` (the loop, RI, the segment loop with ACT and deep supervision), `tools/pretrain.py`'s field loop and FPA rows, the 2k monitor with earliest-tie selection, the census, `tools/arc_suite.py`'s draws / selectors / retention / dynamics rows, `qhrrn2.episodic`'s corpus and the rg/rt gates, the chain/harness pattern of the champion night, the frozen-analyzer pattern with a selftest.

**What is new (the build list, ≈ 2 days):** B1 `dec_cell` generalized: F as a config, the attention token mixer, the input-role embedding, the void readout, the support encoder (the same block over the pairs, pooled per field); B2 the trainer's ARC field loop: episodic batches → ten-field inputs and targets (void as a target class), the size-aware exactness, the monitor on the val slice + dev-30, the FPA rows on the embedded output, the family/orbit plumbing; the Sudoku-only asserts lifted for `--cell dec --arc`; B3 the evaluator: cold traces with per-step exactness, latent draws (k), the FPA-start retention and ladder, the dihedral vote (8 views; any-view, count vote, the consistency row), the color-permutation flip test, C1/C2/C3 rows, the halting and commit heads, rg / rt / val-hard / dev-30 and the ARC-AGI-1 public evaluation (400 tasks: cold pass@1, k 8 draws, the vote's pass@2) — one tool, `tools/eval_decarc.py`, sharing `arc_suite.py`'s record schema so `lens_arc_suite_read.py` reads both; B4 `tools/analyze_decarc.py --selftest` with the registered rules and predictions (§4), the onset definition, the INTEGRITY gates (one grid per (arm, vsel), counts, the color-flip floor); B5 `tools/chain_decarc.sh` + `tools/harness_decarc.sh` (idempotent GCS markers, the 5-min live bank, the extension rule, the remat retry, the rider queue) with every negative scenario asserting its staged failure; B6 tests: exact S10 equivariance of the whole forward (a color permutation of input, supports and target permutes the logits exactly), τ ≥ 1 bit-identity of the commit head, the void readout's invariance, the FPA ε = 0 start reproducing the target, CPU smokes of the real trainer at the real width with every registry flag, the evaluator's rows on real tasks, `analyze_decarc.py` on no data exiting clean, the harness's exact output names.

## 2. The night: four arms in two rounds, one battery

| arm | one variable | why | budget |
|---|---|---|---|
| D0 | DEC-ARC w160, RI σ1 + FPA, seed 0 | the object | 30k (+10k by rule) |
| D1 | seed 1 | the measurement law: the claim is the pair's mean with its spread; every contrast paired on identical queries (McNemar) | 30k (+10k) |
| D2 | the plain twin (RI 0, FPA 0) | the selector law on the DEC-ARC: does RI + the anchors give a clean residual WITH coverage on ARC (the value Dri lacked), or does it condense as on the natives | 30k |
| N0 | the native cell at d96, A5-class @40k with the back-port (the d96 rung's registered arm; freethink A-5 iii) | the cell contrast at matched data (labeled cell + regime, as sportC1's X0 comparison was); the scale-matched throat / Law 4 fourth-width rows | 40k |

Chip map: hosts h0 / h1 (4 chips each, DP over the whole host); round 1 = D0 ‖ D1, round 2 = D2 ‖ N0; the battery of a finished arm runs on its own host before the next arm starts; the rider (C2's full-set D64 row, 17 h on 4 chips) is queued on whichever host finishes first, gated on the reserve (§0).

The battery per arm (every row an instrument of the ARC pass, so the Sudoku twin, the native twin and the field's twin all exist): cold at t 16 with per-step exactness (the depth row: flat or rising on this cell?), t 64 (regressions); k 32 latent draws on val-hard and dev-30 (per-draw exact, converged, residual AUC, E5 spurious, oracle, majority, residual-selected, n_distinct); the FPA-start retention at ε 0 and the ladder ε .2 / .4 / .6 / .8 (basin existence, basin radius, the conditioning of the oracle on retention); the dihedral vote (8 views: any-view, count-vote pass@2, the distinct-answers row); the color-permutation flip test (must read the numerics floor); the C1 / C2 / C3 rows (commitment at step 1, confidently wrong, flips per cell, converged-wrong of converged, calibration on wrong cells); the halting head's AUC and halt fraction; the commit head's per-instance AUC and ECE; rg-96 / rt-48 gates; the explosion census; the monitor trajectory with the plateau end; the ARC-AGI-1 public evaluation (400 tasks / 419 inputs: cold pass@1, k 8 draws, the 8-view vote's pass@2; labeled: no identity, no test-time fit, ≤ 1.3 M parameters, our protocol); MACs per step (the instrument of §3, run on the arm's config). N0 gets the native battery of the rung plus the shared rows above where its cell admits them (retention via yprev, draws via canvases).

## 3. The pending instruments and understanding gaps, and what each decides for the build (all Mac, $0, before the registration; in order)

| # | item (register: Part 3b / 2l / Part 3 back-port / A-4) | what it decides for the DEC-ARC | cost | when |
|---|---|---|---|---|
| G1 | **The orbit-invariance ARC twin** on `pretrain13_Dri` / `_C53`: color permutation × dihedral on val-hard's unseen inputs, flip rate against the compute-route floor (`lens_orbit_invariance` promoted to a tool, Part 3b) | whether exact color symmetry is a lever or a guarantee on ARC (the natives were never color-augmented per row; if their flip rate is high, exactness is the lever; if low, exactness buys robustness and the vote's consistency row) — and the DEC-ARC's registered flip floor | Mac, 2–3 h (traces at 5 transforms × 144 queries × 2 maps) | now |
| G2 | **The verifier-dependence ARC rows on our substrates:** H_q as the halting-head twin (AUC .76 / .59 val-hard), a trained commit-head readout fitted on Dri's carried states (the joint lens on ARC) | which readout the DEC-ARC carries and what its abstention row is predicted to read; closes the verifier-dependence table's ARC column for §6 | Mac, 2–4 h | now |
| G3 | **The C1 columns on the field's ARC traces** (`arc_out/cold_all_fp32/rows.jsonl` carries `exact_by_step` and `token_acc_by_step`) | the field's ARC commitment dynamics beside ours and the DEC-ARC's (the D15 row on three substrates) | Mac, 1 h | now |
| G4 | **The MAC instrument** (measured multiply-accumulates per step per example, per cell and width; the owed compute column) | the width gate of §1.7 and the paper's compute column for Sudoku AND ARC | Mac, 2 h | before the pilot |
| G5 | **The physics-tool selftest** and the analyzer discipline for `analyze_decarc.py` (hand-built records; the AUC-orientation lesson) | no number leaves the pass unchecked | Mac, 1–2 h | with B4 |
| G6 | **The onset definition** registered (the monitor's plateau end) and the 2k monitor on ARC (per-output exact on the val slice; dev-30 second key; earliest tie) | selection on ARC under the Sudoku lesson (the 64-puzzle monitor's artifact); the memorization clock read on ARC | build, 1 h | with B2 |
| G7 | **The RI distribution for a cell with no answer register** (latent draws z0 ~ N(0, σ), not canvases) and the retention instrument via the FPA start (§1.6) | the draws and retention rows exist for the DEC-ARC at all | build, in B3 | with B3 |
| G8 | **The memorization channel on ARC:** wd 1.0 of the field regime vs the natives' 1e-4 (H-40's budget tax = memorization in ARC clothing) | read on the monitor trajectory (plateau end) of D0/D1 vs N0; a wd arm is NOT affordable — labeled as the cell + regime contrast | — | analysis |
| G9 | **D15's mechanism** (why the same native family commits 78 % at step 1 on ARC and 3–13 % on Sudoku): the one-shot target vs the propagation target | an understanding row for §6; the DEC-ARC's commitment at step 1 is the prediction that tests it (P6) | Mac, reading + a row from G3 | now |
| G10 | **The US-zone control of the multi-draw scan pace** (Part 3b) | the pilot times one k 32 batch in us-east1-d | ≈ $5 inside the pilot | pilot |
| G11 | The candidate hypotheses (capacity-ordered memorization; attention at seeds), the `--curv` row, the L6 / HRM ARC-2 checkpoints, the full-set augmentation vote (GPU) | not on this shot | — | after the freeze |

Understanding gaps that the build itself closes (registered as predictions in §4 rather than pre-work): whether depth is a property of the cell or the domain (P3); whether the selector law's value appears on ARC when the cell holds basins by construction (P4); whether exact color symmetry generalizes the way the S9 state did (P1, P7); what the verifier-free public-eval number of a ≤ 1.3 M identity-free cell is (P8).

## 4. Prediction stubs (bands locked at registration after the pilot; credences then)

- **P1 exactness:** the color-permutation flip rate on unseen inputs reads the numerics floor (≤ the compute-route floor) on every DEC-ARC arm; the natives' rate (G1) is the comparison.
- **P2 parity gate:** val-hard per-output cold ≥ the native d64 twin's 7.6 % on the identical 144 queries (the DEC-ARC must not be worse than the cell it replaces); band after the pilot.
- **P3 the depth law on one cell across domains:** exact-by-step on the DEC-ARC — flat from step 2 (the domain owns depth) or rising (the cell owns it); either reading is a §6 sentence; regressions counted at exact pairing.
- **P4 the selector law's value:** residual AUC ≥ .85 on D0/D1 AND oracle@32 − cold > 0 (coverage exists under RI + FPA where Dri condensed); D2's AUC below D0/D1's by the Sudoku margin.
- **P5 basin existence:** FPA-start retention at ε 0 ≥ .9 on solved pairs and > the natives' 19–28 % on ALL pairs (the anchors train basin holding); the oracle conditions on retention (the ARC era's 31×) — if retention rises without the oracle, the inventory is there and the search is not.
- **P6 the failure texture:** converged-wrong of converged ≥ .9 with 1 flip per cell (ARC's texture survives the cell) — or churn appears (the cell's texture ports); commitment at step 1 read against 78 % (natives) and 93–96 % (the Sudoku DEC).
- **P7 the dihedral vote's worth:** any-view − identity and the count-vote's collection; the consistency row (distinct answers solved vs failed) reproducing the field's .76 / .001 separation.
- **P8 the public number:** ARC-AGI-1 evaluation pass@2 with the 8-view vote, no identity, no fit — reported with its columns, whatever it lands; the field's 43.0 (7 M + table + 1,000 views) and FPRM's 47.5 (no TTT, no aug) beside it.
- **P9 the control:** N0 reproduces the d96 rung's registered bands (rg-96 ≥ 23.5, N 42–50, I < 499) or the rung's kill; the throat / profile rows scale-match D6.

## 5. Calendar (IST; the freeze Sep 16)

- **Sep 10 (today):** this plan; G1–G3 + G9 started on the Mac (background); the build begins (B1).
- **Sep 11:** B1–B3 with B6's tests; G4 (the MAC row sets the width); G5.
- **Sep 12:** B4–B5 (analyzer with selftest, chain + harness with negative scenarios), CPU smokes of the real trainer and evaluator at the real width; the adversarial review and the pre-mortem; the registration commit (rules and bands locked with the pilot's slots open).
- **Sep 13 morning:** the pilot on a v6e-8 (2–3 h, ≈ $20): HBM at the real batch, the pace (it/s) that fixes the width and the walls, the first two monitor rows, one k 32 draw batch timed (G10); bands locked; **launch** the same day on the PI's go.
- **Sep 13 → Sep 14:** the night (≈ 20–24 h + batteries); the rider if the reserve holds.
- **Sep 15:** the analysis pass (analyzer untouched), the §6 rows, the ledger entry; **Sep 16 freeze.**

## 6. The PI's decisions

1. The recommendation of §0 (A with the control inside, the rider conditional) — or B / C instead.
2. The base width (w 160 recommended after the MAC instrument: the 2× point; the pilot may take 128) and the batch/steps (30k + the extension rule).
3. The fourth arm: the plain twin D2 (recommended) or a second width instead.
4. The public evaluation row in the battery (recommended; it is the number the field will ask for).
5. The rider: C2's full-set D64 row, conditional on the reserve.

## 7. The fallback that keeps the single shot

If the build is not registered by Sep 12 evening (any of B1–B6 failing its tests or the review), the night becomes the ARC-d96 rung of the original recipe (N0 + a seed + a plain arm, ≈ $70–100; no new code beyond the back-port already staged), launched on the same pilot day — it still scale-matches D6 / Law 4 and gives §6 its native-at-width rows; the DEC-ARC build then goes to paper 2 after the freeze with its tests already written.

## 8. A correction carried into the record (2026-09-10)

The ARC note, Instrument Map Part 2l and the ledger's ARC entry called `pretrain13_Dri` / `_C53` "d48 natives, 763k"; the checkpoint's config reads d 64 and its model has 763,420 parameters (the "763k" was right, the width was not). The note and the map are corrected in place; the ledger entry stands with this correction beside it in the next entry.

## 9. Registration draft (2026-09-10 evening; locked at the registration commit after the adversarial review; the pilot fills the two [PILOT] slots)

**Amendments to §1 from the build (audited against the code, not the sketch):**
- **§1.4 conditioning → the natives' protocol, made colour-indexed.** The support encoder is NOT built for this shot. The DEC-ARC conditions on a per-task code exactly as the natives do — a learned row per training task, the deployed arm-A fit of the code on a task's support pairs at test time (600 steps; `eval_dev30._fit`, never re-implemented) — but the code is (F = 10, d_task = 32) per colour, projected per field by one shared matrix, so the joint action of a colour permutation on the input and on the code's field axis is an exact symmetry of the map (`tests/test_decarc.py::test_decarc_exact_s10`). Consequence: the cell is the ONLY variable against the natives (same corpus, same conditioning protocol, same fit), and every native evaluation tool runs on the DEC-ARC unchanged (`model.forward_fields` takes the code as `task_vec`; the size is decoded from the VOID readout through `model.decode_size`, the rg cells' decode bit-exact). The identity-free / fit-free support encoder is paper 2's object. D7 is labeled identically for both cells.
- **§1.7 width w160** (the measured MAC gate: 2.0× the field's ARC-1 cell per segment, ≈ 0.9 M parameters with the 1,024-cell position table).
- **§1.6 the FPA rows on ARC** corrupt inside the OUTPUT region uniformly over the ten colours and anchor through the input's role table (void = the "outside" role), so the eps = 0 start is the handed-truth retention instrument and the eps ladder the basin radius; the loss on the DEC-ARC averages the output region and the VOID region separately with `w_void` .5 (the size is a target of the VOID readout).
- **The 2k monitor** = the cold rollout at T = 16 on the held-out queries of `n_val` = 96 training tasks with their trained code rows (per-output exact; the EMA weights select, the raw monitor second, earliest tie); the natives' N0 selects on its 2k val rows (`select_ckpt --row val --key val_frac --tie earliest`). Both arms therefore train on the same corpus with 96 tasks' queries held out (the banked d64 natives held out 20; the d64 twin is the "as-is" reference, labeled).

**The registered arms (tools/chain_decarc.sh, `arm_flags`, locked):** D0 / D1 = `--cell decarc --dec-width 160 --decarc-heads 4 --trm-layers 2 --trm-h-cycles 3 --trm-l-cycles 6 --T 16 --trm-lambda 0.05 --trm-beta 0.01 --loss stablemax --batch 64 --wd 0.1 --warmup 2000 --lr 2e-4 --ema 0.999 --w-void 0.5 --table-lr 1e-2 --table-wd 0.1 --trm-ri-sigma 1.0 --fpa-k 1 --fpa-eps 0.2 --fpa-frac 0.25 --rearc --conceptarc --orbit 4 --n-val 96 --sot --act --dp --remat` at seeds 0 / 1, 30k steps + the extension rule (a selected grid inside the last 4k → +10k once); D2 = the same with `--trm-ri-sigma 0 --fpa-k 0`; N0 = `--d 96 --T 6 --anchor-p 0.3 --beta-flux 3e-5 --beta-flux-nl 1e-5 --ni-sigma 0.01 --n-val 96 --val-every 2000` at 40k (the d96 rung's A5-class arm with the val back-port).

**The battery (tools/eval_decarc.py per DEC arm; tools/arc_suite.py for N0):** val-hard and dev-30 with k = 32 latent restarts (z0 ~ N(0, 1)), the FPA-start ladder eps ∈ {0, .2, .4, .6, .8} (2 draws per rung, 8 segments), the 8-view dihedral vote (the whole task re-fitted per view, answers inverted and counted), the colour-flip row (an S10 palette of the whole task, re-fitted, against the seed + 1 re-fit floor); rg-96 and rt-48 with k = 8; the ARC-AGI-1 evaluation split (400 tasks) with k = 8 and ARC1_VIEWS = 8 (drop order: 8 → 2 if the pilot's fit pace makes the row exceed 3 h on four chips); the final grid on val-hard (k 0) = the memorization row. Every row carries its checkpoint path and `--ema`; the trace cross-checks the probe on the first query of every task.

**The decision rules (tools/analyze_decarc.py, selftest 24/24; the registry R at its top):**
- INTEGRITY: every arm present, one checkpoint per arm across its evals, the trace cross-check on every set, a val-hard summary per arm. STABILITY: no STOPPED / NAN_ABORT.
- R-DA-1 EXACTNESS: colour flip rate ≤ the fit-seed floor + 2 pp on every DEC arm → EXACT.
- R-DA-2 PARITY: val-hard clean exact of D0 / D1 ≥ the banked d64 plain twin's 7.64 % − 2 pp → PARITY; > 12 % [PILOT band] → ABOVE; below → BELOW.
- R-DA-3 DEPTH: exact at step 16 − exact at step 2 ≥ +2 pp → PROPAGATES; ≤ −2 pp → REGRESSES; else FLAT (regressions counted).
- R-DA-4 SELECTOR: residual-on-z AUC ≥ .85 AND oracle@32 − cold > 0 → CLEAN+COVERAGE; AUC ≥ .85 with no coverage → CLEAN-NO-COVERAGE; else DIRTY; the plain twin's AUC ≥ .10 below the pair's mean → MECHANISM.
- R-DA-5 RETENTION: FPA-start retention (eps 0) ≥ .90 on solved queries AND > 27.8 % (the natives' upper band) on all queries → BASINS-HELD; the oracle's conditioning on retention reported.
- R-DA-6 FAILURE: converged-wrong of converged ≥ .90 AND flips per cell ≤ 1.5 → FROZEN; else CHURN.
- R-DA-7 VOTE: vote pass@2 > the cold exact → VOTE-PAYS; else VOTE-FLAT (with any-view and the distinct-answers rows).
- R-DA-8 PUBLIC: the ARC-AGI-1 evaluation pass@1 and the vote's pass@2, reported with their columns (no rule).
- R-DA-9 CONTROL: N0's rg-96 ≥ 23.5 % → HIT (the d96 rung's registered band); < 21 % → KILL; else BETWEEN.
- SEEDS: |D0 − D1| on val-hard ≤ 3 pp → TIGHT. MEMORIZATION: the selected grid's monitor − the final grid's > 5 pp → MEMORIZED.

**Predictions (credences; bands to be re-read once against the pilot's pace, never against its numbers):** P1 EXACT on all three DEC arms (90 %). P2 PARITY on both seeds (55 %; BELOW 30 %; ABOVE 15 %). P3 FLAT on both (60 %; PROPAGATES 25 %; REGRESSES 15 %). P4 CLEAN+COVERAGE on D0 / D1 (40 %), CLEAN-NO-COVERAGE (35 %), DIRTY (25 %); MECHANISM (70 %). P5 BASINS-HELD (45 %). P6 FROZEN (75 %). P7 VOTE-PAYS (60 %). P9 CONTROL HIT (55 %; KILL 20 %). SEEDS TIGHT (60 %). MEMORIZATION: D0 / D1 AT-PEAK by 30k (55 %).

**The pilot (2–3 h on the v6e-8, before the night; ≈ $20):** the REAL chain under `GCS=gs://qhrrn2-rescue/decarc_pilot` with `DA_STEPS_DEC=2000 DA_STEPS_NAT=2000 DA_K_VH=4 DA_VIEWS_VH=2 DA_ARC1_VIEWS=1 DA_EXT_STEPS=0` on both workers: it reads (i) the preflight pace of every arm (it/s) and the HBM outcome at batch 64 (the OOM → --remat path), (ii) the 2k monitor's wall, (iii) the evaluator's walls per set (the fit per task, the k draws, the vote's re-fits), (iv) the shard/summarize/n-gate path on real outputs; it decides the width (w160 holds unless a DEC arm's 30k projects past 12 h on four chips → w128) and ARC1_VIEWS (8 unless the public row projects past 3 h → 2). The pilot's accuracy numbers are NOT read (the ops phase reads no accuracy values; the ARC pass's G1 full row — 48 tasks × 6 transforms on Dri / C53 — runs on the pilot node as its instrument slot).

**Walls (inferred until the pilot):** a DEC arm at w160, batch 64, 30k steps on four v6e chips ≈ 8–12 h (the champion's w384 at 729-token examples ran 1.05 it/s at batch 768 on four chips; the DEC-ARC's 9,000-token examples at batch 64 carry ≈ 0.8× the tokens and ≈ 2.7× the per-token arithmetic of the field's Sudoku cell); the battery per DEC arm ≈ 2–4 h on four chips (the arm-A fit ≈ 20–40 s per task on a chip; val-hard + dev-30 with 8 views ≈ 9 fits per task; the public row 400 tasks × (1 + ARC1_VIEWS − 1) fits); N0 ≈ 4–6 h + 1 h. Two rounds on two hosts ≈ 20–28 h.

## 10. Adversarial review and pre-mortem of the build (2026-09-10 evening; what the audit changed, what it could not verify, what would fool us)

**What the audit changed before the pod (each a defect the reading caught, fixed and re-tested):**
1. **The probe traces ignored the field cells' pinned dampings.** `probe_e1e3.trace`, `arc_suite.trace_conf` and the new orbit tool computed eta / eta_z from sigmoid(params) — right for the rg cells (learned, floor 0), wrong for any field cell (eta_z pinned at 1.0): the DEC-ARC's trace damped its carried latent by half and its preds diverged from `train.predict`. All three now read `model.eq_etas` (the rg cells bit-exact; `tests/test_decarc.py::test_decarc_deployed_fit_and_probe_trace`, `tests/test_probe_e1e3.py` green).
2. **The task-code table would not have trained under the field regime.** The natives train their code table at the model's lr 1e-3; the DEC-ARC's field regime is lr 1e-4 with wd 1.0, and a code row is touched only ≈ B × steps / n_tasks ≈ 1,000 times — the rows would stay near their 0.1-normal init and decay. TRM's ARC recipe trains its puzzle embeddings at lr 1e-2 with decay .1 for exactly this reason. `tools/pretrain.py --table-lr 1e-2 --table-wd 0.1` (optax.multi_transform over the two top-level keys; the default path byte-identical) is in the registry.
3. **The native control's gate sets.** The chain listed the rg-96 / rt-48 ids by importing the evaluator (a heavy import that fails silently on a stub node → an EMPTY list → the wrong set evaluated into the gate's directory). The harness caught it (S1's n-gate 48 ≠ 96); the ids are now listed from the data directories with an explicit GATE-TASKS-EMPTY abort.
4. **The `--cell` choice list, the analyzer's record paths and the stub's resume** — each caught by a smoke or the harness, each fixed.

**What the offline harness and the CPU smokes do NOT verify (the pilot's job):** HBM at the real batch (the attention scores of 10 fields × 1,024 cells per row: ≈ 170 MB per row per pass in f32 before remat; batch 64 over four chips = 16 rows per chip), the real pace (it/s) and hence the walls, the monitor's wall at n_val 96, the evaluator's per-task fit time on a chip and the 8-view re-fits' cost, the compile cache across shards, and the `--dp` table gradient under pmap on real hardware (the smoke ran single-device). The pilot reads exactly these and nothing else.

**What would fool us (pre-mortem), and the guard on each:**
- *The DEC-ARC trains but the codes do not condition* (every task the same answer family): the monitor's val_pix stuck near the background rate and `n_distinct` of the training predictions collapsing — read at the pilot's 2k monitor (val_pix must move) and in the night's first MONITOR rows; the guard is the table optimizer (audit item 2) and the pilot's read.
- *Exactness holds in the unit test but the deployed protocol flips colours* (the fit's own variance): the colour-flip row is measured against the seed + 1 re-fit floor (R-DA-1 reads the difference, not the raw rate), and the rate is on 144 queries at 5–10 % exact — small counts, labeled.
- *The VOID readout learns the size from the input's size* (most ARC outputs share the input's size): a right size on same-size tasks says nothing; `size1_ok` is read on the failures and on the size-changing tasks separately in the physics pass.
- *The 2k monitor selects a memorizer* (its queries are held-out queries of TRAINED tasks, not unseen tasks): the val-hard rows are the few-shot instrument; a MEMORIZED letter with a PARITY letter is the reading that says the monitor and the target disagree — the report separates them.
- *The k = 32 latent draws never leave the cold endpoint under RI* (Dri's condensation again): R-DA-4 reads AUC and coverage separately; CLEAN-NO-COVERAGE is a registered outcome, not a failure of the instrument.
- *The FPA start reads retention of the WRONG thing* (the anchor is read through the input's role table, so a void-structured answer embedding could be trivially stable): the eps ladder's rungs (.2–.8) separate a map that holds the truth from one that holds any embedding — a flat ladder at 1.0 across every rung would be the tell, and the analyzer reports the rungs.
- *The dihedral vote's re-fits make the vote a fit-variance vote*: the `n_distinct` and `correct_views` rows on solved vs failed queries are the consistency reading (the field's .76 / .001 separation is the comparator).
- *A preemption mid-round loses an arm's evals*: every row is banked by marker and re-run only if missing (S9); the pretrain resumes from the 5-min live bank (S11).
- *The public row's cost blows the wall*: ARC1_VIEWS drops 8 → 2 by the pilot's projection; the row is the last in each arm's battery so the val-hard / dev-30 / gate rows are banked first.
- *The rider eats the reserve*: RIDER=0 by default; RIDER=1 only on the PI's word at the end of round 2.

**Owed after the night, not before:** the DEC-ARC through the field's ARC-1 augmentation protocol (1,000 palette × dihedral copies with identities) is paper 2's comparison; the support encoder likewise; the census (explosion) instrument has no DEC-ARC form yet (the monitor's finiteness and the NaN guard stand in).

## 11. The design review before the second approval (2026-09-11 morning; the PI: "make sure the DEC-ARC design is the best attack we have for ARC to clearly demonstrate the differences in the paper")

**What §6 needs from this night, and whether the design delivers it.** The section's spine (D3 verification, D4 inventory vs search, D5 failure classes, D9 depth, D11/D12 memorization and the identity, D14 verifier dependence, D15 commitment) is at its strongest when ONE cell is read on both domains through ONE suite, so that no difference can be charged to the cell. The DEC-ARC is exactly that reading: the champion loop, the same objectives (RI + FPA), the same readouts (the halting head, the commit head), the same instruments (the depth row, k restarts with the residual selector, the FPA-start retention and ladder, C1–C3, the vote as the consistency verifier) on ARC, against the Sudoku DEC's rows already banked. Every registered letter (R-DA-3 depth, R-DA-4 selector value vs mechanism, R-DA-5 basins, R-DA-6 texture, R-DA-7 the vote) is a §6 sentence with the Sudoku twin beside it. The design is the right attack for the section; the review found two things to change and three to label.

**Changed (2026-09-11):**
1. **The field's ARC regime, not the Sudoku regime.** TRM trains Sudoku-Extreme at wd 1.0 / lr 1e-4 and ARC-1 at wd 0.1 / lr 2e-4 (constant after warmup; EMA on both). The DEC-ARC arms now use the field's ARC regime (`--wd 0.1 --lr 2e-4`), the code table at 1e-2 / .1 (§10). The cross-domain statement becomes "the same cell, loop and objectives, each domain under the field's own regime for it" — which is how the field itself operates, and it removes the objection that the Sudoku decay starved a 12k-pair corpus at batch 64. The memorization law is read on the monitor trajectory either way; a wd 1.0 arm on ARC (H-40's memorization-tax test) is a later cell, not this shot.
2. **The native control's public row.** N0 also runs the 400 ARC-1 evaluation tasks through the native suite (4-way sharded, merged, n-gated 400), so the paper's public number has a same-protocol native comparator beside the DEC-ARC's, and the field's 43.0 (identity table, 1,000 views) is the third column with its protocol named.

**Labeled, not changed (each a deliberate choice for a single shot):**
- *The conditioning is the natives' fit protocol* (§9): the cell is the only variable; the identity-free encoder is paper 2's. D7 applies to both cells identically.
- *Retention is trained for on D0 / D1* (the FPA anchors) — so R-DA-5 is read on D0 / D1 AND on D2 (no anchors): D2's retention is the cell's untrained basin reading, the pair's is the mechanism's; the natives' 19–28 % had anchor rows too (anchor_p .3), so the comparison is like for like.
- *Two comparators for the cell contrast:* the banked d64 natives (763k, n_val 20; matched parameters, "as-is") and N0 at d96 (the d96 rung's arm with the val back-port; matched selection instrument). Neither is a perfect twin; both are named.

**Why not a different arm set.** A "symmetry removed" arm (per-token colour embeddings without field sharing = the field's TRM-Attention on our protocol) would test Law 6 on ARC but is a second cell to build and cannot ride this shot; the field's released ARC model already gives that cell's rows under its own protocol. A wd 1.0 arm, a w128 arm and a support-encoder arm are the next cells in that order. D2 (the plain twin) stays because Law 3's value-vs-mechanism reading on ARC is the section's sharpest new sentence and needs the RI-free twin on the same cell.

**The Mac invariance rows (G1) are not a prerequisite for the pilot.** The DEC-ARC's colour exactness holds by construction and is tested in the unit gate; G1 measures the NATIVES' invariance under colour and dihedral transforms — the comparator column for R-DA-1 and a §6 row — and its full 48-task × 6-transform version is the pilot node's instrument slot. The reduced Mac run (24 tasks × 4 transforms × 2 maps) now runs as 8 parallel single-threaded processes (the sequential queue used ≈ 7 of 18 cores for one fit at a time).

**Hardware correction (2026-09-10 13:10Z, at the pilot's bring-up).** A v6e-8 is ONE host with 8 chips (pod.sh `accel_workers`, measured on the frontier run), not two hosts of four: the supervisor launches the chain once with CHAIN_WORKERS=1, the chain's 1-worker map runs D0 D1 D2 N0 in sequence with `--dp` over all 8 chips, and every battery shards 8-way. The chip-hours are unchanged; each arm's wall is about half the two-host estimate and the arms no longer overlap. The pilot measures the 8-chip pace directly. The Mumbai node came up in `UNHEALTHY_MAINTENANCE` health (a maintenance banner on every ssh); the supervisor's state table owns that case (a failed canary or bring-up strikes and re-hunts; a positively not-READY node is deleted).

## 12. The Sudoku lessons folded in before the registration (2026-09-15; the ARC reorientation; the PI: "get in top shape for this ARC reorientation with all the lessons we've learned from sudoku ... get the DEC-ARC build stress tested and ready for the pod"). NOT yet the registration: §9's rules stand; this section lists what the registration entry will carry beyond §9–§11.

**12.1 The evaluation start (the width-192 long run's CPU lens, `Report_2026-09-15_W192_Long_Verdict.md` §8).** An RI-trained cell trains every fresh row from z ~ N(0, σ) and never from its fixed buffers; on Sudoku the fixed start left and re-entered the basin as the weights drifted (COLD 8.6–99.2 % across 14k–150k) while every random start solved (RI / RIFIX 94–100 %, never lost). The DEC-ARC's monitor and every cold row used the buffers. Now (`decarc_cell.z0_eval`, `cfg.decarc_eval_start`, the registry flag `--decarc-eval-start fieldfix`): the monitor selects on, and the evaluator's cold rows, `predict` and the fit's validation start from, a SEEDED draw from the training family shared by every input — **tied over the ten colour fields** ("fieldfix": one draw per stream × cell × channel broadcast over the fields), so that it is exactly S10-invariant and the cell's colour exactness (R-DA-1) survives the start. The lens's own RIFIX (an untied draw) is NOT invariant: a colour permutation of the task would not permute its trajectory (`tests/test_decarc.py::test_decarc_exact_s10_survives_the_invariant_starts_not_rifix`), so it is a descriptive row only. The plain twin D2 (σ 0) keeps its buffers, its training start. The other starts of the same fitted code are measured beside the cold row at no re-fit (`--start-rows buffers,symfix,rifix`; the analyzer's descriptive START line: exact, agreement with the cold row, solved-then-lost) — the ARC twin of the lens. The FPA anchor's z_L stays the buffer (the state FPA trains the map to hold) in training and in the ladder alike. Consequence for the registry: every R-DA letter is read on the fieldfix start; the pilot's rows carried no science and are not re-read. Prediction stubs for the entry: on D0 / D1 the buffers row reads below the fieldfix row by ≥ 2 pp (55 %); symfix within 2 pp of fieldfix (50 %); rifix within 2 pp of fieldfix (60 %); on D2 every start equals its cold row (the same buffers; 95 %).

**12.2 The selection on a ~150-query monitor (the Night A instrument lesson; the paper-final extension lesson).** The 96-task monitor's exact count is an integer near 5–15 solved queries: ties at the maximum are the rule, and the raw monitor breaks few. A third key, the EMA pixel accuracy (`val_pix_ema`, now written by the trainer; `select_ckpt.py --third-key`), breaks them toward the closer map; the selection stays the earliest maximum. The extension rule keys on the PLATEAU'S END, not the argmax (`--plateau-pp 0.02`, `DA_EXT_PLATEAU_PP`): a DEC arm extends once when any banked grid within 2 pp of its maximum lies inside the last 4k of the budget (C8's extension missed by one monitor puzzle at 30k; the argmax inside a plateau is seed noise, `lens_selection_seed.py`). Both are knobs in the chain (unset = the registered rule, byte-identical; harness S17 / S18) and the ARC env sets them.

**12.3 The fit protocol and the battery (the pilot's cost finding; the PI's decision pending since 2026-09-11, the recommendation unchanged).** `DA_FIT_T=1` (one map application per fit step = every cell's training form; the deployed 16-pass fit is 624 h per arm) · `DA_FIT_STEPS=150` (retention is conditioned on a fitted code, pilot read §4) · the V2 battery `DA_VIEWS_VH=2 DA_ARC1_VIEWS=1 DA_K_VH=16 DA_ARC1_K=4` · `DA_COST_BUDGET_H=6`. The trace term (measured 2.9 s per t16 trace on the pilot against the probe's 0.34 s) is now ONE jitted stats call per step (`eval_decarc.trace_dec`, `--trace-fused 1`; the carried state's and the readout's updates are the eager ops they were, so the preds are bit-identical by construction — `tests/test_decarc_eval.py`; one descriptive field corrected on both paths: `res_y` is |softmax − y| before the readout update, as `arc_suite` defines it; no rule reads it). The cost probe times the battery's OWN trace on the chip and cross-checks the fused path against the eager path and the probe trace before any battery; a mismatch falls back to `--trace-fused 0` (harness S19) — the first-time component is staged, not trusted (ops rule 13). WALLS, CORRECTED 2026-09-15 evening (the first version of this paragraph priced the fits at the V1 count and left out the monitor wall; every number here is from the pilot's measured paces, the derived counts labeled): per DEC arm — preflight ≈ 0.1 h; the 30k pretrain 1.54 h at the measured 5.42 it/s; the fifteen 2k monitors 0.94 h at the measured 225 s each; the cost probe ≈ 0.2 h; the V2 battery's 904 fits at 150 steps × the measured 0.84 s (B 6; ARC tasks average ≈ 7 rows, so ≈ 1.0 s) = 3.96–4.6 h on 8 chips; ≈ 15,800 t16 traces (144 + 31 + 288 + 144 + 419 + ≈ 125 queries over the rows) ≈ 0.3 h fused (inferred ≈ 0.6 s per trace until the probe measures it; 1.6 h on the measured 2.9 s eager path); compiles ≈ 0.3 h on the first arm; ≈ 7.4–8.0 h per DEC arm, + 0.8 h when the extension fires (10k steps + five monitors). N0 ≈ 1.3 h (0.70 h pretrain at 15.96 it/s; its native fits at the night's protocol are untimed — the probe reads them). THE NIGHT on one v6e-8: ≈ 24–28 h (no or all extensions; fused), up to ≈ 32 h on the eager fallback; ≈ $165–190 at the US rate, $195–225 Mumbai, + churn (a preemption ≈ +0.5–1 h: the live restore and the recompiles). The cap is launch + 36 h, not 30. The cost probe's per-arm projection (≈ 4.9 h fused, ≈ 5.5 h eager against the 6 h budget) uses the probe's own B and a per-task trace count, so it under-reads the true wall by ≈ 15 %; the budget guards the fit protocol, not the schedule.

**12.4 Two rows added, one instrument each.** (i) `mon96` — the 96 monitor tasks' held-out queries with their TRAINED code rows and NO fit (`tools/pretrain.py` writes `val_tasks.json`; `eval_decarc.py --set mon96`; the chain's row after dev-30, k 0, the ladder from ε 0): the direct form of the retention instrument (the pilot: 0 % handed-truth retention at unfitted codes against `fpa_ce` .15 in training). The analyzer reads it as **R-DA-5b RETENTION-TRAINED** with R-DA-5's thresholds (BASINS-HELD-TRAINED when retention ≥ .90 on the solved queries and > 27.8 % on all), labeled "trained tasks, trained codes, no fit"; R-DA-5 (the fitted codes) stands as registered. (ii) `valhard_bridge` — N0 on val-hard at its deployed 6-pass fit, k 0 (`DA_BRIDGE=1`; the chain runs it on a free chip beside N0's four sets): the descriptive BRIDGE line ties the banked d64 twin's 7.64 % (deployed fits) to the night's T1 fits on one grid. Neither changes a registered letter.

**12.5 The ops layer for the ARC project (shared, spot only; `tools/HANDOFF.md` "THE ARC PROJECT" and "ARC READINESS").** `tools/campaign_decarc_arc.env` carries every knob above in `CHAIN_EXTRA_ENV` (the markers and the live bank under `gs://qhrrn2-arc/decarc`, the sets under `gs://qhrrn2-arc/sets`; harness S17 proves the chain writes nothing under the Sudoku era's prefixes). `pod.sh`'s state table learns the chain's `CHAIN-COST-ABORT` marker (a terminal state: the node torn down, exit 4, never a relaunch into it; `tools/harness_pod_state.sh` 12/12). The heartbeat reads the pod name, prefix and sentinel from the env. The launch ritual: the PI's spend envelope → the ~20-min canary in the ARC project (create `qhrrn2-arc-pod`, bring-up from the ARC bucket, the canary fit, delete) → the registration commit → the supervisor.

**12.6 Unchanged, and the decisions the PI owns.** Unchanged: the four arms and their flags (§9; only `--decarc-eval-start` added to the DEC arms), the corpus, the loop, the field's ARC regime, R-DA-1..9's thresholds and bands, the predictions' credences, the 30k budget + the extension, the 8-way sharding, the live bank, the drop order. Owed to the PI before the registration entry: (a) the fit protocol (12.3; T 1 · 150 · V2 recommended); (b) the plateau width (0.02 recommended) and the third key; (c) the zone order in the ARC project (the env lists US first, Mumbai last; the pilot ran Mumbai from the start on the PI's word); (d) the spend envelope on the lab's billing and the canary; (e) a later cell, not this shot: a w128 arm (the width × training-time lens: narrower cells sit on the generalizing side of the clock; w160 is the measured 2× MAC point), a wd 1.0 arm, the support encoder.

## 13. The launch: two pods in the shared ARC project (2026-09-15; the PI: "Let's launch the runs on the pod then. Carefully set up the Google cloud and monitoring environment as this is a shared project [the ARC project; id redacted]. Yes let's use two v6e-8 pods.")

**13.1 Decisions taken at the launch.** The PI launched on the staged recommendation for every §12.6 item and chose the two-pod lever: (a) T 1 · 150 steps · V2; (b) the plateau width 0.02 + the third key; (c) US zones first, Mumbai last; (d) the canary first, the spend band of 13.4; (e) w128 / wd 1.0 / the support encoder stay later cells. No science knob differs from §12 (the three envs' knobs hash-identical).

**13.2 What the shared project required (found read-only before any spend).** (i) The project offers only the `v2-alpha-tpuv6e` runtime for v6e; it rejects the proven `v6e-ubuntu-2404` with INVALID_ARGUMENT. pod.sh read every failed create as "no capacity", so an unmodified launch would have hunted forever without a node — now `TPU_RUNTIME` (the env) → the create and `QHRRN_TPU_RUNTIME` → the dispatcher, with a pip seed that works on either image. (ii) gcloud's TPU SSH installs our key on the node's own metadata (checked in the SDK source: `AddSSHKeyIfNeeded` updates the node, reads the project metadata only), so SSH never touches the shared project's settings. (iii) The code archive the nodes pull lives in a bucket lab members can read, so it now excludes the git-ignored identity files and backups. (iv) Our fleet and queued resources there were zero; the bucket US multi-region with uniform access, public access prevention and the staged inputs intact.

**13.3 The canary (the first node in the project; `tools/arc_canary.sh`, driven by markers).** Driven by markers, 15:24–15:40Z, ≈ 16 min of node life, ≈ $2. The create with `v6e-ubuntu-2404` was rejected (INVALID_ARGUMENT, "The field RuntimeVersion cannot be v6e-ubuntu-2404"); `v2-alpha-tpuv6e` was granted in us-east1-d as a labeled spot v6e-8 after ≈ 7 min in CREATING. `dispatcher up --with-data` finished in 217 s: sshd came up after five retries, the code archive was pulled from `gs://qhrrn2-arc/decarc/ops` (the node's service account reads our bucket), the venv restore missed as expected, the full bootstrap ran on Ubuntu 22.04.5 with system Python 3.10 (the pip seed's fallback path), and the venv was banked back to the bucket (it writes there too; the pods restore it). The canary fit (`eval_pop.py`, 60 steps) PASSED. On the node: gsutil and gcloud present; the venv's Python 3.14.7; JAX lists 8 TPU devices; a gsutil write, stat and delete of a test object in our bucket all OK; the default service account's scopes include cloud-platform; PyPI answers 200; 180 vCPUs, 1.4 TB RAM, 85 GB free. The node read its own state and then deleted ITSELF with the node-side guard's exact command; the Mac read NOT_FOUND after 80 s — the Mac-independent backstop works in the shared project. Fleet zero after.

**13.4 The two-pod shape.** Pod 0 = worker 0 of 2 (D0 then D2), pod 1 = worker 1 of 2 (D1 then N0) — the registered two-worker map, so the seed pair runs first and in parallel. Each pod: its own supervisor (`POD_ENV`), its own live prefix, `DA_SHARE_EXIT` (the pod finishing its share first banks `SHARE_DONE_w<W>`, its supervisor tears it down; the other finalizes). Found and fixed in the same build: an ARM-PARTIAL used to sit in the completion loop for C1_WAIT_PASSES × 120 s (20 h) before its relaunch; the worker now exits 1 at once when its own arms are incomplete. The guard and the DMS push now follow every (re)launch (the owed 2026-09-08 lesson). And the harness's two-node scenario caught a real defect in the finish: the finalizer's pull cache keyed on a file's presence in /tmp, not on its extraction, so a pull interrupted by a wall recycle would have been skipped unextracted and the final tarball banked without that row; the cache now records a successful extraction, and a gate refuses to bank a final missing any row whose OK marker exists (FINAL-INCOMPLETE → exit 1 → the relaunch re-pulls). Walls: pod 0 ≈ 15.5–18 h (≈ 20.5 h eager), pod 1 ≈ 9–10.5 h; the campaign ≈ 16–18 h. Spend ≈ $170–215 at the US rate, ≈ $200–230 in Mumbai, + churn. Cap launch + 30 h.

**13.5 Monitoring.** Layer 1: the launchd watchdog over both projects (only `qhrrn2-*` in the shared one; deletes past the deadline). Layer 1b: the node-side delete guard at the deadline, re-planted after every launch. Layer 2: `tools/ops_watch_pods.sh` (the per-pod heartbeat in single-tick mode; ends on an alert, a pod's share-done, the completion or the hour). Layer 3: the hourly heartbeat to the PI from those wake-ups.
