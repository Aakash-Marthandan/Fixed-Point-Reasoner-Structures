# sportC2 — the pre-parity graft night at d128 (REGISTERED 2026-09-04 — ledger §5 LAUNCH REGISTRATION; rules locked in `tools/analyze_sportC2.py --selftest`; launch on the explicit PI go)

**Date:** 2026-09-04 · **Status:** BUILD COMPLETE, harness + smokes + pre-mortem done (§8) → REGISTERED (ledger §5 2026-09-04 LAUNCH REGISTRATION) → launch on the explicit PI go · **PI decisions already taken (2026-09-03 evening):** the arm set as tabled in the freethink is APPROVED; the ARC comparison builds/rung are DEFERRED until after the Sudoku champion work (sportC2 → sportC3), carrying everything learned. · **Inputs:** `Research_Brainstorm.md` "Freethink 2026-09-03" (X-4 the amalgamation principle, X-7 the design, §0 the nine groundings), `Report_2026-09-03_Champion_sportC1_Verdict.md`, `Program_Review_2026-09-03.md`.

## §1. Purpose
Test, one variable at a time and with an instrument that reads the mechanism, the four levers the freethink derived from the measured deficits of our cell — reachability (ρ ≈ .5 vs the field's .98), per-step propagation (8–15 outer steps vs 1–2), calibrated commitment (top-5 confident cells at stalls only 70–72 % correct at p .96), and the memorization regime (wd 1e-4 collapses curvature and erases the low-givens capability) — plus the two additive reads on the field's cell (its digit orbit; our geometry on its loop) and the undertrained read on R0, BEFORE the parity run commits a recipe at d160.

## §2. Arms (d128 ws8 native9 unless X; every measured champion ingredient kept: z-norm, FPA k4 ε.2, β_nl 1e-6, exact S9, aug 1000, RI .5, val-selection, EMA row)

| arm | one variable (from its base) | base | steps / regime | worker |
|---|---|---|---|---|
| **W0** | `--wd 1.0` | sportC1 B0 (our regime: lr 1e-3, B64, two-phase 50k cosine + 30k floor) | 80k, two-stage | w0 |
| **R1** | `--sot --sot-segments 4` (the persistent (y, z) carry; rows replaced when EXACT or after 4 segments = t 64) | W0 | 80k, two-stage | w1 |
| **R2** | `--inner-k 3` (3 latent passes per readout, trained; evaluator/census/monitor/calib mirror it) | W0 | 80k, two-stage (≈ 3× per-step cost) | w0 |
| **R3** | `--hard-p 0.5` (per outer step, the feedback is the argmax one-hot with a straight-through gradient; deep supervision at later steps IS the revision signal) | W0 | 80k, two-stage | w1 |
| **R4** | `--init-from` sportC1 R0's 50k ckpt, +50k steps (fresh optimizer, EMA restarted — labeled) | sportC1 R0 (field regime: wd 1.0, lr 1e-4 const, batch 384, remat) | 50k continuation | w2 |
| **X1** | no `--sudoku-digit-aug` | sportC1 X0 (TRM cell + SOT + ACT, field regime) | 50k | w2 |
| **X2** | `--trm-token-mixer group9` (our group mixer on a 64-dim projection replaces the token-mixing MLP; prefix tokens get a pooled cell summary; params ≈ 5.9M, labeled) | X0 | 50k | w3 |
| riders | B0/B1 20k scans + B0's EMA full on the CORRECT A:20k grids (the sportC1 provenance gap); canvas C3X/D4 EqR-statistic evals (owed since the pilot); `stall_calibration` on every arm's vsel grid (standing) | — | — | w3 |

**Deviation from the freethink table, stated:** R1/R2/R3 are grafted on the W0 base (our lr/schedule with wd 1.0) rather than on R0's field regime — the field regime at batch 384 puts an 80k K=3 arm at ~21 h on a 4-chip worker (infeasible in one night) and a common fast base makes the three grafts comparable; R4 keeps the field-regime read. If W0 memorizes anyway, the grafts read at their val-selected grids (the sportC1 convention) and R-C2-0 says so.

## §3. Instruments per arm (all standing; the three freethink lenses run at analysis time from records)
Screens strat-512 k256 at {stage-A end, B+5k, B+15k, vb} (natives) / {15k, 35k, vb} (R4, X); retfm; full tests at vsel AND final (+ the alternate-weights row); the 20k k128 scan at vsel (b1, verified, t1r, per-draw records); explosion census on vsel AND final (t64 + t256); **stall calibration** (`tools/stall_calibration.py`: top-5 correctness / confidence / entropy on stalled puzzles, strat-512) on the vsel grid; **R3 only (pre-mortem addition, descriptive, no rule):** the same vsel grid read in its HARD-feedback inference mode — `screen_R3_vb_hard` (strat-512 cold, `--hard-feedback`) and `calib_R3_vsel_hard` — because R3 trains with hard feedback on half its steps and every registered row evaluates it soft; at analysis: reachability-vs-givens, the flip spectrum, curvature concentration.

## §4. Decision rules (locked verbatim in `tools/analyze_sportC2.py`; selftest green before launch)
INTEGRITY adds the sportC1 lesson: every vsel-labeled eval of an arm on ONE checkpoint path (breach withholds). STABILITY (admission) = not STOPPED ∧ retfm ≥ .9 ∧ census ≤ .02; MEMORIZATION (end CE < .02) is reported separately and never disqualifies the val-selected grid. CNC2 = max(|vcold(W0) − .4283|, .01). R-C2-0 REGIME (W0): CE ≥ .05 ∧ val-peak ≥ 25k ∧ vcold ≥ .4283 + CNC2 → WD-HOLDS / CE < .02 → WD-MEMORIZES / else WD-PARTIAL. R-C2-1 CARRY (R1): reach (verified@128) ≥ .70 WIDENS / ≤ .55 FLAT / PARTIAL; + SELECTOR-INTACT iff t1r/verified ≥ .97. R-C2-2 DEPTH (R2): median first_exact ≤ ½ W0's → PROPAGATES; reach at 21–25 givens ≥ .50 → REACHES; neither → FLAT. R-C2-3 COMMIT (R3): calib top-5 on stalled ≥ .90 CALIBRATED / ≤ .75 FLAT / PARTIAL; + SPURIOUS-APPEAR iff t1r/verified < .97. R-C2-4 (R4): vcold − .3733 ≥ .03 CLIMBS / ≥ .01 SLOW / FLAT. R-C2-5 (X1): .8603 − cold@D16 ≥ .03 ORBIT-LOAD-BEARING / ≤ .01 ORBIT-LEARNED / PARTIAL. R-C2-6 (X2): cold@D16 − .8603 ≥ .02 HELPS / ≤ −.02 HURTS / NEUTRAL. R-C2-7 CHAMPION-RECIPE (mechanical): levers with their primary letter (WIDENS / PROPAGATES-or-REACHES / CALIBRATED) on a STABLE arm with vcold ≥ vcold(W0) − CNC2 are carried to sportC3; wd 1.0 carried iff HOLDS/PARTIAL; champion-so-far = argmax vcold over stable non-memorized natives.

## §5. Predictions (numeric, pre-data)
W0 vcold [43, 50], CE end ≥ .05 (55 %) · R1 reach [.65, .85] (50 %), vcold [42, 50], selector intact (80 %) · R2 first_exact median ≤ 6 vs W0 ≈ 10 (55 %), vcold [44, 52] · R3 top-5 ≥ .90 (45 %), entropy at step 1 < .3, vcold [42, 50] · R4 vcold [39, 44] (60 %) · X1 cold@D16 [80, 85] (a drop of ≥ 1pp, 60 %) · X2 [84, 88] (neutral, 60 %). Kills as in the freethink X-7 table.

## §6. Cost, wall, ops
v6e-16 4×4 static map w0 W0→R2 · w1 R1→R3 · w2 R4→X1 · w3 X2→riders; estimated walls (4-chip workers, measured sportC1 paces): W0 2 h + R2 ≈ 5.5 h (+ evals ≈ 4 h) ≈ 12 h; w1 ≈ 8 h; w2 R4 ≈ 7 h + X1 2.5 h + evals ≈ 12 h; w3 X2 ≈ 3 h + riders ≈ 2 h + evals ≈ 7 h → ≈ 12–14 h ≈ $170–230 (US 16 $13.64/h; Mumbai ≈ $16/h). 8-shape fallback (sequential, PI-approved deadline): W0 R1 R3 X1 R2 R4 X2 in priority order. Deadline launch + 16 h (16) / PI-gated on the 8. **`runs/tpu_deadline.txt` is in the past — the launch procedure bumps it first.** Chain `tools/chain_sportC2.sh` through `tools/harness_sportC2.sh` (all sportC1 scenarios incl. S8 node-change restore, + S9 launch-time OOM retry) + CPU smokes on real code (§8) before silicon; pre-mortem after the harness (§8). **Wall register (inferred from sportC1's measured paces; the canary/first rows re-price):** w0 = W0 ≈ 4 h (80k at ≈ 21 it/s on 4 chips ≈ 63 min + ≈ 2.5–3 h of evals) + R2 ≈ 9–10 h (K = 3 triples the per-step cost of BOTH the pretrain, ≈ 3.2 h, and every eval, ≈ 6–7 h) ≈ **13–15 h = the long pole**; w1 = R1 + R3 ≈ 8 h; w2 = R4 ≈ 7 h (batch 384 with remat on 4 chips ≈ 2 it/s) + 2 h evals + X1 ≈ 4.5 h ≈ 13.5 h; w3 = X2 ≈ 5–6 h + riders ≈ 3 h ≈ 9 h. Deadline launch + 16 h; the B1 projection gate (hold + PI framing if the measured paces project the 16-shape past 1.2 × 14 h ≈ 17 h); the 8-shape sequential ≈ 20–22 h needs the PI-approved extension. Launch-time HBM OOM (R2's 3× unroll is the one untested memory point): `pt_run` retries ONCE with `--remat` (numerics-equivalent, `tests/test_e10.py::test_eq_remat_matches_no_remat`; labeled on disk in `RETRY_REMAT.txt`; harness S9) — never a NaN-death amputation.

## §7. Labeled deviations
Grafts on the W0 base (§2); R4's fresh optimizer + EMA restart (`--init-from` loads R0's RAW 50k weights, not its EMA; step counter restarts at 0 so R4's screens at 15k/35k are effective 65k/85k); the SOT carry not checkpointed (a resume restarts rows); X2's parameter count (≈ 5.84M vs X0's 5.04M) and prefix summary path; hard rows are a per-step whole-grid commitment (not per cell) and R3's registered rows evaluate it SOFT (the hard-mode rows of §3 are descriptive); inner cycles multiply per-step compute ×3 (the matched-inference-compute row is the parity run's, not this night's); W0's decoupled decay RATE (lr × wd = 1e-3 at the cosine peak, ≈ 5e-4 on average) is 5–10× the field's 1e-4 — an over-regularized W0 reads WD-PARTIAL by the letter (CE ≥ .05 with vcold below B0 + CNC2) and is labeled so; the matched-RATE variant (wd 0.1 at lr 1e-3) is the sportC3 contingency, not a change to the registered arm; R1's MEMORIZATION gate reads the SOT-rg loop's mixed-row CE (fresh rows + carried STUCK rows, biased upward vs W0's fresh-row CE) — the vsel-vs-final cold gap is the corroborating descriptive read; a launch-time OOM retry with `--remat` on any arm is labeled in its pretrain tarball.

## §8. PRE-MORTEM AUDIT of the sportC2 build (2026-09-04, run AFTER the harness; the sportC1 §13 method: data-validity first, delay second, each item → a harness scenario, a test, a smoke, or a procedure)

**Catches (fixed before this section was written; each now harness-, test- or smoke-covered):**
- **PM-C2-1 (harness construction, the PM-5 class):** the first sportC2 harness run read 21 PASS / 24 FAIL — every FAIL was the sandbox still carrying sportC1's arm names (a fake `sportC1/R4_ckpt.pkl` where the chain's `fetch_init` pulls `R0_ckpt.pkl`; rider tarballs `R2a/R3a` where the chain reads `B0a/B1a`; assertions on `A0/A1/R0/X0` dirs), so R4 never started and every "complete" scenario read INCOMPLETE. The chain itself passed every scenario it reached. Fixed: every scenario names sportC2's arms; re-run = 49/49 (S1–S9 incl. the negative S9b/S9c; run 1 read 21/24 on sandbox arm names, run 2 47/48 on the S9b stub guard, run 3 49/49).
- **PM-C2-2 (data-validity, the mislabeled-death class):** a LAUNCH-TIME HBM exhaustion (rc ≠ 0 before any step is logged) would have entered the one-shot NaN path → `AMPUTATE-FAILED` (no finite grid) → the arm lost and labeled as a death. R2's K = 3 unroll (48 + 4 FPA passes per row at 16 rows/chip) is the one memory point no prior campaign measured (X0's 192 rows × 14 blocks trained without remat at ≈ 11 GB; R2 is estimated ≈ 10 GB with 2× uncertainty). Fix: `pt_run` — if rc ≠ 0 AND no loss row was logged AND the log carries an OOM message AND the args lack `--remat`, retry ONCE with `--remat` (numerics-equivalent by the named test), echo `PRETRAIN-OOM-RETRY-REMAT`, write `RETRY_REMAT.txt` into the pretrain dir (ships in the tarball); an arm already on `--remat` (R4) is not retried (stop-and-report). Harness S9 (positive on R2a; negative on R4).
- **PM-C2-3 (instrument coverage):** R3 trains with hard feedback on half its steps; every registered row evaluates it soft (the registered convention: the commitment is trained in, inference stays the shared loop). The mechanism it tests deserves one read in its own mode → `screen_R3_vb_hard` + `calib_R3_vsel_hard` (labeled, descriptive; §3). Harness S1 asserts them on R3 only; the evaluator's and the calibration tool's `--hard-feedback` paths smoked on real code (§8 smokes).
- **PM-C2-4 (the ops source of truth):** `campaign_sportC2.env`'s header still described sportC1's arm set (a copy-paste artifact); the values were right. Fixed.

**Data-validity register (green, each verified on disk or by a named test):** the analyzer's grid-consistency INTEGRITY gate reads `ckpt` from every vsel-labeled summary — verified that the REAL tools emit it (sportC1's banked `summary_all.json` of full/scan/retfm/screen and `census.json` carry `ckpt`; `stall_calibration.py` writes it); `reach_lowgivens` reads `test_q` from the npz (key present; 422,786 × 9 × 9 int8); `fe_median` reads `first_exact` + `cold_exact` from the MERGED full records (present in sportC1's `records_all.npz`); `val_peak_step` reads BOTH stage dirs' monitors (the stage-A dir ships at STAGEA-OK and is in the final bundle's glob; harness S1); every eval of an arm runs on the ONE `$VBCK` path (a resume that changed the selection would fire the gate — the intended behaviour; S8 covers the stage-A restore); `select_key` / `HEAD_EMA` / `HEADLINE_T` agree between chain and analyzer (raw val_t64 on W0/R1/R2/R3, val_t64_ema on R4, val_t16_ema on X1/X2; X headline D16 with the D64 depth row + the t64 k128 scan); `inner_k` is mirrored from the ckpt config by the evaluator, the census, the calibration tool and the monitor (smoke: the R2 ckpt's summaries report `inner_k 3`); X1/X2's base = sportC1's X0 flags verbatim with the digit-aug flag moved to the arm line (diffed); R4's `--init-from` asserts tree equality with R0's config (native_common identical across the two chains, diffed); the trainer's resume path takes precedence over `--init-from` (smoke: second launch reads RESUMED, `resumes.txt` records the step); the SOT-rg loop accepts `--init-from` (the stage-B pattern) and DP (smokes); the R1 memorization-gate bias and W0's decay-rate reading are labeled in §7; the trm + group9 checkpoint round-trips through every tool the chain calls (smokes: evaluator full-style and screen-style with vote-unverified/b1/t1r, census, calibration, select_ckpt on `val_t3_ema`).

**Delay register:** §6's wall register (w0 ≈ 13–15 h is the long pole; deadline launch + 16 h; B1 gate 1.2×; 8-shape needs the PI-approved extension); the 8.5 h wall recycle resumes every arm from its 1k-cadence `ckpt_latest` (R1/X1/X2 restart their carries, labeled); evals resume from 300 s partials; the wait loop (200 × 120 s = 6.7 h) covers the w1-vs-w0 skew (≈ 6 h); spend tripwire 1.5 × $230 = $345; chip pins per worker sequential (no pin-collision class); `--merge` on CPU.

**Smokes (CPU, real code, this session; log in the session scratchpad, rc = 0 on every line):** X2 tiny (trm + SOT + ACT + digit aug + group9 token mixer) single + DP-2, its checkpoint through the evaluator full-style (EMA) and screen-style (strat, k2, vote-unverified: ckpt/ema/cell_kind/inner_k/hard_feedback/b1/t1r/vote/majority keys present), the census, the calibration tool and `select_ckpt --key val_t3_ema`; R4-style source run (field regime, remat, EMA) → `--init-from` continuation (INIT-FROM, step 0, fresh optimizer; monitors on val_t64_ema; select_ckpt) → a second launch RESUMED from `ckpt_latest` at step 20 with `resumes.txt`; R1-style SOT-rg loop with `--init-from` at the floor lr (the stage-B pattern; 'SOT-RG: 8 rows; segments <= 2') and DP-2 fresh ('SOT-RG DP: 2 devices, 4 rows/device') → evaluator k2; R3-style hard_p .5 training → evaluator and calibration in `--hard-feedback` mode (summaries carry hard_feedback true) and soft; R2-style inner_k 3 → evaluator, final-map retfm-style eval, census and calibration all report inner_k 3 from the ckpt config; W0-style wd 1.0 at lr 1e-3 stage A → stage B `--init-from` at the floor lr — every line rc 0

**Owed after the night (not blocking):** the seven freethink grounding scripts promoted to `tools/` (the three lenses run from records at analysis, $0); the MAC-count instrument; the spend_report zone-aware fix.
