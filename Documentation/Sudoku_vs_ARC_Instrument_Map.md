# The Instrument Suite Across Domains — Status at Scale, and the Sudoku-vs-ARC Differences Catalog

**Date:** 2026-08-27 · **Anchors:** the Basin Measurement Note (2026-08-12) and its Update Log (2026-08-19, Professor review set) define the ARC-era suite; ledger §5 entries define every status claim. This document does two jobs the program now owes: (1) account for every instrument in the Professor-facing suite — what ported to Sudoku, what is waiting, what hit a limit at d64; (2) catalog the measured differences between the two domains — the section paper 1 promises ("the field puts them in one boat; our instruments separate them").

---

## Part 1 — Instrument-by-instrument status

The Basin Measurement Note's five core instruments, its 08-19 additions, and the Sudoku-era additions since. Status vocabulary: PORTED (same code or bit-comparable protocol), ANALOG (different instrument, same question), STRUCTURAL-ARC (requires ARC's structure; not portable, not lost — waits for the convert phase), OWED (registered, unbuilt), LIMIT (working but out-ranged at d64, fix named).

| Instrument (note §) | What it measures (plain) | ARC status | Sudoku status at d64 | Note |
|---|---|---|---|---|
| **Retention** Ret(g*), + final-map variant retfm | handed the correct answer, does the map keep it — the basin-existence probe | backbone since E10 (6%→29% repair; vacancy floor) | **PORTED, scale-robust**; the schedule/final-map split (wave 3a) discovered the collapse law; caught the d64 collapse instantly | the single most load-bearing instrument on both domains |
| **Corruption ladder / basin radius** Lad(ε), S(ε), r̄ | how much corruption of the answer the basin absorbs — code distance | the radius laws (L3, record .80 @d48/40k; r̄ .22–.28; d64 ladders unsaturated) | **PORTED; LIMIT at d64**: S(ε)=1.00 through ε=.4 on every arm — Sudoku basins at width are wider than the deepest tested rung. Fix owed rung 2: larger ε rungs (e.g. .6/.8) or graded-distance readout | the saturation is domain×scale: ARC's d64 ladders still discriminate; Sudoku's single-attractor basins are intrinsically wide |
| **Codebook size N + packing plane (N, r̄)** | how many distinct rule-basins the substrate holds, at what typical radius | the width-ceiling chart; L2's separability | **STRUCTURAL-ARC** — Sudoku has one rule; N degenerates to 1. Conceptual descendant measured this rung: per-puzzle endpoint statistics (n_distinct across draws), whose late-training **condensation** on the priced arm (H-47) is an N-like collapse in draw space | "codebook vs single codeword" is difference D1 below |
| **Cold quench** exact@T | one deterministic attempt from a blank start | ported concept (exact@sel) | **PORTED** = the cold/single-attempt headline (25.27 % at d64) | identical meaning both domains |
| **Thermal capture / barrier spectroscopy** (Langevin T-sweep, Arrhenius fits) | can temperature kick trajectories over barriers into truth basins | anti-Arrhenius verdict (hop rate falls with T; walls = absent basins; cold candidates law) | **ANALOG, superseded**: Sudoku's free verification makes the *verified multi-init funnel* (vote@k, first-hit records) strictly stronger for the same question; no temperature axis needed | ARC keeps the thermal result as knowledge; the funnel is the Sudoku-native form |
| **Throat / flux ledger** I_s, I_total, β/β_nl | information crossing each internal cut; the toll | Law 1 (785→459 declining, UV-led; floors; profiles) | **PORTED, EXTENDS**: priced throat ≈ 967–985 nats at d64 vs 1.0–1.6k at d16 — task-set constant across 12× params; free flux inflates ~4× (S2). Per-scale I_s via probes intact | the *meaning* differs by domain — difference D6 |
| **Spectral profile distance** (knee/free fixed points) | which universal flux shape the code sits on | two-profile structure, H-34 diagnostic | **PORTED**: Sudoku's third profile (~.93 UV share) replicates at d64 — profile is a task-geometry readout, diagnostic use is within-domain | |
| **rg-96 / rt-48 / val-hard gates** (three-level distance-from-training ladder) | generalization to never-trained rule families / fresh instances of trained families | the transfer laws (L4 seeded at three widths; OOD gradient) | **STRUCTURAL-ARC** — no rule families exist on Sudoku. The Sudoku analog of the generalization axis is the **rating axis** (1k training puzzles → 423k test incl. far harder; the funnel spectroscopy per octile is the instrument) | difference D2; Law 4 is ARC-scoped for exactly this reason |
| **E1/E3 battery** (wrong-stable fraction, endpoint condensation, truth-erasure) | how many attractors exist, how many are true, whether truth survives the map | the reachability wall (92 % wrong-stable); E3b truth-erasure; nd1 condensation | **PARTIAL ANALOG + OWED**: violations-at-stuck-states (median counts) and multi-init endpoint stats serve; the formal Sudoku wrong-stable instrument ("spurious attractor = stuck state with ≥1 violation", registered 2026-08-15) remains unbuilt | difference D5 (failure classes) |
| **η (damping / flow constant)** | how much of each step's proposal the state accepts | dictionary: tracks optimization length; price effect decays with width | **PORTED**: at d64 reads as stability/mechanism fingerprint (NI→0.995 replacement dynamics; FPA .85; priced .82–.89) | same scalar, different diagnostic role per domain (documented in the 08-19 dictionary) |
| **λ (update-Jacobian eigenvalue, power iteration)** | local expansion/contraction at the solution | never built for ARC (η + retention served) | **Sudoku-born (wave 3a); LIMIT at d64**: the joint-state version false-alarms on healthy wide arms (scratchpad modes expansive, answer-mode contractive — third instrument-contract case). Instruments of record: retfm + ret_sched + answer-mode λ | back-port to ARC rungs = suite upgrade owed when ARC resumes |
| **Trajectory monitors + val-best checkpoint selection** | accuracy/stability along training; legitimate early stopping | never existed for ARC pretrains | **Sudoku-born (wave 3a), scale-robust**: at d64 selection ≈ final on healthy arms and correctly reached back to step 5000 on the collapsing one. Cadence LIMIT: 5k too coarse for d64 collapse onset → 2k at rung 2 | back-port to any future ARC pretraining (checkpoint variance was ARC's dominant noise too) |
| **Funnel instruments** (nested vote@k, per-draw verified first-hit, per-rating spectroscopy) | how effectively many random starts convert to verified solutions, by difficulty | partial analog only (multi-init coverage; no free verifier → vote/snap needed) | **Sudoku-born (wave 2+), now the sharpest tools**: ownership flip, tail-first growth, condensation all theirs | difference D3 (verification) is why these are Sudoku-native |
| **Fisher tools** (probe_aniso subspace sensitivity; EWC/KL-anchored TTT; Adam-v Fisher) | where the code is stored / where learning pressure concentrates | aniso probe + TTT anchors = ARC/TTT-layer instruments | aniso/TTT **STRUCTURAL-ARC** (no TTT on Sudoku — difference D7); **Fisher-from-Adam-v first-used this rung** (consolidation signature: eq→readout migration on stable arms; no-consolidation on the collapsing arm; code-block concentration on priced) | |
| **Vacancy floor register** (per-family zero-basin table) | families the architecture holds no basins for (Copy/ExtractObjects/Count on 37+ substrates) | standing architectural finding | **STRUCTURAL-ARC** (needs family structure) | |
| **Measurement law** (n≥3 or pairing; sub-10-count deltas non-evidence) | statistics floor | binding | **binding, domain-general**; this rung was seed-thin (one pair) and is labeled accordingly; rung 2 seeds the carrier | |

**Suite summary at d64.** Everything portable ported and survived scale except two range limits (ε-ladder saturation; joint-λ contract) and one cadence limit (5k monitors) — all three *caught by the suite's own redundancy* (retention disagreed with λ; the pegged ladder was self-evident), none corrupted a conclusion, and each has a named fix in the rung-2 protocol. The ARC-structural instruments (N, packing plane, rg/rt gates, vacancy floor, TTT/aniso layer, thermal spectroscopy) are not lost — they are the convert-phase suite, waiting where ARC's structure exists. The Sudoku era *added* five instruments the ARC suite never had (final-map retention, λ, trajectory monitors + val-selection, verified funnels, Adam-v Fisher) — the back-port of these to ARC is a registered upgrade when the ARC campaigns resume.

---

## Part 2 — The Sudoku-vs-ARC differences catalog (paper-1 section material)

The field (HRM → TRM → EqR → FPRM) evaluates Sudoku and ARC as one benchmark family. Our instruments say they are structurally different problems, and every difference below is *measured*, not asserted.

**D1 — Rule inventory vs single rule.** ARC is a many-rule problem: hundreds of rule families, each task requiring few-shot inference of *which* rule applies. Sudoku has one universal rule known in advance; every instance is a completion problem. Consequence: ARC's central capacity question is the **codebook** (how many rule-basins can the substrate hold — N, the packing plane, the vacancy floor); Sudoku's is the **landscape of a single attractor** (how reliably is the one basin found). Instruments N/(N,r̄) exist only on ARC; endpoint statistics are their Sudoku shadow.

**D2 — What "generalization" means.** ARC: transfer to never-trained rule families (the rg gates) — the axis where pricing pays (Law 4, seeded at three widths). Sudoku: harder *instances* of the same rule (the rating axis; 1k training puzzles → 423k test set) — the axis the funnel spectroscopy measures. Law 4 is ARC-scoped because Sudoku has no unseen families to transfer to.

**D3 — Verification.** A completed Sudoku grid self-verifies (uniqueness ⇒ any valid completion is *the* solution): breadth is a clean protocol (draw, check, accept), and the many-attempts metric is exact. ARC has no verifier: candidate answers must be *selected* (vote, snap, stability scoring, pass@2 conventions) — the entire ARC conversion stack (populations, snap-vote, MDL selection) exists to substitute for the missing verifier. This is the single deepest protocol difference and it decides which instruments are native to which domain.

**D4 — What limits performance (the landscape-class law, quantitative).** ARC is **inventory-limited**: basins exist for only a minority of task pairs (retention ~29–34 %), and where they don't exist no amount of iteration, temperature, or attempts helps (t=96 extension: +0 solves; anti-Arrhenius; 31× basin-existence conditioning). Sudoku is **search-limited**: basins exist essentially everywhere (retention ≈ 1.0 at every difficulty), and performance tracks the search the instance requires (extended iteration +16/40 at deep levels; per-draw hit rate geometric in rating; vote@k log-linear). The same two instruments (retention; extended iteration) give opposite verdicts on the two domains — the program's cleanest two-sided result.

**D5 — Failure classes.** ARC failures are **wrong-and-stable**: 92 % of converged limits are confidently wrong attractors; the map actively erases handed truth on the old substrate. Sudoku failures are **near-misses**: stuck states carry a median of ~3 constraint violations with all givens intact — incomplete propagation/search, not capture by a wrong basin. (Formal Sudoku wrong-stable instrument = registered-unbuilt; violations serve meanwhile.)

**D6 — Information geometry.** ARC: the throat is a **task-set constant** (785→459 nats, declining with capacity, UV-led) and the priced profile is IR-fattened (the knee) — the code spends on *rule structure and transport*; pricing forces abstraction (anti-water-filling). Sudoku: the throat is **instance-conditional** (801/987/1528 nats at 50/40/30 givens — harder instances route more information; the ledger becomes a per-instance required-computation meter) and the profile is extreme-UV (~.9 at the finest scale) — a local constraint-propagation geometry. Same ledger, different physics readout — the profile is a property of the task distribution, not the architecture.

**D7 — Per-task adaptation.** ARC requires a TTT layer (few-shot rule inference from supports; the basin-preservation constraint on adaptation; keyhole vs LoRA vs full-fit adjudications). Sudoku has no supports and no TTT at all (the EqR/FPRM convention we share). Consequently the whole TTT instrument family (anisotropy probe, KL/EWC anchors, adaptation-budget laws) is ARC-only.

**D8 — What the price buys, by domain (the sign-flip arc).** ARC: a transfer dividend (Law 4) at a mild cold cost — the task's information is an abstract rule, and pricing forces the abstraction. Sudoku: at small scale the price *killed* the solver (the working grid is the information; pricing starved it — H-43); at larger scale the sign flipped to a **stability dividend** (H-44), which at d64 became load-bearing (the un-priced shallow map is broken; the priced twin is whole), with the newly measured cost that **precision and draw-diversity compete for the fixed budget** (H-47). One mechanism, three regimes, fully scale- and domain-dependent — the program's strongest argument that mechanism verdicts must carry their scale and domain scopes.

**D9 — Depth and breadth mean different things.** On Sudoku, depth = constraint propagation and breadth = search over guesses — EqR's D/B axes are mechanistically *justified* there (and our d64 result shows the deep anchored map becomes the best breadth engine at width). On ARC, depth adds nothing (D4) and "breadth" means candidate diversity for selection — a pretraining/inventory property, not an inference-time axis.

**D10 — Difficulty is measurable on Sudoku, not on ARC.** Sudoku's per-instance solver-derived rating enables the spectroscopy family (hit-rate-vs-rating slopes, per-octile votes). ARC has only coarse family labels — no per-instance difficulty axis exists, which limits how sharply ARC results can be stratified. (A measured ARC difficulty proxy is an open instrument idea.)

---

## Part 2b — Operational demonstration manifest (each difference as a figure, from data on disk)

PI directive 2026-08-27: the differences are shown **operationally**, not argued. Status: BANKED = figure computable today from disk ($0); CELL = a small named run is required.

| Diff | The operational demonstration | Data source | Status |
|---|---|---|---|
| D1 | ARC codebook-size-vs-width table (N across d16–d64, seeded) side by side with Sudoku endpoint-condensation (n_distinct / flat k-curve on the priced d64 arm) — "many basins" vs "one basin found many ways, condensing" | ARC rung-0/1 batteries; sportB screens + p4 records | BANKED |
| D2 | ARC three-level distance-from-training ladder (rt < val-hard < rg, seeded) vs Sudoku per-octile funnel (hit-rate vs rating, three scales) — the two generalization axes as two decay curves | rung-1b/1c tables; funnel spectroscopy artifacts | BANKED |
| D3 | Verified-vote vs **unverified majority-vote** at k ∈ {16, 64, 128} on the same draws, both domains' conventions on one chart — the value of a free verifier, quantified | needs per-draw predictions: small evaluator stat (`--save-draws` or an unverified-vote counter) + eval-only rerun on banked B2-d64 + S5-d16 ckpts | **CELL** (~1–2 chip-h, eval-only, informational; register with the rung-2 launch) |
| D4 | The two-sided iteration figure: extended inference on ARC (+0 exact at t=96) vs Sudoku (+16/40 at t=64; t6→t64 full-test deltas on every arm) — same instrument, opposite verdicts. Flagship. | ARC horizon probe (08-09); Sudoku t-sweeps + full_t6/t64 pairs | BANKED |
| D5 | ARC wrong-stable fraction (0.92 of converged limits) vs Sudoku spurious-attractor rate (stuck states with ≥1 violation; the formal instrument = a $0 analysis over banked probe rows: violations, givens-kept, multi_init_best_wrong) | e1e3 batteries; sudprobe results.jsonl (all campaigns) | BANKED ($0 analysis to run at rung-2 close) |
| D6 | Throat behavior: ARC task-set-constant (785→459 declining, UV-led) vs Sudoku instance-conditional (801/987/1528 by givens) + the three spectral profiles on one chart (knee / free / Sudoku-UV) | flux artifacts across campaigns; S-port cell 1; sportB probes | BANKED |
| D7 | The TTT layer's existence: ARC adaptation tables (keyhole/LoRA/full-fit, basin-preservation constraint) vs the Sudoku column "none needed" — a protocol-stack diagram with measured rows | ARC TTT adjudications (08-12/14) | BANKED |
| D8 | The price's sign across domain × scale: one table — ARC transfer dividend (Law 4, three widths, seeded) · Sudoku d16 kill (H-43) · d32/d16-50k recovery (H-44) · d64 single-toggle stability (B4 broken / B5 whole) · the budget-competition condensation (H-47) | Law-4 tables; sport waves; sportB | BANKED |
| D9 | Depth/breadth semantics: Sudoku depth-repairs + funnel-ownership flip vs ARC depth-null + candidate-selection stack results | D4 data + sportB verdict + C.3′ line | BANKED |
| D10 | Difficulty measurability: Sudoku per-octile spectroscopy (possible) vs ARC family-label coarseness (the absence stated as a limitation + open instrument) | spectroscopy artifacts | BANKED (statement) |

## Part 2d — Differences catalog: measured additions from rungs 2/2b (2026-08-29 update; PI directive: keep this catalog current as the section's spine)

**D3 QUANTIFIED (the verifier's worth).** The demo cell put numbers on the deepest protocol difference: verified vote vs unverified majority at k=128 — B2-d64 **68.8 vs 18.8 %**, S5-d16 **70.7 vs 5.1 %**. The free verifier is worth +50–65 pp and unverified majority barely climbs with k. The entire breadth dividend rides verification; ARC's conversion stack is the substitute for exactly this. (Figure data banked: `runs/sxd3demo_*`.)

**D5 ABSOLUTE (wrong-attractor contrast).** valid_wrong_frac = **0.0000 across 5 arms × 422,786 evaluations** at d96 — Sudoku never forms a wrong-valid attractor at any measured scale (uniqueness structurally empties the class); failures are stuck partial propagations (mean 17–30 violations, givens 100 % intact). vs ARC's 92 % wrong-stable converged limits. The contrast is no longer statistical; it is exact.

**D11 — What overtraining costs (NEW row, from the cross-era comparison).** ARC: overtraining erodes *transfer* — the optimization-length tax (H-40, confirmed; β-rescue H-41 falsified; budgets frozen at 40k) eats unseen-family basins while in-distribution keeps sharpening. Sudoku: overtraining destroys *stability* (H-45 contractivity collapse, shallow maps; accelerating with width) and *attempt-diversity* (H-47 late-training funnel condensation on priced arms) — while cold keeps improving wherever the map survives. Mechanism of the difference = D2: Sudoku has no unseen families to erode, so the tax re-expresses in the dynamics. Both domains therefore have task-determined optimal training points, for *different observables* — and rung-2b's D1 (anchored T6 @2.5× the collapse-tested budget, six screens across training, val-selection keeping the peak) is the live test of whether the anchor deletes Sudoku's. Registered rules R2b-1/R2b-2 read it either way.

**D8/D9 extensions (rung 2).** The toll's sign arc gained its fourth regime: at d96 the price became the precondition for *training* the deep map at all (free T12 0/4 state-explosion deaths vs priced 2/2 clean — H-48); and the funnel changed owners a second time (d16 shallow → d64 deep → d96 anchored-shallow): breadth ownership tracks *whichever recipe is dynamically stable at that scale*, sharpening D9's "depth and breadth mean different things" into a scale-indexed statement.

**SCALE-MATCHING GAP (PI, 2026-08-29 — the ARC-resume hook).** The catalog's cross-domain rows currently compare Sudoku at d16–d96 (soon d128, 3.71 M) against ARC frozen at d64 (763 k) — the ARC d96 rung was registered-in-outline (rung-1c verdict: A5-class @40k, knee-β, predictions drafted: rg-96 ≥ 23.5 kill < 21, N 42–50, I < 499, knee L1 ≤ .15, η .21–.22) but never launched when the pod pivoted to Sudoku. PI direction: after the Sudoku ladder closes, run the ARC extension (d96, optionally d128) BEFORE the ICLR paper to scale-match D6 (the two throat behaviors), Law 4's fourth width, H-42's registered d96 test, and the optimization-length tax at width — carrying the Sudoku-era instrument back-port (final-map retention, 2k monitors + val-selection, λ_y, Adam-v Fisher; Part 3 item 3) so both domains are read with the same upgraded suite. Cheap by Sudoku standards (ARC arms ≈ 40k steps on a small corpus; d96 rung ≈ $60–100, d128 ≈ $120–200 — inside the reserve).

**CORRECTION + INSTRUMENT REVIVAL (2026-08-29, graded-ladder retro-analysis):** Part 1's ladder-row "LIMIT at d64: S(ε)=1.00 on every arm" was a reader artifact (dict iterated as list in the rung-1/2 physics passes; wave-3a-era analyzers read it correctly). TRUE story: the ladder discriminated at every scale (d64: priced .82, collapsed .37 @ .4; d96 extended rungs: .04–.20 @ .8) — the instrument never died, its reader did. The graded first-failure-rung distribution (instrument-redesign option 1) is computable retroactively from every banked campaign (`analyze_graded_ladder.py`); the watershed probe (option 3) demotes to optional top-tail resolution. New cross-scale rows this unlocks: (i) toll-narrows-basins, growing with scale — the radius companion to H-47's condensation, and a DOMAIN INVERSION of ARC's Law 3/4 where pricing WIDENED transfer basins (catalog row D8 extended); (ii) the collapse signature reads in basin space (broken arms: wide first-failure spread at low ε); (iii) the per-row schedule-leak count (retained_per_step non-perfect rows) = a graded per-row version of the retfm/ret_sched dissociation, separating horizon maps (~500/512 leak) from stable maps (0–30) at a glance.

## Part 3 — Owed items this document creates or restates

1. Rung-2 protocol: extended ε rungs; answer-mode-λ-first monitoring at 2k cadence; screens on ≥3 checkpoints (all already in the rung-2 consult list).
2. Sudoku wrong-stable instrument (stuck-with-violations formalized) — registered-unbuilt since 08-15; cheap; candidate rung-2 rider.
3. Back-port register for the ARC resume: final-map retention, trajectory monitors + val-selection, answer-mode λ, Adam-v Fisher lens (and a difficulty-proxy instrument, D10).
4. Paper-1: this catalog is the skeleton of the "two problems, one boat" section; D3/D4/D6 are its figures (verification, the two-sided iteration result, the two throats/profiles).

## Part 2e — Differences catalog: additions from the champion-track pilot (2026-09-02; `Report_2026-09-02_ChampionPilot_Verdict.md`)

**D5 extended — a memorizing Sudoku map acquires the ARC failure texture.** The native no-RI arm that memorized its corpus (train CE 0.000; 99.9 % of training puzzles solved cold, 19.4 % of the test) fails with near-valid, confidently wrong complete grids (median 5 violations at 51/81 cells), while every healthy map fails as a stuck partial propagation (27–32 violations at the same 51/81). The wrong-stable class Sudoku "never forms" (D5 absolute, valid_wrong 0.0000) appears as soon as the map memorizes — the failure texture is a memorization readout, not a domain constant.

**D6 extended — the native (3-adic, 2-cut) pyramid routes ~12× less stream flux than the canvas (1.0–1.1e5 vs 1.3e6 nats) with 87–92 % at its finest (box-forming) cut; the registered "mass migrates to the box scale" signature was ill-posed for a two-cut pyramid (the box tokens are the OUTPUT of the first cut) and is retired as a rule; the profile stays a descriptive readout.**

**D12 — NEW: data-diversity dependence.** On Sudoku the canvas placement offsets were an implicit ×576 augmentation; removing them (native9) exposed memorization at 32 epochs (aug 100). The remedy set is measured: aug1000 (+5.70pp paired at 10k), the two-phase floor schedule (train CE plateaus at .04–.05; cold flat), val-selection. ARC's corpus never showed this (many-rule inventory; orbit ×4 + offsets); the single-rule domain with a 1k-puzzle convention is where memorization binds.

**D3/D9 extended — init-invariance under RI.** RI-trained maps are path-independent (b1 ≈ cold per octile): EqR's verification-free residual selector recovers ≥98 % of the free verifier's gain on them (t1r@128 52.49 vs verified 52.85), unverified majority adds +5.5pp — and the funnel is deep-narrow (ρ .36–.44). The verifier's worth (D3, +50–65pp on wide-funnel maps) collapses to ≈0 on init-invariant maps: verification value is a property of the map class, not of the domain.

**Instrument added — the explosion census** (CPU, $0): fraction of cold trajectories whose state goes non-finite or exceeds 1e6 by t=64/256 on a final grid; separates η→1 (replacement-dynamics) maps that explode at inference (P2 5/64, P5 |z| 3e18) from bounded ones (P1, P3s1). Sudoku-born; back-port candidate for ARC equilibrium maps.

## Part 2f — Differences catalog: additions from the champion d128 round and the field-recipe reproduction (2026-09-03; `Report_2026-09-03_Champion_sportC1_Verdict.md`)

**D12 sharpened — memorization is not corpus-size-bound when the augmentation group is learnable.** At d128 both aug-1000 two-phase arms memorized in the hot phase at the same step the pilot's aug-100 arms did (train CE → 0 by 30–40k; vsel 42.8 → final 23–25 on the full test), after ~2.6 passes over 1.0M augmented pairs: the position group multiplies copies of the same 1,000 solutions under a symmetry the mixer learns. The remedy measured inside the round is the field's weight decay (R0: wd 1.0, CE .42, val still rising, 5× less free stream flux, codebook uncommitted). On the field's own cell, the no-ACT loop memorizes (train_exact .999) and the ACT loop does not (≈3× fewer passes per sample). ARC never showed the class (many-rule inventory); Sudoku's single-rule, 1k-puzzle convention is where it binds — and the field's conventions (wd 1.0, EMA, ACT) are its remedies.

**D3/D9 sharpened — verification-free selection is an RI-training property.** On every native RI map (init-invariant, deep-narrow) EqR's residual selector recovers ≥ 97.5 % of the free verifier's gain; on the field's RI-free base (X0) it recovers nothing beyond one draw (t1r@128 92.5 ≈ b1 92.1, non-monotone in k) while the free verifier reaches 99.7 % (ρ .98, r .5–.9 per draw — the widest, fastest funnel measured). Their 99.8 is residual calibration bought by RI/NI training; with a verifier the base already covers 99.7. Sudoku's free verifier makes the coverage column strictly stronger than any selector on any map class.

**D8/D10 sharpened — flat difficulty curves are a property of box-aligned geometries, not of native9 alone.** The field cell in the field regime reads 92.5 → 92.2 across rating octiles 2–8; our native cells 28 → 22 (R0) and 32 → 33 (B vsel, full test); the canvas declines 24.6 → 15.0. The pilot's "3-adic geometry flattens difficulty" survives as "misaligned dyadic geometry steepens it".

**New Sudoku fact for the paper's comparator section — the regime decomposition.** At matched data, augmentation, weight decay, learning rate, batch and EMA on the identical 20k puzzles, the field's 5.0M TRM-MLP cell + SOT/ACT loop is 55pp ahead of our 3.0M native cell (92.9 vs 37.1); our cell in our regime at its val-selected grid (42.8) is above our cell in theirs at 50k (37.3, still rising). The gap between the two systems is the cell and its training loop, not the optimizer regime — the ARC-side transfer of this fact (does their loop help on a many-rule inventory?) is a paper-2 question.

**Instruments (status changes).** Explosion census: PORTED to the z-norm cell and to η .98 (0 % exploded at t=64/256 on every grid; the class is removed by normalizing the carried z, confirming the pilot's z-channel trace); it does NOT see in-batch RI-row deaths (A0's 30k grid bounded four thousand steps before its NaN) — the death class is a training-row phenomenon, prevention structural. Val-selection: PORTED to two-stage arms with a defect found and fixed (stage-A dir restore on a node change; harness S8); a grid-consistency integrity gate (one checkpoint path per (arm, vsel)) joins the analyzer template. CLEAN gates: split stability {STOPPED, retfm, census} from memorization {CE, vsel-vs-final} — two consecutive rounds' letters (ARITY-HURTS, NORM-FAILS) fired on the other mechanism.

**D4 sharpened (lens G, 2026-09-03) — the reachability wall is cell/loop-dependent, not a domain constant.** ARC basins exist for ~30 % of pairs; our Sudoku cell reaches ρ ≈ .5 of the test set from any init at every d128 checkpoint; the field's reproduced cell reaches .98 by 35k. On our cell in the field's regime ρ is still rising at 50k (training-limited); in our regime it never moved while cold collapsed (architecture-limited).

**D5 quantified as a continuum (converged-wrong rate per draw, one instrument on both domains):** ARC .92 of converged limits · memorized Sudoku maps .03–.16 · the field's RI-free base .03–.06 · our RI+FPA maps ≤ .001. Verification-free residual selection (EqR) works only where this rate is ~0 — after RI/NI on their base, by construction on ours, never on ARC.

**D9 sharpened — one-shot vs propagation is decided by per-step computation:** ARC maps solve at step 0–1 or never; the field's 42-layer Sudoku step solves most puzzles in 1–2 outer steps with a propagation tail (D16→D64 +6.8pp, zero regressions); our 5–6-layer step propagates over 8–15 steps (p90 20–32). Our solved endpoints are exact fixed points (λ .67–.73); our stuck states are not fixed points (λ ≈ 1, still moving) — Sudoku's failures are unfinished propagation on both cells.

**D11/D12 bridge (prediction for the ARC extension):** Sudoku memorization and ARC's optimization-length transfer tax (H-40) are one pressure with two observables; weight decay is the measured Sudoku lever; an ARC d96 arm at wd 1.0 predicts a reduced budget tax.

**Instruments added by the 2026-09-03 freethink grounding ($0, disk; `tools/freethink_2026-09-03/`):** reachability-vs-givens (the erasure-threshold curve of a map as a decoder; ours crosses 50 % near 26 givens, the field's has no threshold in 17–35); the flip spectrum (per-step argmax revisions to correct/wrong, solved vs unsolved — separates commit-and-revise from soft churn; both cells fail by churn, ours at entropy .3–.4, theirs at .02); calibration at stalls (top-k confidence vs correctness on stalled puzzles: our maps 70–72 % correct at p .96 — the commitment signal is untrustworthy where it matters; the instrument of record for the calibrated-commitment graft); curvature concentration from Adam-v (PR/n; weight decay spreads it 50×). ARC back-port: the flip spectrum and calibration lens apply directly to ARC equilibrium maps (their wrong-stable class is the confident-freeze end of the commitment axis).

## Part 2g — Differences catalog: additions from the sportC2 graft night (2026-09-05; `Report_2026-09-05_sportC2_Verdict.md`, physics pass, decoder lens `tools/analyze_sportC2_ecc.py`)

**D13 (new) — A map is a measurable erasure decoder; the decoder class is read from three curves.** Sudoku's channel erases 81 − givens cells (57–79 % of the codeword at 17–35 givens); the maximum-likelihood decoder solves every puzzle (unique solutions), a pure propagation decoder (naked + hidden singles, computed) solves 11 % of the natural test distribution — 77 % of tdoku's rating-0 class and none above it. Every learned map sits between the two: its cold-success curve against givens has a 50 % threshold g50 and a 10–90 % width, and its solve rate on rating > 0 puzzles is its measured search yield. Our native d128 maps: g50 26–27 givens, width 11–15, search yield 25–36 % (R3 / B0 35 %); the field's reproduced map: no threshold in 17–35 (g50 extrapolates to 11), search yield 92 %; the memorized field cell (no digit aug) 29.0 / 17 %; the canvas maps 27–30 on cold but NO threshold under list decoding with a verifier (reach g50 ≈ 10 — a wide-funnel map is ML-like with restarts). Memorization shrinks the radius from the low-givens end first (R3 vsel → final: 27–29 givens 71 → 30 %; B0 50 → 10 % at 17–21). ARC analog: the retention-vs-corruption ladder S(ε) is the same curve with the corruption fraction as the erasure load.

**D4 sharpened again — reachability responds to training in both directions.** The persistent carry (train on the map's own late states) NARROWS the reachable set (verified@128 43.8 vs the base's 56.9 on identical puzzles) while raising single-shot accuracy; weight decay at a high rate WIDENS it (56.9 vs the un-decayed twin's 53.0); fifty thousand more steps in the field regime widen it slowly (50.8 → 56.1) without moving single-shot accuracy; trained inner cycles narrow it (42.7). Per-puzzle per-draw success rates on our maps are bimodal (43–57 % of puzzles never decodable, 33–43 % decodable on nearly every trial, 6–9 % in between where a list of restarts helps; the carry sharpens the bimodality to 56 / 37 / 6); the field's map has 93.5 % of puzzles above a per-trial rate of .5. "Draws stop exploring" (init-invariance's price) is now a trainable dial, and the carry turns it the wrong way.

**D5 continuum extended — a third spurious class.** The carried map (R1) is the first native map whose convergence residual does not certify correctness (AUC .62; 36 % of wrong restarts below the correct-restart median): stationary SOFT wrong states — high-entropy fixed points on unsolved puzzles (entropy .45 at t=64, 7 % of cells committed). The memorized field cell (X1) emits 5.9 % fully valid grids that contradict the givens (recalled training solutions) — the wrong-stable texture at its ARC-like end. The continuum now reads: our RI + FPA + toll maps ≤ .1 % (exact contractive solutions, unsolved states still moving) · the carried map 36 % soft-stationary · the field base 3–6 % confident-wrong fixed points · memorized maps 3–16 % · the memorized field cell's given-violating recalls · ARC 92 %.

**D9 — per-step computation is not additive on our cell.** Three trained latent passes per readout (R2) leave the first-exact step at 9 vs 11 and make the hardest-erasure puzzles less reachable (19 vs 40 % at 21–25 givens); the field's 42-layer flat step is not reproduced by cycling our 5-layer hierarchical step. Depth = propagation holds; propagation per step is set by the operator, not by repetition.

**D12 sharpened — which augmentation carries information.** Position-group augmentation (learnable by the mixer) adds nothing beyond the 1,000 solutions (sportC1); the DIGIT orbit is the field's actual anti-memorization device: without it the TRM cell memorizes the corpus by 10k steps (86.0 → 21.2 at D16, 92.8 → 24.2 at D64; train-exact .99; 5.9 % valid-wrong recalls) with weight decay, ACT and EMA all on. Our cell is exactly S9-equivariant and never uses digit augmentation: the symmetry is worth ≈ 65 points in the field's own units. ARC's orbit (D₄ × palette) is already used by both; the lesson transfers as "augment along the symmetries the cell cannot shortcut".

**D3 — verification-free selection exists in our family too.** The canvas two-phase map C3X's residual selector reaches 88.16 % at 128 restarts (verifier 89.20; ratio .988, sel-5k rider) — EqR's column on a 2.1M canvas map; D4's selector is weaker (58.4 vs 80.5). Our native maps' selectors track the verifier to ≥ .97 (no spurious attractors) except the carried map. The field's all-to-all token mixer is load-bearing for the field loop's restart robustness: with our group mixer in its place a single random-init restart solves 0.01 % of puzzles (X0: 92.1 %).

**Regularization as a rate (H-49 sharpened).** Weight decay's effect on memorization is set by lr × wd: at our lr 1e-3, wd 1.0 (rate 5e-4 in the hot phase, 5× the field's) delays the training-error collapse from 32–40k to 48–56k at −8.2 points of accuracy, and the floor phase (rate 3e-5) lets memorization return; the field regime on our cell (rate 1e-4) plateaus at 37 % and shows the onset of memorization at 38 epochs. Hard-decision feedback rows accelerate fitting (+8.9 points at their 40k peak, then collapse) rather than calibrate commitment (76 % vs 71 % correct among the five most confident cells at stalls; the bar was 90).

**Instruments (status changes).** The decoder lens (`analyze_sportC2_ecc.py`: peeling reference, erasure thresholds, search-demand spectrum, list-decoding channel spectrum, soft-syndrome AUC, calibration gap, decoder dynamics with monotonicity / churn / syndrome oscillation, decimation quality at stalls) joins the analysis template as a standing output; the calibration-at-stalls row is standing; the one-checkpoint INTEGRITY gate and the CLEAN split (stability vs memorization) worked as designed; two new instrument limits: the noise definition must never be a treatment contrast (CNC2 read 8.2pp = the wd effect), and val-selection runs at 10k resolution because grids (5k) and monitors (2k) coincide only at 10k multiples (fix owed).

## Part 2h — Differences catalog: additions from FINAL PHASE Night A (2026-09-07; `Report_2026-09-07_NightA_Verdict.md`, the physics pass and the decoder lens on the finalA arms)

**D13 sharpened — the decoder class is set by the loop, not by the operator or the regime.** The field's two-timescale loop on our nine-field S9-exact state (the DEC) reads DECIMATING on every arm: no erasure threshold in 17–35 givens at D64 (crossings extrapolate to −20…11), search-class yields 93.8–97.8 % (the field's reproduced recipe 91.7; our single-timescale natives 25–36), 94–97 % of cells committed at p > .9 after the first outer step, monotone solves 69–77 % (X0 59), first-exact median 1 with 31–39 % of solves exact at step one (X0 24.5; natives 0). The hardest erasure loads (21–23 givens) are solved 84–97 % (natives 12–15). The class carries its defect too: at stalls 88–93 % of cells are committed and 54–68 % of them are wrong, peeling from them contradicts on 100 %, the five most confident cells are right 54–62 % of the time at confidence 1.000 — the calibrated-commitment gap is class-intrinsic; what the loop changes on the symmetric state is how many puzzles reach a stall (2–7 % at D64 against the field cell's 7–9).

**D12 sharpened — augmentation on an equivariant cell.** With every weight shared over the digit axis the DEC cannot learn from a relabeled copy: digit augmentation (A4) reads +3.2 pp at the selected grids, inside the corrected floor, against the 65 pp the same orbit is worth to the field's cell. The equivariant cell sees the same effective corpus as the field cell without the orbit and memorizes it at the field's rate (Finding 3 of the report): segment CE .65 → .01–.16 by 50k, train exact .70–.96, peaks at 10–28k, finals 72–87, the low-givens capability erased first. Position augmentation is the orbit an equivariant cell can still use (the DEC is not position-equivariant); an aug-10000 arm is the registered lever.

**D3 / D9 sharpened — the selector law on the field loop, one variable at a time.** RI makes the field cell init-invariant (b1 92.0 ≈ cold) with a clean residual selector (spurious 0.0, t1r@128 98.3 = verified 98.3) at a 1.4 pp coverage cost; FPA at k = 1 on the field loop leaves cold flat and damages the selector (spurious .52, t1r 38) — the anchor's contraction at corrupted states makes wrong endpoints look converged to a latent-residual selector, so H-45's selector dividend on our answer-register natives does not transfer. The RI-free selector is seed-fragile on both cells (X0 seed 0 7.8 % spurious, seed 1 38 %; DEC seed 0 3.1 %, seed 1 47 % with t1r 3.1 — a few exactly converged wrong draws per puzzle dominate the argmin at a bulk AUC of .53). Every DEC arm reaches 100 % of strat-512 from some draw at k = 256: the verifier's coverage column is trivial on this cell, and the unverified majority reads 93.6–96.7 (X0 88.9).

**D5 continuum — the DEC's stalls are the field's confident churn.** Unsolved trajectories freeze at 24–43 % cells correct with 48–62 % of cells confidently wrong at t = 64, 4.8–6.2 flips to a wrong digit per cell over the last 32 steps (X0 3.3) and a syndrome oscillating by 18–29 — the confident-oscillation end of the commitment axis, never the soft high-entropy churn of our natives.

**D11 extended — the trajectory law on an equivariant cell, and the selection instrument.** Overtraining on the DEC destroys accuracy by memorization at the field's rate (finals 12–23 pp under the peaks), so checkpoint selection is part of the recipe; the 64-puzzle train-file monitor's 1.6 pp resolution with a later-tie rule selected the memorization side on A3 (its 16k grid tests 93.95 vs the selected 89.45 on identical puzzles, matching its seed twin's 94.34) — a 512-puzzle monitor with an earliest-tie rule is the next instrument.

**The compute column (new row for the comparator table; analytic, labeled inferred).** DEC-w384 costs 4.60× the field cell's MACs per outer step (70 vs 15.2 GMAC; 1.12 vs 0.24 TMAC per puzzle at D16); w512 7.84×, w256 2.21×, w192 1.34×. At matched inference compute the DEC's D16 (90–95) straddles X0's D64 (92.8); the DEC's single-pass D64 (98.0–98.1, 4.49 TMAC) sits within 1.7 pp of EqR's 128-draw residual selection (≈ 125 TMAC) at ≈ 28× less compute. The efficiency claim is per parameter and per draw until a narrower DEC is measured.

**Positioning (SE-RRM, arXiv 2603.02193).** Exact symbol equivariance on a TRM-style loop is published (attention over symbols; 2M; 93.73 @16 steps; trained with digit augmentation). Night A confirms the design class with a different cross-symbol operator (the DeepSets mean), ACT, and no augmentation, and adds the one-variable decomposition, the trajectory law, the selector law on the field loop, the decoder-class instruments and the compute column; the ARC column (paper 2) is where the verifier-dependence table separates the two domains.

**Instruments (status changes).** The vb screens (strat-512, k256) serve as the labeled selector/funnel source when a scan is absent; the offline re-run of the five wide-arm scans is pass two. New instrument limit: the 64-puzzle monitor at parity-class accuracy (ties; later-tie rule). The MAC column is analytic until the instrument lands. The decoder lens and the physics pass have finalA-specific tools (`analyze_finalA_ecc.py`, `analyze_finalA_physics.py`); the next campaign parameterizes them by tag.

## Part 2i — the frontier run (2026-09-08; `Report_2026-09-08_Frontier_Analysis.md`): the field's public weights at the full protocol through our evaluator, and the corpus through one lens

- **D13 (decoder class) is two-axis.** R1 (threshold and yield) and C1 (commitment dynamics) dissociate on the two field-loop cells whose operator or orbit was removed (X2, X1: SOFT by yield, decimating by dynamics); across 38 grids the loop sets the dynamics axis and the operator / orbit set the accuracy axis. The public checkpoints: alphaXiv MIXED (g50 18.0), CGAR / EqR DECIMATING, HRM MIXED on both axes. ARC reading: the commit / revision axis of D4 / D5 is C1's axis; the threshold axis has the S(ε) analog.
- **D14 (verifier dependence) gains three field rows:** the halting head is a verifier on all four public models (AUC .998–.9999) and halting on it costs 0.8–2.3 pp; the calibrated-commitment gap is class-intrinsic (top-5 at stalls 45–54 % on the public models, 41–62 % on ours, 63–83 % on the natives); the selector law reads on their weights (EqR 0 / alphaXiv 2.6 / CGAR 31 / HRM 12–15 % spurious) with the verifier at 97.5–99.9 regardless.
- **The depth law (new row):** decimating decoders gain ≈ +1 pp per doubling of the outer-step budget from D16 to D256 with zero regressions (three public models, four depth rows each; the DEC +0.5–1.1 at D128); soft natives gain nothing past the stall. On ARC the equilibrium maps solve at step 0–1 or never — depth is propagation only for a decoder that commits (D9 sharpened).
- **The memorization / trajectory law on the field's released weights (D11 / D12):** EqR +6.9 pp on its own training puzzles over the test; the alphaXiv 60k grid −3.85 under the 50k grid; EMA worth +16.8 on EqR's raw Langevin weights. Selection is load-bearing in the field's regime too.
- **Protocol conventions that hide points (the comparator-table columns):** the D = 16 headline (6–9 pp below D64–D256), EqR's released noise .5 (−10 pp at D16), best-test-checkpoint selection (a non-stationary trajectory read on the test set), and the reproduction distribution (alphaXiv's released grid at 79 vs the paper's 87).
- **Instrument status after the run:** R4 (exact-by-step) and R10 (the halting logits) are standing evaluator outputs; the init-radius (C7), prefix (C9), numerics (C10) and the cross-route rows exist on the pod route; the corpus tables (`suite_records`, `suite_corpus_table`) are the standing $0 lens over every banked grid; the sync-policy A/B pace test is owed on a chip.

## Part 2j — the champion night (2026-09-09; `Report_2026-09-09_Champion_Verdict.md`): the seeded DEC, four one-variable arms, and three readings the ARC section inherits

- **D14 (verifier dependence) gains its first VERIFIER SUBSTITUTE row.** The calibrated commit head (C6: c = σ(mean over the fields of z_H·w + b) per cell on the carried state, BCE against "this cell's argmax is correct" over the free cells, τ .9 hardening) reads CALIBRATED-INERT: on the puzzles the map fails it commits 2.1 % of the free cells (the softmax readout of every decimating grid measured commits 88–93 %) with 21.6 % of them wrong (softmax 54–68 %), its cell reliability has ECE .050, and its mean over the free cells is a per-puzzle correctness signal at AUC .995 (precision 99.97 % / recall 99.93 % at c ≥ .9) with NO verifier — at the halting head's level, cell by cell, at zero accuracy cost. ARC reading: the calibrated-commitment gap (D5) is a property of the softmax readout, not of the decoder's knowledge; a trained commit head is the per-instance correctness signal ARC's verifier-free regime needs (the paper-2 arm), and its abstention on failed puzzles is the ARC-side "confident wrong fixed point" detector the reachability wall lacked.
- **D11 / D12 (memorization and the trajectory): the orbit null and the capacity order.** The whole position orbit online (C3) neither lifts the peak (−0.3 pp) nor delays the decline (plateau end 26k vs 30k; the same shape and timing as the fixed-corpus seed arm run to 50k): position augmentation is not an equivariant cell's anti-memorization lever, and what the DEC fits is the 1,000 solutions in a position-group-invariant form. The rate is capacity-ordered at fixed data and regime (w512 ≈ 12k < w384 ≈ 28k < w192 no decline by 50k). ARC reading: the puzzle-identity embedding is ARC's memorization channel and augmentation votes are its substitute for a verifier (Roye-Azar); the DEC says the orbit's size does not buy time on a cell that is already equivariant — capacity and the training length do. The corrected selection instrument (512-puzzle monitor, earliest tie, 30k) is what the ARC-d96 rung back-ports; the onset instrument is the monitor's plateau end, not the loss (the CE < .05 onset never fires by 50k).
- **D4 / D5 sharpened by the cold-start excursion instrument.** On the w192 arm the cold rollout fell to 17 % at 20k while a random-init draw read 92.8 and the verifier 99.8 (training CE flat, census 0 %); the trained start left the basin the map kept for random inits and returned by 26k. Init-invariance (Law 7) is a trained property of the MAP that its trained start can transiently violate; the raw monitor row and the screens' cold-vs-b1 gap read it. ARC reading: our ARC inference always started from VOID (H-37); an excursion of the trained start is invisible to a VOID-only protocol — the ARC rung's multi-init draws are the instrument.
- **D9 (depth) saturates on the DEC:** +2.3–3.4 pp D16 → D64, +0.25–0.68 D64 → D128, +0.17–0.36 D128 → D256, zero regressions on 27 of 28 rows; the field's weights +4.1–6.6 / +1.5–1.7. Resolution per step is the operator's (set attention 29.8 % exact after one step; the mean coupling 25.2 at w384, 17.1 at w192; EqR 12.1) and the finish is the width's (w192 first by D64). ARC's one-shot solves sit at the operator end of this axis with no propagation to saturate.
- **D3 / D9 (the selector law) at n = 7 and k = 128:** verification-free selection at the verifier's level on every RI + FPA arm (t1r = verified to ≤ 0.1 pp at k = 32 on seven arms and at k = 128 on three; C5 99.88); the verifier's worth on a parity-class map is ≈ 1 pp. The ARC test (freethink A-5 i) stands: RI-trained `pretrain13_Dri` vs its plain twin with k = 32 restarts.
- **The compute column has its efficiency point:** w192 at 1.34× the TRM-MLP's arithmetic per step and 16 % of EqR's parameters reads +6.0 pp over EqR at D64 on identical puzzles (1.31 vs 0.97 TMAC per puzzle) and +3.2 over EqR's D256 at a third of the arithmetic (different subsamples); analytic MACs, labeled inferred until the instrument lands.
- **The comparator columns (verified at the sources 2026-09-09):** FRM v3 99.5 (1k convention, 7.0M, peak over inference compute, a 1,000-puzzle test subset, three seeds); the diffusion curriculum 99.90 (the full 3.83M-puzzle split, K = 10,000, single stochastic rollout); Sotaku 99.12 (2.7M puzzles, 1,024 iterations, a 25k subset); FPRM 94.2 (pass@1, adaptive halting, 422,786); SE-RRM 93.73 (2M, 16 steps); PTRM 98.75 (best-Q@100). The paper's table separates the training-set, restart, selection, steps, test-set and search columns; the DEC leads the thousand-puzzle single-pass column at every depth with seeds and does not claim the absolute frontier.
- **Instrument status:** the physics pass is parameterized by tag (`analyze_champ_physics.py`); the corpus tools carry the `champ` tag; the k128 scan is the filler's standing row; owed: the MAC instrument, a selftest for the descriptive physics tool (its AUC helper first carried the residual selector's orientation), the onset definition for the next analyzer, a US-zone control of the multi-draw scan pace (the sync-policy rider read 0.95: the readback is not Mumbai's factor).

## Part 2k — the explanation-enrichment pass on the champion arms (2026-09-10; `Note_2026-09-10_Champion_Mechanisms.md`; the filler's full-set D64 and k = 128 rows; three new instruments)

- **D5 (failure classes) now has both textures on ONE map, set by the START.** The w192 arm's excursion grid (its trained start outside the basin) fails as frozen wrong fixed points — cells correct flat at 43–47 % from step 1, 2.0 flips per cell, syndrome oscillation 12.5 — while its intact grids fail as the class's confident churn (7.0 flips, oscillation 30), and random-init draws on the excursion grid still solve 92.8 % (the verifier 99.8 %). The D5 continuum's frozen end is not ARC's alone: on Sudoku it is the failure mode of a trajectory started outside the basin the map holds. ARC reading: our VOID-start protocol on ARC never drew from the RI distribution (H-37); the ARC rung's multi-init draws are the instrument that separates "no basin" from "wrong start".
- **The ignition (new instrument reading; the concurrent gap of Lai et al. settled on our cell):** decimation switches on between 2k and 4k steps (commitment at step 1 from 36 % at entropy .28 to 94 % at .02) BEFORE the solves arrive (24 % at 4k, 77 % at 6k, 94 % at 8k); at the first edge the failures are quiet wrong fixed points (0.18 flips), at the second the confident churn appears (4–5 flips, oscillation 25) with the propagation that recovers the solved puzzles; the training loss moves slowly through both (.75 → .49). ARC reading: the "bifurcation in training" is a commitment switch, and ARC's one-shot solves sit at the first edge with no second one.
- **D14 (verifier dependence) sharpened by the joint-readout lens:** the trained commit head is calibrated per PUZZLE (mean c .47 on failures vs .95 on successes; ECE .013 on the failing puzzles' cells against the softmax's .501) and not per CELL (AUC .63 for "this committed cell is wrong", the softmax's own .62): a decimating decoder's carried state carries a trajectory-level correctness signal (the halting head .997–1.000, the head .995 per puzzle, even the softmax's mean confidence) but not the identity of its wrong commitments. ARC reading: the verifier substitute a generalization task can have from this cell is a per-instance abstention, not a per-cell repair; closing the cell-level gap needs a revision or search mechanism, not a readout.
- **The revision axis (a new mechanism axis for §4.4 / §6):** the operator sets how much is decided per step (set attention 29.8 % exact after one step, the mean coupling 25.2 at w384 and 17.1 at w192, EqR 12.1), the width sets how much is left to propagate (D16 → D64 +2.3 pp on the attention arm, +3.3 on w192), and the finish order at depth is the reverse of the decision order. On the w192 arm the extension bought deeper propagation of the same decoder (first-exact p90 18 → 7 from 10k to 46k). ARC reading: ARC maps decide at step 0–1 and have nothing to propagate; the axis collapses to its decisive end.
- **D11 / D12: the memorization clock is capacity, not the orbit** (w512 ≈ 12k < w384 ≈ 28k < w192 > 50k; the whole position orbit online changes neither the peak nor the clock); the plateau end of the held-out monitor is the onset instrument (the segment-CE onset never fires). The orbit-invariance instrument (`tools/lens_orbit_invariance_{make,read}.py`; `runs/analysis/champ_orbit_invariance_20260910.*`) reads the aug-1000 map as ALREADY position-invariant on unseen puzzles: a random position element flips the solve status of 5.3–5.5 % of the strat-512 on C0 (3.9–4.5 % on C5) against a 4.3 % floor between compute routes and a 3.3 % floor under a digit permutation the construction leaves exactly invariant; the online-orbit arm is no more invariant (5.9–6.5 %). The orbit had nothing to teach a map that already generalizes across the group; ARC reading: the color/dihedral orbit's worth on an equivariant ARC cell should be read with the same instrument before it is paid for in training.
- **The full-set rows:** D64 on all 422,786 for C0 / C1 / C3 / C5 / C6 (98.47 / 97.55 / 98.13 / 99.16 / 98.58) with ZERO depth regressions D16 → D64 at exact pairing and +4.4 to +6.0 pp over EqR's released weights on the identical set; k = 128 residual selection = the verifier to ≤ 0.06 pp on all seven arms (99.48–99.88). The paper's D64 comparison row is on the field's own set now.
- **Literature cross-read (the gaps file Part C):** FRM's exposure bias / Fixed-Point Forcing = the selector law's mechanism (train on states the model produces: RI + the anchor rows; AUROC .50 → 1.00 there, .375 → .999 here); the Confidence Shortcut is the softmax's and a trained readout does not undo it cell by cell; LDT's soundness is what a readout cannot restore after the fact; PTRM's three classes, Speed-is-Confidence's irreducible set (0.12–0.48 % at k = 128 here) and guided reasoning's guide alignment all read as trajectory-level signals of a decimating decoder.
- **Instrument status:** `tools/lens_commit_joint.py` (the joint readout lens) joins the suite; `analyze_champ_physics.py` reads the filler's full-set rows (section I2); the orbit-invariance instrument is a transformed-npz + evaluator recipe (`scratchpad/make_orbit_npz.py`; promote to a tool with the ARC rung's color-permutation twin); the physics tool's selftest is still owed.

## Part 2l — the ARC analysis pass (2026-09-10; `Note_2026-09-10_ARC_Instruments.md`): every difference and similarity read operationally through one suite — our RI/plain substrates on val-hard at k = 32 (`runs/analysis/arc_suite_paired_20260910.txt`) and the field's released ARC-1 TRM through cold, the identity controls, demo retention, k = 8 draws and the augmentation vote (`runs/analysis/arc_field_trm_20260910.*`)

- **D4 (inventory- vs search-limited) on three substrates.** Retention of the handed truth 18.8 / 27.8 % of query pairs (RI / plain) against 1.00 on the DEC's solved endpoints; the k = 32 oracle is gated by basin existence (×∞ / ×55 conditioning; 0 of 117 and 1 of 104 no-basin queries reached by any restart); the field's model retains its own demonstrations (95.0 %, 98 % at step 1) and its test accuracy conditions on that only ×2.4 (33.0 vs 13.5 %). Physics: one attractor with a restoring force (the constraints re-read every step) vs an inventory of attractors with no force outside it; retention is the existence test, the oracle the reachability test, and they coincide on Sudoku and dissociate on ARC.
- **D9 (depth) has its negative on the field's weights.** Flat from step 2–4 on ours and on the field's model (29.6 → 31.5 → 30.5), and 8 of 419 inputs solved at step 1 are LOST by step 16 (ever-exact 32.9 vs 30.6); the DEC's +3.3 pp D16 → D64 with zero regressions in 422,786 on five arms. A decimating map without a restoring force does not protect its correct fixed points.
- **D3 / the selector law: the mechanism ports, the value does not.** RI cleans the residual on ARC as on Sudoku (AUC .85 vs .57, E5 spurious 8.5 vs 59 %; the DEC .999 vs .375) and condenses the draws (4.7 vs 13.5 distinct endpoints; per-draw exact = cold exactly) so the oracle falls 15.3 → 4.9 % and residual selection returns the cold pass on both; the field's AUC .66–.68, spurious 29 %, residual@8 31.3 vs oracle 32.5. Init-invariance (Law 7) holds on both (the field's draws all-or-none on 97 % of inputs) and on ARC it is invariance to the WRONG endpoint: basin widening makes the residual trustworthy and kills exploration in one move. The verifier's worth on ARC exists and is uncollected: +7.6 pp (plain, k32), the field's +1.9 over 8 inits and +17 pp over ~1000 augmentation views, of which the count vote collects ≈ 5 — and the vote pays under the DEC's majority condition (wrong candidates disagree: solved inputs 5 distinct answers / .76 correct views, failed 444 / .001): a consistency test on the map's symmetry, i.e. the orbit-invariance instrument read as a selector. The productive restarts on ARC are augmentation views, not init draws.
- **D5 (failure classes): one mechanism, two textures, set by the restoring force.** ARC failures: 100 % of converged failures wrong, conf .975 on wrong cells vs .991–.996 on right, 15–16 % of committed cells wrong, 1.0 flips per cell; the DEC's: 4–7 flips, syndrome oscillation 13–30, 53–65 % of committed cells wrong, valid-but-wrong ≤ 2e-4. The cell-level calibrated-commitment gap is the same on both (no readout locates the wrong cells: AUC .63 / .62); the Sudoku excursion showed the frozen texture from a wrong START, ARC shows it from an absent BASIN; the converged-wrong rate is the continuum's coordinate (ARC .92–1.00, memorized Sudoku maps .03–.16, RI + FPA ≤ .001).
- **D14 (verifier dependence) on the field's ARC weights.** The halting head reads AUC .917 on ARC (Sudoku: .9974–1.000 on our seven, .998–.9999 on the released models), halting on 57 % of inputs at precision 52.5 / recall 97.7 — a verifier where correctness is a function of the visible state, an over-halting hint where it depends on a hidden rule; the per-puzzle abstention the commit head achieved on Sudoku (ECE .013) is the object that could port and has not been built on ARC.
- **D7 / D11 / D12: the identity is the memorization channel, measured.** 30.6 → 4.8 % with the blank identity (the 64-input control 40.6 / 6.3 deranged / 6.3 blank / 1.6 random trained rows); +64 pp demo-test through the table; the Sudoku prefix inert (+.6 / .0 / −.1). D1's codebook is indexed by a learned code on the field's model rather than inferred from the support pairs.
- **NEW ROW D15 — commitment is set by the domain, not only the loop.** Our ARC natives commit 78 % of the cells at conf > .9 after one step (→ 96–98 % by step 16; the DEC 93–96 %) where the same native family on Sudoku commits 3–13 %: the one-shot decision of a generalization task is decimation without a second edge (the ignition's first edge with no propagation edge; the revision axis collapsed to its decisive end). Structural-ARC rows with no Sudoku twin: the output-size head (right at step 1 on 72–74 %, a quarter of the failures are size failures) and the rule head H_q.
- **The seven similarities the suite establishes (the section's other half):** decimating decoders; init-invariant when RI-trained; the selector law's mechanism; the cell-level gap; the vote condition; memorization as fitting what the model is conditioned on; symmetry as a free verifier.
- **Scope, stated for the paper:** our ARC substrates are early d48 natives at 5–8 % on val-hard; the field's model is 7M + its identity table at 30.6 / 43 %; the rows read the regime on both, not the task's solvability; the dev-30 conditioning cells are n ≤ 9 and labeled.
- **Instrument status:** `tools/lens_arc_suite_read.py` (paired reader of `tools/arc_suite.py` outputs) and `tools/lens_arc_field_read.py` (the field harness rows) join the suite. Owed, added to Part 3b: the halting-head twin on our substrates (H_q), a commit-head readout on ARC, the field's L6 checkpoint / HRM ARC-2 (no identity table without the authors' identifiers), the full-set augmentation vote (a GPU box), the C1 columns on the field's per-step traces.

## Part 3b — Owed register as of 2026-09-10 (the PI: "mark the owed items for later"; each with its consumer)

| owed item | why | consumer | cost |
|---|---|---|---|
| A selftest for the descriptive physics tool (`analyze_champ_physics.py`) on hand-built records, like the frozen analyzers' | its AUC helper first carried the residual selector's orientation into the halting and commit rows (caught by the reading, fixed before any number left) | every future analysis pass | Mac, 1 h |
| The MAC / FLOP instrument (measured multiply-accumulates per outer step per puzzle, per cell and width) | the compute column is analytic and labeled inferred (70.0 / 20.4 / 15.2 GMAC per step) | the paper's compute column; the FRM-style accuracy–compute figure | Mac, 2 h |
| Promote the orbit-invariance recipe (`tools/lens_orbit_invariance_{make,read}.py`) to a tool with the ARC twin (color permutation × dihedral flips on the ARC canvas) | measure a trained map's invariance on UNSEEN inputs before paying for an augmentation lever | the ARC rung's augmentation decision; paper 2 | Mac, 2 h |
| A US-zone control of the multi-draw scan pace (one 512-row batch of a k32 scan in us-east1-d) | the sync-policy rider read 0.95: the per-step readback is not Mumbai's 0.42× factor; the cause is open | the next pod campaign's walls | ≈ $5 on a chip |
| The registered onset definition for the next analyzer: the monitor's plateau end (the first grid ≥ 2 pp under the running maximum for the rest of the run) | the segment-CE < .05 onset never fires on the DEC by 50k | the next registration | build, 1 h |
| The full-set evaluations of the best models (`Plan_2026-09-10_FullSet_Evals.md`: D64 full for C2 and C4; C5 at D128 / D256 on 50k) | C2 (the champion by rule) and C4 have only the 100k D64 row; C5's D128 / D256 are on 20k / 5k | the paper's headline table on the field's own set | a pod, ≈ $150–290; PI decision after the ARC work |
| Candidate hypotheses for the register: the capacity-ordered memorization rate (w192 to 100k, w256 to 50k); the attention coupling at seeds 1–2 | one-seed readings on the champion night | paper 1's mechanism section; paper 2's cell choice | ≈ $60 / ≈ $150 |
| The physics tool's `--curv` (Adam-v curvature) row, dropped this pass | Night A found no reading; kept optional | none until a hypothesis needs it | Mac, minutes |
| ARC (Part 2l): the halting-head twin on our substrates (H_q as the analog) and a commit-head readout on an ARC substrate; the C1 columns on the field's per-step traces | the verifier-dependence table needs both readouts on both domains | §6 of paper 1; paper 2's abstention instrument | Mac, 2–4 h |
| ARC (Part 2l): the field's L6 checkpoint and HRM ARC-2 through the same rows; the full-set augmentation vote | the L6 run is the report's 52.75 pass@2; the vote on 400 tasks needs a GPU box (GPUS_ALL_REGIONS = 0) | the field-through-our-instruments section | a GPU VM, ≈ 1 day |
