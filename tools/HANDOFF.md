# HANDOFF — TPU ops (persistent; the STATUS block is rewritten per campaign; the standing sections below it change only when a lesson lands)

**Read this first, then `tools/OPS_RUNBOOK.md`.** The repo outranks conversation memory. The ops model runs a campaign to completion and STOPS; the analysis pass (Fable, on PI go) adjudicates. ETAs/clocks to the PI in IST (UTC+5:30) with UTC in parentheses. **Concision pass 2026-09-02:** every superseded campaign block (2026-08-19 → 2026-09-01) is preserved verbatim in git — §8 lists the commit per campaign — and its lessons are folded into §7.

## STATUS — sportC2 REGISTERED, LAUNCH-READY (2026-09-04; fleet ZERO; `runs/tpu_deadline.txt` still in the PAST until the launch bumps it) — LAUNCH ONLY ON THE EXPLICIT PI GO
- **What is locked (the registration commit):** ledger §5 2026-09-04 LAUNCH REGISTRATION; design + pre-mortem `Documentation/Plan_2026-09-04_sportC2.md` §1–§8; rules `tools/analyze_sportC2.py` (selftest 21/21; UNTOUCHED from here on); chain `tools/chain_sportC2.sh` through `tools/harness_sportC2.sh` = 49/49 (S1–S9 incl. the negative S9b/S9c; run 1 read 21/24 on sandbox arm names, run 2 47/48 on the S9b stub guard, run 3 49/49); CPU smokes on real code (plan §8, all rc 0); suite 151 green; `tools/campaign_sportC2.env` = the source of truth once copied to `tools/campaign.env`. PI decisions on record (2026-09-03 evening): the arm set APPROVED; ARC deferred until after the Sudoku champion work.
- **Arms and the static map (v6e-16 = 4 hosts × 4 chips; v6e-8 = 1×8 sequential in the priority order W0 R1 R3 X1 R2 R4 X2 → riders):** w0 W0 → R2 · w1 R1 → R3 · w2 R4 → X1 · w3 X2 → riders (B0/B1 correct-grid 20k scans + B0's EMA full; canvas C3X/D4 sel-5k). Recipes: W0/R1/R2/R3 = d128 native two-stage (50k cosine 1e-3→3e-5 + 30k floor @3e-5, B64, **wd 1.0**, z-norm) with R1 `--sot --sot-segments 4`, R2 `--inner-k 3`, R3 `--hard-p 0.5`; R4 = sportC1 R0's 50k weights `--init-from` + 50k in the field regime (batch 384, lr 1e-4, wd 1.0, remat; the chain fetches `sportC1/R0_ckpt.pkl` itself); X1/X2 = the TRM/EqR cell + SOT + ACT in the field regime (batch 768, 50k), X1 without digit aug, X2 with `--trm-token-mixer group9`. Headline weights: raw on W0/R1/R2/R3, EMA on R4/X1/X2 (the chain handles it; nothing to do).
- **LAUNCH PROCEDURE (on the explicit go; the sportC1 procedure):** (0) `gcloud config list` = aakashemailbox@gmail.com / quantum-llm · `git status` clean at the registration commit · `bash tools/pod.sh status` node ABSENT everywhere · `launchctl list | grep qhrrn2` loaded; (1) `cp tools/campaign_sportC2.env tools/campaign.env`; (2) `echo $(( $(date -u +%s) + 16*3600 )) > runs/tpu_deadline.txt` (= launch + 16 h; the watchdog deletes any node on sight while the knob is in the past) and `rm -f runs/pod_strikes.txt` (strikes 0/2); (3) `DRY_SLEEP=150 nohup bash tools/pod.sh supervise 16 >/dev/null 2>&1 &` then `nohup caffeinate -i -s -w "$(cat runs/pod_supervisor.pid)" >/dev/null 2>&1 &` — the supervisor banks `code_<sha>.tgz`, hunts v6e-16 then v6e-8 across the US zones (Mumbai only on a PI go), bootstraps, runs the canary, launches the chain once per worker; (4) on the first READY: `bash tools/plant_guard.sh 20` → `GUARD-OK` (20 attempts 45 s apart; the deadline is read from `runs/tpu_deadline.txt`; re-run after EVERY node change and EVERY deadline change); (5) arm the session layers (§2): the edge Monitor on `runs/pod_qhrrn2-pod2.log` + the hourly heartbeat cron; (6) the B1 projection gate at the first MEASURED rows: hold + PI framing if the 16-shape projects past ≈ 17 h (1.2 × 14 h) or the 8-shape past ≈ 24 h; the ops phase reads NO accuracy values.
- **INFERRED paces / walls (plan §6; the canary and the first rows re-price):** W0 ≈ 4 h (80k at ≈ 21 it/s on 4 chips + ≈ 2.5–3 h of evals); R2 ≈ 9–10 h (K = 3 triples the per-step cost of the pretrain AND of every eval) → **w0 ≈ 13–15 h = the long pole**; R4 ≈ 7 h pretrain (≈ 2 it/s on 4 chips at batch 384 with remat) + 2 h evals + X1 ≈ 4.5 h → w2 ≈ 13.5 h; w1 ≈ 8 h; w3 ≈ 9 h. Spend band ≈ $170–230 (US 16 $13.64/h · US 8 $6.82/h · Mumbai-16 ≈ $16/h); tripwire 1.5× = $345. The 8-shape sequential ≈ 20–22 h needs a PI-approved deadline extension (+8–10 h) — frame it, never extend alone.
- **Log signatures specific to this chain (remote `runs/detached.log`, surfaced by `pod.sh status` PROGRESS snippets and the GCS markers):** sportC1's set (`PRETRAIN-START <arm> … prec=highest|default` · `STAGEA-OK` (+ `<arm>a_pretrain.tgz`, `<arm>_STAGEA_OK`) · `PRETRAIN-OK` (`<arm>_PRETRAIN_OK`, `<arm>_pretrain.tgz`, `<arm>_ckpt.pkl`) · `VALBEST <arm> A:|B:NNNNNN val step -> path` · `VB-FALLBACK-FINAL` (labeled, not an error) · `PRETRAIN-RESTORE <arm>[a]` (grids + the stage-A dir re-pulled after a node change — automatic) · `STAGEA-RESTORE-MISSING` (labeled) · `SCREEN-SKIP` · `EVAL-OK|EVAL-SKIP|CENSUS-OK|CENSUS-SKIP` · `FULL-FINAL <arm> := vsel` · `ARM-OK` · `SPORTC2-WORKER-DONE worker=N waiting` (normal on a 16) · `CHAIN-SPORTC2-COMPLETE` + `sportC2_final.tgz` + `SELF-TEARDOWN`) PLUS: `CALIB-OK|CALIB-SKIP` (the stall-calibration rows; `calib_R3_vsel_hard` and `screen_R3_vb_hard` are R3's labeled hard-feedback rows), **`PRETRAIN-OOM-RETRY-REMAT <arm>`** (a launch-time HBM exhaustion before any logged step, retried ONCE with `--remat` — labeled in `RETRY_REMAT.txt`, NOT a death; report it in the heartbeat), `RIDER-CKPT-MISS` (riders are non-blocking). **Registered outcomes, never retried:** `PRETRAIN-NAN <arm> [stage A|stage B] (rc=3)` → `AMPUTATED to ckpt_NNNNNN step N` + `STOPPED.txt` + evals on the amputated final (a stage-A death STOPs the arm: no stage B). **Stop-and-report signatures:** `AMPUTATE-FAILED` (incl. a persistent OOM), `EVAL-N-BAD`, `EVAL-SHARD-FAILED`, `EVAL-FAILED`, `CENSUS-FAILED`, `CALIB-FAILED`, `BAD-ARM`, `NPZ-MISSING`, `INIT-CKPT-MISSING R4` (R0's ckpt absent from GCS — verified present today), `SPORTC2-INCOMPLETE` (wait passes exhausted — read WHY: which `*_ARM_OK` is absent → that worker's log).
- **Decision table (this campaign):** sportC1's (§4) verbatim: preemption → the supervisor re-hunts (a second running-preemption on the 16 ⇒ demoted to the 8 for the supervisor's life); on the NEW node every arm resumes from its banked 1k-cadence `ckpt_latest` (EMA restored; `resumes.txt` records the step; R1/X1/X2 restart their carries — labeled; R4 resumes its continuation, never its init) and evals resume from 300 s partials; **re-plant the node-side guard on EVERY new node and after ANY deadline change**; the chain re-pulls grids and the stage-A dir by itself before val-selection (no manual pulls); the 8.5 h wall-ceiling recycle (`worker(s) N IDLE — relaunching`) is NORMAL; churn circuit-breaker: ≥ 2 node losses in 2 h during US-day → stop the supervisor, notify the PI, wait for the night window unless told otherwise; Mumbai = US-dry fallback ONLY on a PI go; deadline binding with arms unfinished → the guard clips = a labeled pause (banked partials make it cheap), extension = PI go only, then re-plant the guard.
- **Heartbeat (hourly, IST with UTC; even if uneventful):** node zone/shape/state · per worker: current arm + phase (PROGRESS snippet + GCS markers) · completion fraction + ETA from MEASURED pace · chip census · spend from the watchdog spans × rate (`spend_report.py` over-rates US-16 spans — hand-derive) vs the $170–230 band · deadline remaining · incidents since the last heartbeat (an OOM retry counts as one). **Ops reads NO accuracy/vote values.**
- **Completion (§6 generic + these counts):** 7 `*_ARM_OK`; pretrain dirs W0 R1 R2 R3 R4 X1 X2 (+ W0a R1a R2a R3a stage-A dirs) with `ckpt_latest.pkl`, any `STOPPED.txt` / `RETRY_REMAT.txt` / `NAN_ABORT.txt` quoted verbatim and `resumes.txt` noted; per native arm `full_vsel_t64`, `full_final_t64`, `full_vsel_t64_alt` (n == 422786 each), `retfm_t8` (512), `sxscan` (20000), screens (512), `sxcensus_*_vsel` + `_final` (census.json), `sxcalib_*_vsel` (calib.json); R3 additionally `sxscreen_psportC2R3_vb_hard` + `sxcalib_psportC2R3_vsel_hard`; X1/X2: `full_vsel_t16`, `full_final_t16`, `full_vsel_t16_alt`, `full_vsel_t64`, scan, censuses, calib; riders `sxscan_psportC1B0_vselA20k` + `…B1_vselA20k` (20000), `sxeval_psportC1B0/full_vselA20k_t64_ema` (422786), `sxrider_C3X_sel5k` + `sxrider_D4_sel5k` (5000). Sentinel in the pod log + `sportC2_final.tgz` in GCS → positive `describe` ABSENT → fleet-zero sweep (all campaign zones, nodes + QRs) → pull with `gcloud storage cp` + crc32c → extract into `runs/` → COUNTS ONLY → stop the session layers (leave the launchd watchdog) → ONE factual ops report → STOP. The analysis pass (`analyze_sportC2.py` untouched + the three freethink lenses, $0) runs in a fresh session on the PI's go.
- **Calendar:** night Sep 4–5 → analysis Sep 5–6 → sportC3 (parity at d160, ×2 seeds, the surviving levers) Sep 6–7 → ARC-d96 (+ wd 1.0 arm, commitment lens) after the champion work → draft from Sep 8; freeze ~Sep 10–12; ICLR abstract Sep 18.

## 1. First three commands (any time, and after any app restart)
```bash
bash tools/pod.sh status            # node (all zones), supervisor, deadline, GCS bank, remote job
tail -3 runs/pod_qhrrn2-pod2.log    # what the loop last saw / did
launchctl list | grep qhrrn2        # watchdog loaded? (reinstall: bash tools/install_watchdog.sh)
```
Then `git log --oneline -3` (has a close/verdict landed?) and the newest ledger §5 entry. If `supervisor: NOT RUNNING` and the campaign is incomplete: `DRY_SLEEP=150 nohup bash tools/pod.sh supervise 14 >/dev/null 2>&1 &` + `nohup caffeinate -i -s -w "$(cat runs/pod_supervisor.pid)" &` (adopts a live chain; refuses to start twice).

## 2. Re-arm the session layers after any app restart (supervisor + caffeinate + launchd watchdog survive; these do not)
(a) edge monitor: `tail -F runs/pod_qhrrn2-pod2.log` filtered to `CREATED|READY|CANARY|PREEMPT|DEMOT|PRETRAIN-NAN|EVAL-N-BAD|MERGE-N-BAD|INCOMPLETE|COMPLETE|FAILED|TEARDOWN|needs eyes|deadline` — never put a literal pgrep/pkill pattern in a watcher's text (self-match class, §7). (b) hourly heartbeat to the PI: per-process completion fraction + concrete ETA from MEASURED pace (arm markers in GCS, `PRETRAIN-OK` timestamps, monitor step rows), IST (UTC), spend vs the registered band from watchdog spans × rate (`spend_report.py` over-rates US-16 spans at the Mumbai 2.35× — hand-derive), chip census, deadline rule (extend +14h when <10h AND the PI has authorized). (c) verify the watchdog and the node-side guard (`bash tools/plant_guard.sh <hours>` → `GUARD-OK`; re-run after EVERY node change and EVERY deadline change).

## 3. Normal log signatures
`CREATED` / `CANARY` / `LAUNCH chain` / `launch wN: detached + verified` / `RUNNING 1/1 worker(s)` progress rows / `PRETRAIN-START|OK <arm>` / `STAGEA_OK` (two-stage) / `EVAL-OK <name>` / `EVAL-SKIP` (idempotent marker) / `ARM-OK <arm>` / wall-ceiling recycle ≈ launch+8.5h: `worker(s) 0 IDLE — relaunching (attempt N/10)` then a new `launch w0` = NORMAL (resume) / `COMPILE-CACHE restored` / `VENV-RESTORE-OK` / `code from GCS archive` / `SELF-TEARDOWN` / `CHAIN-<TAG>-COMPLETE`. Multi-host (v6e-16 = 4 hosts × 4 chips): four `START worker=k/4` lines.

## 4. Decision table (the loop does the mechanics; you decide these)
`PRETRAIN-NAN <arm> -> amputate` = a REGISTERED outcome (one-shot; never retry the arm) · `AMPUTATE-FAILED` = STOP + report · `EVAL-N-BAD` / `MERGE-N-BAD` / `EVAL-SHARD-FAILED` = read that shard log AT THE NODE immediately; N-BAD = partition integrity — stop, report, delete nothing · preemption → the supervisor re-hunts (strike 2 on the 16 ⇒ demoted to v6e-8 for its life); on the NEW node partials auto-resume (≤10 min loss) — re-plant the guard + re-pull banked grids/metrics before any eval (else `select_ckpt` silently falls back to the final grid) · `<TAG>-INCOMPLETE` = wait-passes exhausted — read WHY (which `*_ARM_OK` is absent → that worker's log) before any relaunch · deadline binding → the guard clips = a labeled pause (banked partials make it cheap); extension = PI-GATED ONLY, then re-run plant_guard · churn circuit-breaker: ≥2 node losses in 2h during US-day → stop the supervisor, notify the PI, wait for the night window unless told otherwise · Mumbai/asia = US-dry fallback only (PI policy) · a deliberate node kill sweeps its own chain's claim markers in the same action.

## 5. Never (ops phase)
No analyzer runs, no accuracy/vote values read or quoted, no verdicts, no tuning, no chain/env edits beyond the registered live verbs (kill-orphan / extend-deadline-with-PI-go / pause), no second pod / on-demand / queued resources without an explicit PI go, never two supervisors, never change `ARMS` order mid-campaign, never `gsutil -m`, never judge a remote command through `| tail`.

## 6. Completion procedure (generic; the campaign registration names the counts)
Sentinel in the pod log + final tgz in GCS verified at source → node positive-describe ABSENT → fleet-zero sweep (all campaign zones, nodes + QRs) → pull final tgz (`gcloud storage cp` + crc32c) → extract into `runs/` → COUNTS ONLY (pretrain dirs with `ckpt_latest.pkl` + any `STOPPED.txt` quoted verbatim; eval dirs with `summary_all.json`: full n==422786, scan n==20000, screens/retfm n==512; `*_ARM_OK` per arm) → run the campaign's auditor if one exists (`--phase final` 0-FAIL) → stop session layers (leave the launchd watchdog) → ONE factual ops report (completion time, node timeline, incidents, spend hand-derived, "analysis pass ready") → STOP. The analysis pass carries the ledger entry + commit.

## 7. Lessons register (one line each; every one cost a bite; details in the ledger's CONSOLIDATED OPS RECORDS and the per-campaign blocks in git, §8)
- **Billing/backstops:** guest shutdown (DMS) does NOT stop TPU billing — only node deletion does; the node-side delete guard (`plant_guard.sh`, zone-discovering) is the Mac-independent backstop; the launchd watchdog hard-deletes past `runs/tpu_deadline.txt`; teardown verification = a POSITIVE `describe` (a network outage once faked "already gone" → ≤2h idle node); keep-awake (caffeinate) must outlive the supervisor whenever any watch runs (a 7h Mac sleep once blinded a wait).
- **Ssh/remote:** `--ssh-flag "-o StrictHostKeyChecking=no" --ssh-flag "-o UserKnownHostsFile=/dev/null"` (reused-IP 255 class); bound every remote call (`perl -e 'alarm N; exec @ARGV' --`); stderr visible; verify by ARTIFACT (GCS object / node log / describe), never wrapper pgrep (the "LAUNCHED" illusion); canary verifies by artifact on ssh timeout (a passed fit read FAILED through a hung channel); `nohup` over ssh hangs the session — ship remote work via the dispatcher's detach path; `mkdir` before `scp`; a payload containing a pkill pattern kills its own session (two-call pattern).
- **Data/pulls:** `gcloud storage cp` per object + crc32c against `gcloud storage ls -L` (gsutil -m stalls; serial `gsutil cp` stalled at full size with a wrong hash); tarballs of two-stage arms must include the stage-A dir + `config.json`; resumes re-split the rng (valid draw, labeled) and a death within ~3k steps of a resume is "resume-adjacent" (C1, P5) — record resume steps.
- **Chains (sportC1 analysis, 2026-09-03):** a two-stage arm's STAGE-A dir must be restored on a node change or the both-stage val-selection silently degrades to stage-B-only (B0/B1's post-preemption scans + vsel censuses ran on memorized stage-B grids while their fulls/screens ran on the right A:20k grids — the analyzer's gates did not check grid consistency across an arm's evals); fixed in `chain_sportC1.sh` (`ensure_local_pretrain` restores `${arm}a_pretrain.tgz`; harness S8) — EVERY resume path needs its own harness scenario, and the analyzer asserts one ckpt path per (arm, vsel).
- **Chains:** one-shot amputation truncates `metrics.jsonl` to the last finite grid (death step bounded by the grid census) and the trainer keeps running on NaNs (early-NaN abort owed); fixed-step screens beyond a death are garbage (243 violations) — skip; completion guard: sentinel/final only when every artifact is present (a vacuous sentinel once tore a pod down mid-campaign); merges n-gated and idempotent; `--merge` on a busy node needs `JAX_PLATFORMS=cpu`; multi-host DP needs per-host confinement (93a79d4); v6e-16 = 4×4 not 2×8; T12 at d16 needs `--remat`; the relaunch counter resets on healthy polls (a monotone counter once nearly tore down a healthy campaign at a wall recycle); static worker maps beat claim systems (claim races/TTL takeovers were the rung-2 incident class).
- **Local tooling:** exit-status masking through `| tail`/`| grep` bit ≥4 times; `grep -c` already prints 0 (`|| echo 0` fabricates); zsh does not word-split unquoted vars; macOS has no `timeout(1)`; `/bin/bash` is 3.2 (no associative arrays); a Monitor/watcher whose command text contains a `pgrep -f` pattern self-matches and can deadlock the waiter (bracket the pattern).
- **Spot weather:** us-east1-d churns in US-day hours (maintenance kills with zero progress happen); night windows land in minutes; Mumbai asia-south1-c is a reliable v6e-16 source (2.35× cost) — fallback only; QR flex-start waited ≥7h ungranted; quota buckets are zonal and `Unlimited` rows are decoration — only creates tell the truth.
- **Rates (PI 08-30):** v6e-8 US $6.82/h · v6e-16 US $13.64 · Mumbai-16 ≈$16 · Mumbai-8 ≈$8; `spend_report.py` applies the 2.35× to all ×2 spans (zone-aware fix owed); hand-derive from watchdog spans.
- **Harnesses (sportC2, 2026-09-04):** an adapted harness is UNVERIFIED until it runs — the first sportC2 run read 21/24 on sandbox construction still carrying the previous campaign's arm names (fake init ckpt / rider tarball names / assertion dirs: the PM-5 class re-entering through the copy) while the chain passed every scenario it reached; and a stub's own guard can hide the exact path a negative scenario stages (S9b) — every negative scenario asserts that the failure it stages actually fired.
- **Measured paces:** canvas d96 T12 3.20 it/s DP-8 (1.24 DP-4); native9 d96 T12 ≈19 it/s wall (39 printed; 50k in 43 min), T16 ≈0.83×; native full test 8 min sharded-8, 20k k128 scan ≈27 min; canvas full t64 4-way ≈2.5h; screens 19 min sharded-8 (canvas); eval bank quantum 128 records; CPU (Mac): native 20k cold eval k=0 ≈18 min, canvas ≈10× slower.

## 8. Superseded campaign blocks (verbatim in git; `git show <hash>:tools/HANDOFF.md`)
sportC1 LIVE (launch block) → 280a110 · sportC1 COMPLETE + analysis-pass STATUS → 86bf9bf, eea3c32 · sportC2 BUILT (the pre-registration pickup list) → eea3c32 · sportC0 pilot LIVE/Mumbai-switch blocks → 93850f8, dcd14de · rung-2b rider scans LIVE + COMPLETE → ee4cb1c, 29c3303 · rung 2b LIVE/COMPLETE/night-close → 3bb3079, b49c970, c60c9aa, 29c3303 · rung 2 registered/in-flight/night-pause/carriers-stopped/COMPLETE → 3a8f18a, ad08286, a30499a, 5df9769 · rung 1 tail (pause, hardened relaunch, flex-start contingency) + COMPLETE → 297351e, 5d34d03, f6e6744, 9f0454f, e068012 · rung 1 launch + Opus handoff → b1d21fe, 0db2f1e, d1be32b · wave 3a → 2f0b033, 923d546, c62c7da, 7edfd08, 541860c · wave 2 (launch, pause, resume, handoff, complete) → f164feb, d6f2574, e12a2eb · sprint S2 wave 1 (launch, drought pause, complete) → 5771779, 7978123 · rung 1c/1b (launch, weather pause, complete) → a20cdf0, 3430935, 96284dc, b5b5510.
