# HANDOFF — TPU ops (persistent; the STATUS block is rewritten per campaign; the standing sections below it change only when a lesson lands)

**Read this first, then `tools/OPS_RUNBOOK.md`.** The repo outranks conversation memory. The ops model runs a campaign to completion and STOPS; the analysis pass (Fable, on PI go) adjudicates. ETAs/clocks to the PI in IST (UTC+5:30) with UTC in parentheses. **Concision pass 2026-09-02:** every superseded campaign block (2026-08-19 → 2026-09-01) is preserved verbatim in git — §8 lists the commit per campaign — and its lessons are folded into §7.

## STATUS — sportC2 LIVE, mid-flight on a US v6e-8 (session handed off 2026-09-04 10:36Z; POD LEFT RUNNING) — NEW SESSION PICKUP

### OPUS OPS PICKUP (2026-09-04 11:25Z, Fable → Opus; READ THIS FIRST — the OPS NOTES + the old launch block below are history)
- **LIVE STATE (measured at the source, 11:21Z):** US **v6e-8** spot in us-east1-d, READY since 09:01Z (incarnation 6), chain pid 14491 launched 09:07:51Z, **1×8 sequential**. **Banked 3/7: W0 R1 R3.** **X1** in its k=128 scan: all 8 shards at 2048/2500 (≈ 28 min per 512 records → scan EVAL-OK ≈ 11:50Z, then census ×2 + calib → ARM-OK X1 ≈ 12:10Z). Then **R2** (stage A RESUMES at step 29,000 — the restored `runs/pretrainsportC2_R2a/ckpt_latest.pkl` load-verified: state/opt_state/rng/state_ema) → **R4** (RESUMES at 10,000) → **X2** (its t64 full, scan, censuses, calib remain) → riders (B0/B1 scans + B0 EMA full, then the two canvas riders, now SHARDED) → sentinel + self-teardown. Supervisor **pid 44019** (restarted 11:20Z, adopted the live chain; code archive **`code_5c9b296.tgz`** banked = the chain every relaunch/bring-up runs from now), caffeinate on it; ladder v6e-8 only; strikes 0/2; watchdog loaded; node guard planted (pid 7948 on the node, fires at the deadline epoch). Spend ≈ $155 by watchdog spans at 10:40Z (Mumbai-8 11.5 h ≈ $92 · US-16 2.5 h ≈ $34 · US-8 4.25 h ≈ $29); the remaining sequential hours add ≈ $125–145 → ≈ $280–300, above the $170–230 band, below the $345 tripwire.
- **ARMED IN THIS SESSION (they survive the model switch; re-arm only after an app restart, §2):** Monitor `sportC2 edge monitor` (pod-log state changes, deduped) · Monitor `15-min measured snapshot` = `bash tools/ops_snapshot.sh` every 900 s (TICK blocks: T/PID/MARK/PT/EV/DMS/GUARD/DISK/LB — the ONLY progress read to trust; `pod.sh status`'s PROGRESS snippet sticks at the last marker during a pretrain) · Monitor `guard-planter` (`plant_guard.sh 20` on every `CREATED in` line) · cron `e46c0c81` hourly at :23 = the heartbeat. Pace = two TICKs' trainer `step N` lines (step delta / wall gap), never the printed it/s.
- **LIVE 5-MIN GCS BANKING (new 11:14Z, PI directive; `tools/live_bank.sh`; harness S10 restore+RESUMED / S10b torn ckpt → labeled fallback / S10c a banked arm's stale live copy never restored over its final; 61/61):** the loop (node pid 64497, standalone via setsid; pidfile `runs/live_bank.pid`, log `runs/live_bank.log`) rsyncs runs/ → `gs://qhrrn2-rescue/sportC2/live/runs/` every 300 s (banked arms excluded; serial, never deletes; bounded 900 s). The NEW chain (from the next relaunch on) runs `live_bank.sh restore` at start: on a FRESH node it pulls the live prefix no-clobber (banked arms excluded — their tarballs are the record) and load-verifies every `ckpt_latest` (torn → the newest loadable 5k grid, `LIVE-RESTORE-FALLBACK`, labeled, ≤ 5k steps re-run; none → `LIVE-RESTORE-FRESH`, labeled) — then `pretrain.py` RESUMES and every eval resumes from its 300 s partials. **Until the first relaunch (the 17:37Z wall recycle) this node runs the OLD chain** (no restore step); a preemption in that window is still covered: the new node's bring-up pulls the new archive and its chain restores from the live prefix. **Every TICK:** the `LB` line must be < 10 min old (stale/absent ⇒ over ssh `cd ~/qhrrn2 && (setsid nohup bash tools/live_bank.sh loop < /dev/null > runs/live_bank.out 2>&1 &)`, then report it). **At every relaunch/bring-up:** the pod log must read `code from GCS archive (code_5c9b296.tgz)` and the node's `runs/detached.log` must show `LIVE-RESTORE …` + `LIVE-BANK loop …` lines — an older archive name ⇒ FABLE-NEEDED. The live prefix is deleted at the close (`gsutil -q rm -r gs://qhrrn2-rescue/sportC2/live`), after the final pull.
- **RIDER FIX (chain delta, harness-verified, protocol unchanged):** the two canvas riders (C3X/D4 sel-5k k128) ran single-chip via `eval_one` — ~11 h EACH at the measured 2b pace (2.8 h per 1250-record chip-slice), ~22 h holding one chip BEFORE the sentinel (sportC1's never started for this reason); now `eval_sharded` 8-way + merge + n-gate 5000 (≈ 1.5 h each). Effective from the first relaunch (long before the riders phase ≈ 04:30Z Sep 5); verify the archive name as above.
- **LABELED DATA GAPS (for the analysis pass):** R2's stage-A 5k grids at 5k–25k and R4's 5k grid were lost with the v6e-16 (the hand-banked resume tarballs held ckpt_latest + metrics + config only; monitors intact) — R2's stage-A vsel candidates start at 30k, R4's at 15k; metrics rows 29k→30k (R2a) and 10k→10.05k (R4) are duplicated by the resume (last-wins). Resume steps land in `resumes.txt` automatically.
- **DMS / WALL-RECYCLE INVARIANT:** the guest poweroff timer (DMS) = last relaunch + 10 h (now 19:07Z); the wall ceiling (30,600 s) relaunches the chain every 8.5 h (next ≈ 17:37Z, then ≈ 02:10Z Sep 5) and re-arms it — safe by construction while the chain recycles. TICK prints `DMS … (in N min)`: if N < 90 and no relaunch is in progress ⇒ `bash tools/plant_guard.sh 5` (its CALL1 pushes the DMS +780 min; re-planting the guard is harmless) and report. A relaunch resumes R2/R4 from `ckpt_latest` (≤ 1k steps lost) and evals from partials — NORMAL, not an incident.
- **DEADLINE / TIMELINE — EXTENDED 2026-09-04 ~11:58Z (Opus, on the PI's explicit go once the R2 pace was measured at 9.3 it/s wall):** `runs/tpu_deadline.txt` = launch+44h = **2026-09-05 10:43:16Z (16:13 IST)**; node guard re-planted at the new epoch (GUARD-OK, one process, old 04:43Z guard replaced; DMS pushed to 00:57Z, ahead of the 17:37Z recycle); supervisor + watchdog both re-read the file automatically. Projected COMPLETE ≈ 08:00–08:30Z Sep 5 sits inside it; the pessimistic tail (R2 evals long + a preemption) reaches ~10:30Z, so re-price R2's EVAL pace at each heartbeat and surface a further small extension EARLY if the tail approaches 10:43Z. Prior state (for the record): `runs/tpu_deadline.txt` = launch+38 h = **2026-09-05 04:43Z (10:13 IST)**. Projection from measured paces (a native two-stage arm ≈ 3.6 h all-in on this 8; R2 ≈ 3× per step; R4 at R0's measured 4 it/s; a field-cell scan ≈ 2.3 h): R2 ARM-OK ≈ 21:00Z · R4 ≈ 01:50Z · X2 ≈ 04:50Z · B riders ≈ 06:00Z · canvas riders (sharded) ≈ 09:00Z · COMPLETE ≈ 09:15Z Sep 5 (± 2 h) — the current deadline clips inside X2. RECOMMENDED to the PI: extend to launch+44 h = **10:43Z Sep 5 (16:13 IST)**. ONLY on the PI's go: `echo 1788605000 > runs/tpu_deadline.txt` (= 2026-09-05T10:43:20Z) → `bash tools/plant_guard.sh 5` → `GUARD-OK … fires at … 10:43` → note it here + commit. Re-price at every heartbeat from R2's measured pace; if the projection moves inside the deadline no extension is needed; without a go the guard + watchdog delete the node at 04:43Z (banked arms safe; X2's tail + riders lost = a labeled pause).
- **HISTORY:** launched US-16 14:43Z Sep 3 → US-day churn/drought → Mumbai-8 bridge (banked W0 R1 R3) → 9 am IST US switch → US-16 preempted at canary + dry → US-8 → PI: extend to +38 h, ride the 8 (cost over speed, analysis next morning) → 11:14–11:25Z (this handoff): live bank + rider fix + snapshot tool + layers re-armed.

### OPS NOTE 2026-09-04 06:15Z (Opus, US-night outcome + PI calls)
- The 09:03 IST US-night switch found **v6e-16 churny** (first US-16 preempted during canary 04:01Z; re-hunt dry on v6e-16 all 5 zones) and landed a **US v6e-8** in us-east1-d — the chain runs **1×8 SEQUENTIAL**. 3/7 arms (W0 R1 R3) were banked on the Mumbai-8 bridge before the switch; X1 R2 R4 X2 + riders remain.
- **PI calls (2026-09-04 ~06:01–06:15Z):** (1) deadline EXTENDED to launch+38h (04:43Z Sep 5) to let the v6e-8 finish sequentially — guard re-planted; (2) ladder set to **v6e-16-only** (`ACCEL_LIST="v6e-16"`), supervisor restarted to adopt the running v6e-8 — so a preemption now re-hunts v6e-16 ONLY (holds out for the faster parallel shape, no v6e-8 fallback; if v6e-16 stays dry a long time the run idles until it lands — re-surface to the PI if that bites the deadline). R2 (inner-k=3) is the ~9–12h sequential long pole.

### OPS NOTE 2026-09-04 09:07Z (Opus, PI switch to v6e-8 + manual resume-bank)
- **PI call ~08:50Z:** the v6e-16 was leaving w1 idle (R1/R3 done); PI chose to switch to a **v6e-8** (all 8 chips busy, ~half the hourly rate, accepting the longer sequential wall since analysis is next morning). Ladder is now `ACCEL=v6e-8 / ACCEL_LIST="v6e-8"`.
- **This chain has NO live mid-pretrain GCS sync** (verified: `pretrain.py` save_ckpt writes ckpt_latest to LOCAL disk every 1000 steps, non-atomic; the chain banks to GCS only at STAGEA_OK / PRETRAIN_OK). So a node change mid-pretrain restarts the in-flight arm — UNLESS you manually bank+restore. The old `chain_r0` had a live 5-min GCS ckpt sync; the sportC chains dropped it.
- **Manual resume-bank done this switch (the pattern to reuse):** before deleting the v6e-16, copied each mid-pretrain arm's `ckpt_latest` (R2 w0 stage A, R4 w2), **load-verified it unpickles** (falls back to the latest complete step-grid if caught mid-write), tarred ckpt_latest+metrics+config → `gs://…/sportC2/{R2a,R4}_resume.tgz`. After the v6e-8 came up (X1 runs first, ~1h buffer), restored those into `runs/pretrainsportC2_{R2a,R4}/` so `pretrain.py` resumes from them (R2 step 29000, R4 step 10000; verified state+opt+rng present). Do NOT set STAGEA_OK/PRETRAIN_OK for a mid-stage arm — that would make the chain skip it.
- Master deadline unchanged (launch+38h = 2026-09-05 04:43Z). Guard re-planted on the v6e-8.

### OPS-MODEL QUICK-START (Opus; a fresh session or a mid-campaign model switch — do these in order, then only the mechanics of §4)
1. **Orient, no actions yet:** `git log --oneline -3` (this block's commit = the launch checkpoint) · `bash tools/pod.sh status` · `tail -5 runs/pod_qhrrn2-pod2.log` · `launchctl list | grep qhrrn2` (the watchdog must be loaded) · `date -u -r "$(cat runs/tpu_deadline.txt)"` (the deadline; set at launch to 2026-09-04T06:43:16Z = 2026-09-04 12:13 IST).
2. **Supervisor:** `supervisor: RUNNING pid N` → touch nothing. `NOT RUNNING` with no `CHAIN-SPORTC2-COMPLETE` in the log and no `sportC2_final.tgz` in GCS → `DRY_SLEEP=150 nohup bash tools/pod.sh supervise 16 >/dev/null 2>&1 &` then `nohup caffeinate -i -s -w "$(cat runs/pod_supervisor.pid)" >/dev/null 2>&1 &` (it adopts a live chain and refuses to start twice). If the deadline is < 10 h away with arms unfinished → FABLE-NEEDED (an extension is PI-gated); never edit `runs/tpu_deadline.txt` yourself.
3. **Guard:** on the first READY of EVERY node incarnation, `bash tools/plant_guard.sh 20` → `GUARD-OK zone=…` (also after any deadline change; re-planting an already-guarded node is harmless; `GUARD-RETRY` lines while the node is still creating are normal).
4. **Session layers (they die with the session — re-arm):** (a) a Monitor on `runs/pod_qhrrn2-pod2.log` filtered to `CREATED|READY|CANARY|PREEMPT|DEMOT|STRIKE|PRETRAIN-NAN|OOM-RETRY|EVAL-N-BAD|MERGE-N-BAD|FAILED|INCOMPLETE|COMPLETE|TEARDOWN|needs eyes|deadline` (never a pgrep/pkill pattern inside a watcher's text); (b) an hourly heartbeat cron.
5. **Every hour:** the heartbeat (template below), even if uneventful. **At every Monitor event:** the §4 decision table — the mechanics you do yourself; everything else → FABLE-NEEDED.
6. **At completion:** §6 verbatim (counts only, NO accuracy values), ONE factual ops report, STOP.

### FABLE-NEEDED — the escalation protocol (the PI switches the model back)
Write `FABLE-NEEDED: <signature or decision> — <one line>` as the FIRST line of your message, quote the verbatim log lines under it, state node / supervisor / guard / deadline, then STOP acting on the campaign (keep only the billing backstops true: the guard and the watchdog). Do not attempt a fix, a relaunch of a failed arm, or an interpretation.
**Escalate on:** (a) any stop-and-report signature — `AMPUTATE-FAILED` · `EVAL-N-BAD` · `MERGE-N-BAD` · `EVAL-SHARD-FAILED` · `EVAL-FAILED` · `CENSUS-FAILED` · `CALIB-FAILED` · `BAD-ARM` · `NPZ-MISSING` · `INIT-CKPT-MISSING` · `SPORTC2-INCOMPLETE` · `SELF-TEARDOWN-FAILED` · the supervisor's `needs eyes` (exit 3) or its `deadline` exit with arms unfinished; (b) any PI-gated decision — a deadline extension (the B1 projection gate, < 10 h left with arms unfinished), on-demand or queued resources, a second pod, Mumbai/asia zones OUTSIDE the registered contingency below (which is pre-authorized), the churn circuit-breaker (≥ 2 node losses in 2 h during US-day); (c) any edit to `tools/chain_sportC2.sh`, `tools/campaign.env`, `tools/analyze_sportC2.py`, the arm order, or `runs/tpu_deadline.txt`; (d) a SECOND `PRETRAIN-NAN` in the campaign, an `OOM-RETRY` on more than one arm, spend past the $345 tripwire, a campaign zone reporting `PROBE-FAIL` in `runs/tpu_status_log.txt` for > 1 h, a READY node with no PROGRESS change for > 45 min outside a wall recycle, or any log line not named in this block; (e) anything that needs an accuracy/vote value, an analyzer run, or a result interpretation; **(f) since 2026-09-04 11:25Z:** a relaunch/bring-up whose pod-log line names an archive other than `code_5c9b296.tgz`, a `LIVE-RESTORE-FRESH` line (an arm restarting from step 0 — labeled, the PI must know), the live-bank loop still down 30 min after one restart attempt, `LIVE-BANK rc≠0` on three consecutive passes.
**Do NOT escalate (do it, then report it in the next heartbeat):** preemption re-hunts and the 16 → 8 demotion (automatic; `STRIKE n/2`), re-planting the guard on a new node, the 8.5 h wall recycle (`worker(s) N IDLE — relaunching` then a new `launch wN: detached + verified`), ONE `PRETRAIN-NAN … AMPUTATED` (a registered one-shot outcome — never retry the arm), ONE `PRETRAIN-OOM-RETRY-REMAT` (labeled; report it), `RIDER-CKPT-MISS` (non-blocking), `SPORTC2-WORKER-DONE … waiting` (normal on a 16), `VB-FALLBACK-FINAL` / `PRETRAIN-RESTORE` / `STAGEA-RESTORE-MISSING` (labeled, automatic), `EVAL-SKIP|CENSUS-SKIP|CALIB-SKIP` (idempotent), `read UNKNOWN` (network; the loop keeps polling), the completion procedure §6; **since 2026-09-04 11:25Z:** `LIVE-RESTORE-FALLBACK` (labeled, ≤ 5k steps re-run — report it), one `LIVE-BANK rc≠0` (transient; the next pass self-heals), restarting the live-bank loop when its `LB` line is stale, `LIVE-BANK loop already running` at a relaunch (the standalone loop is alive — correct), the DMS push via `plant_guard.sh 5` when TICK shows it < 90 min out.

### REGISTERED CONTINGENCY — MUMBAI v6e-8, SEQUENTIAL (PI-authorized 2026-09-03 20:20 IST: "we can try a v6e-8 in Mumbai if and when the US pod gets preempted; on an 8 we run things sequentially — a small speed boost at a smaller cost and ~1.8× the time"). This is PRE-AUTHORIZED: the ops model executes the block below verbatim and reports it in the next heartbeat — no FABLE-NEEDED for the switch itself; any deviation from the block IS Fable-needed.
**Trigger:** the first RUNNING-preemption of the US pod (the supervisor log shows `node PREEMPTED|TERMINATED … — down` / `STRIKE 1/2 … preempted`, and `bash tools/pod.sh status` reads `node: ABSENT`). NOT a trigger: a dry US hunt before the first node (ask the PI), a bring-up failure (the supervisor's own strike/re-hunt handles it), the 8.5 h wall recycle.
**What the switch costs (state it in the heartbeat):** this chain has NO live mid-pretrain GCS sync — an arm whose PRETRAIN was in flight at the preemption restarts from step 0 on the new node; arms with `<arm>_PRETRAIN_OK` / `<arm>_ARM_OK` are banked and SKIP; evals resume from their 300 s partials. On the 8 the chain runs 1×8 sequentially in the priority order W0 R1 R3 X1 R2 R4 X2 → riders (harness S1); from scratch ≈ 20–22 h at Mumbai-8 ≈ $8/h (a US v6e-8 is $6.82/h but US-day churn is the reason for Mumbai).
**The block (copy verbatim; each step verifies the previous one):**
```bash
# 0. verify at the SOURCE that the US node is gone (never act on a proxy)
grep -E 'PREEMPT|TERMINATED|STRIKE|DOWN|positively not READY' runs/pod_qhrrn2-pod2.log | tail -5
bash tools/pod.sh status | sed -n '1,3p'            # must read: node: ABSENT
# 1. stop the ONE supervisor (caffeinate exits with it); clear the strike counter
kill "$(cat runs/pod_supervisor.pid)"; rm -f runs/pod_supervisor.pid runs/pod_strikes.txt
# 2. the Mumbai-8 env (the sportC1 Mumbai switch's exact values); print and check the three lines
sed -i '' -e 's/^ZONES=.*/ZONES="asia-south1-c asia-south1-b"/' -e 's/^ACCEL=.*/ACCEL=v6e-8/' -e 's/^ACCEL_LIST=.*/ACCEL_LIST="v6e-8"/' tools/campaign.env
grep -E '^(ZONES|ACCEL|ACCEL_LIST)=' tools/campaign.env
# 3. KEEP the master deadline (launch+30h = 2026-09-04 20:43Z / 02:13 IST Sep 5) — do NOT reset it (the US-NIGHT SWITCH at 9am IST brings the run back to US before then); just re-plant the guard against the current file
bash tools/plant_guard.sh 40 2>&1 | grep -E 'GUARD-OK|GUARD-PLANT-FAILED'; date -u -r "$(cat runs/tpu_deadline.txt)"
# 4. restart the supervisor (race-free pid capture) + keep-awake
nohup bash -c 'DRY_SLEEP=150 exec bash tools/pod.sh supervise 26' >/dev/null 2>&1 & SUP=$!; nohup caffeinate -i -s -w "$SUP" >/dev/null 2>&1 &
tail -3 runs/pod_qhrrn2-pod2.log                     # SUPERVISE start … accels='v6e-8' zones='asia-south1-c asia-south1-b'
# 5. on the first READY in Mumbai: bash tools/plant_guard.sh 40 -> GUARD-OK (the launching session's planter does this by itself while that session lives)
# 6. record it: git add tools/campaign.env tools/HANDOFF.md; git commit -m "sportC2 MUMBAI-8 FALLBACK taken <UTC> (PI-registered contingency)"; git push
```
**After the switch:** heartbeats continue on the same template (rate ≈ $8/h; the priority order tells you which arms are at risk if the deadline binds: the LAST ones — X2 and the riders); a preemption in Mumbai → the supervisor re-hunts Mumbai by itself; a second preemption or anything else → FABLE-NEEDED.

### US-NIGHT SWITCH — return to a US pod at 9 am IST (03:35 UTC, the reliable US-night spot window) (PI-authorized 2026-09-03 20:30 IST: "switch to a us pod at 9 am ist (us night)"). A one-shot cron (created in the launching session, ~09:05 IST) prompts this; if the session died, run it by hand at ~03:35 UTC. PRE-AUTHORIZED — the ops model executes and reports it; no FABLE-NEEDED for the switch itself.
**The decision (in order):**
1. `bash tools/pod.sh status | sed -n '1,3p'` + `grep '^ZONES=' tools/campaign.env` + `gsutil ls gs://qhrrn2-rescue/sportC2/ | grep -cE '_ARM_OK'`.
2. **Campaign already COMPLETE** (`CHAIN-SPORTC2-COMPLETE` in the pod log or `sportC2_final.tgz` in GCS): nothing to do — report and let the completion procedure run.
3. **Already on US** (`ZONES=` starts `us-`) and node READY/healthy: NO disruptive switch (a healthy US pod is what we want for the night; switching would throw away in-flight pretrain — this chain has no live sync). Just confirm and report "already US for the night window".
4. **On Mumbai** (`ZONES="asia-south1-*"`) OR **US but ABSENT/dry**: switch the env to US and let the supervisor take the night window. Run the block below.
**The block (only for case 4; the reverse of the Mumbai contingency):**
```bash
# a. stop the ONE supervisor (caffeinate exits with it)
kill "$(cat runs/pod_supervisor.pid)"; rm -f runs/pod_supervisor.pid runs/pod_strikes.txt
# b. env back to US (v6e-16 first, v6e-8 fallback) across the five US zones
sed -i '' -e 's/^ZONES=.*/ZONES="us-east1-d us-east5-b us-central1-a us-central2-b us-west1-c"/' -e 's/^ACCEL=.*/ACCEL=v6e-16/' -e 's/^ACCEL_LIST=.*/ACCEL_LIST="v6e-16 v6e-8"/' tools/campaign.env
grep -E '^(ZONES|ACCEL|ACCEL_LIST)=' tools/campaign.env
# c. the master deadline stays launch+30h (20:43Z Sep 4); no change here. Tear down any live Mumbai node so the new supervisor hunts US clean:
bash tools/pod.sh down 2>/dev/null || true
# d. restart the supervisor (race-free pid capture) + keep-awake
nohup bash -c 'DRY_SLEEP=150 exec bash tools/pod.sh supervise 16' >/dev/null 2>&1 & SUP=$!; nohup caffeinate -i -s -w "$SUP" >/dev/null 2>&1 &
tail -3 runs/pod_qhrrn2-pod2.log   # SUPERVISE start … accels='v6e-16 v6e-8' zones='us-east1-d …'
# e. on the first READY in a US zone: bash tools/plant_guard.sh 40 -> GUARD-OK (the launching session's planter does this itself while that session lives)
# f. record it: git add tools/campaign.env tools/HANDOFF.md; git commit -m "sportC2 US-NIGHT SWITCH taken <UTC>"; git push
```
**Cost of the switch (case 4, state it in the heartbeat):** completed arms (`<arm>_ARM_OK` / `<arm>_PRETRAIN_OK`) carry over via GCS and SKIP; a Mumbai in-flight pretrain restarts from 0 on US (no live sync); evals resume from 300 s partials. US-16 4×4 parallel finishes the remaining arms faster and cheaper than Mumbai-8 sequential — the reason for the switch. After it, heartbeats resume on the same template; a US preemption at night → the supervisor re-hunts US by itself (night landings are fast); anything unexpected → FABLE-NEEDED.

### HEARTBEAT TEMPLATE (hourly; IST with UTC; every number from the log/GCS/status, never guessed)
`sportC2 heartbeat <HH:MM IST> (<HH:MMZ>) — node: <zone> <v6e-16|v6e-8> <state> since <HH:MMZ> (incarnation N of the campaign); supervisor pid <N> alive; deadline in <H.h> h (<HH:MMZ>); guard GUARD-OK on this node: <yes/no>. Workers: w0 <arm> <pretrain step S/80000 at M it/s measured → ETA HH:MM IST | eval <name> running | ARM-OK>; w1 …; w2 …; w3 …. Arms OK <k>/7: <list>. Spend: <READY hours by shape> × rate = $<x> vs the $170–230 band. Incidents since the last heartbeat: <none | the verbatim lines>. Next heartbeat <HH:MM IST>.`
Sources: progress + pace = `bash tools/ops_snapshot.sh` TICKs (trainer `step N` line, eval shard counts, DMS, guard, live-bank age — the measured read); node / supervisor / deadline / GCS bank = `bash tools/pod.sh status` (its remote per-arm `partial/DONE` list looks for a `.done` file this chain never writes — read arm completion from the GCS markers instead: `gsutil ls gs://qhrrn2-rescue/sportC2/ | grep -E '_OK|STOPPED'`); pace = two `step N` PROGRESS rows over their wall-clock gap (the trainer's printed it/s overstates the wall pace); events = `grep -E 'CREATED|READY|CANARY|PREEMPT|DEMOT|STRIKE|NAN|OOM|FAILED|INCOMPLETE|COMPLETE|TEARDOWN|deadline' runs/pod_qhrrn2-pod2.log | tail -20`; spend = READY spans from `runs/tpu_status_log.txt` (15-min inventory rows; count rows per shape × 0.25 h) × $13.64 (US v6e-16) / $6.82 (US v6e-8) / ≈ $16 (Mumbai-16) — `spend_report.py` over-rates US-16 spans, hand-derive; IST = UTC + 5:30.

- **Launch facts:** launched 2026-09-03T14:43:16Z (20:13 IST) by Fable on the PI's go; `tools/campaign.env` ← `campaign_sportC2.env`; `runs/tpu_deadline.txt` = **1788583396 = 2026-09-05T04:43:16Z (2026-09-05 10:13 IST) = launch + 38 h** (EXTENDED from launch+16h by the PI's explicit go 2026-09-03 20:30 IST to ride the US-night window per the US-NIGHT SWITCH block below; the supervisor stops creating and the watchdog deletes past it; guard re-planted at the new time; a further extension = PI go only); `runs/pod_strikes.txt` cleared (strikes 0/2); supervisor `DRY_SLEEP=150 bash tools/pod.sh supervise 16` (pid in `runs/pod_supervisor.pid`) + caffeinate on that pid; the guard is planted on the first READY; the edge Monitor + hourly heartbeat cron were armed in the launching session. **What is locked:** ledger §5 2026-09-04 LAUNCH REGISTRATION; design + pre-mortem `Documentation/Plan_2026-09-04_sportC2.md` §1–§8; rules `tools/analyze_sportC2.py` (selftest 21/21; UNTOUCHED from here on); chain `tools/chain_sportC2.sh` through `tools/harness_sportC2.sh` = 49/49 (S1–S9 incl. the negative S9b/S9c; run 1 read 21/24 on sandbox arm names, run 2 47/48 on the S9b stub guard, run 3 49/49); CPU smokes on real code (plan §8, all rc 0); suite 151 green; `tools/campaign_sportC2.env` = the source of truth once copied to `tools/campaign.env`. PI decisions on record (2026-09-03 evening): the arm set APPROVED; ARC deferred until after the Sudoku champion work.
- **Arms and the static map (v6e-16 = 4 hosts × 4 chips; v6e-8 = 1×8 sequential in the priority order W0 R1 R3 X1 R2 R4 X2 → riders):** w0 W0 → R2 · w1 R1 → R3 · w2 R4 → X1 · w3 X2 → riders (B0/B1 correct-grid 20k scans + B0's EMA full; canvas C3X/D4 sel-5k). Recipes: W0/R1/R2/R3 = d128 native two-stage (50k cosine 1e-3→3e-5 + 30k floor @3e-5, B64, **wd 1.0**, z-norm) with R1 `--sot --sot-segments 4`, R2 `--inner-k 3`, R3 `--hard-p 0.5`; R4 = sportC1 R0's 50k weights `--init-from` + 50k in the field regime (batch 384, lr 1e-4, wd 1.0, remat; the chain fetches `sportC1/R0_ckpt.pkl` itself); X1/X2 = the TRM/EqR cell + SOT + ACT in the field regime (batch 768, 50k), X1 without digit aug, X2 with `--trm-token-mixer group9`. Headline weights: raw on W0/R1/R2/R3, EMA on R4/X1/X2 (the chain handles it; nothing to do).
- **LAUNCH PROCEDURE (EXECUTED at launch; kept as the reference for a relaunch after a full stop — a relaunch of the campaign itself is FABLE-NEEDED):** (0) `gcloud config list` = aakashemailbox@gmail.com / quantum-llm · `git status` clean at the registration commit · `bash tools/pod.sh status` node ABSENT everywhere · `launchctl list | grep qhrrn2` loaded; (1) `cp tools/campaign_sportC2.env tools/campaign.env`; (2) `echo $(( $(date -u +%s) + 16*3600 )) > runs/tpu_deadline.txt` (= launch + 16 h; the watchdog deletes any node on sight while the knob is in the past) and `rm -f runs/pod_strikes.txt` (strikes 0/2); (3) `DRY_SLEEP=150 nohup bash tools/pod.sh supervise 16 >/dev/null 2>&1 &` then `nohup caffeinate -i -s -w "$(cat runs/pod_supervisor.pid)" >/dev/null 2>&1 &` — the supervisor banks `code_<sha>.tgz`, hunts v6e-16 then v6e-8 across the US zones (Mumbai only on a PI go), bootstraps, runs the canary, launches the chain once per worker; (4) on the first READY: `bash tools/plant_guard.sh 20` → `GUARD-OK` (20 attempts 45 s apart; the deadline is read from `runs/tpu_deadline.txt`; re-run after EVERY node change and EVERY deadline change); (5) arm the session layers (§2): the edge Monitor on `runs/pod_qhrrn2-pod2.log` + the hourly heartbeat cron; (6) the B1 projection gate at the first MEASURED rows: hold + PI framing if the 16-shape projects past ≈ 17 h (1.2 × 14 h) or the 8-shape past ≈ 24 h; the ops phase reads NO accuracy values.
- **INFERRED paces / walls (plan §6; the canary and the first rows re-price):** W0 ≈ 4 h (80k at ≈ 21 it/s on 4 chips + ≈ 2.5–3 h of evals); R2 ≈ 9–10 h (K = 3 triples the per-step cost of the pretrain AND of every eval) → **w0 ≈ 13–15 h = the long pole**; R4 ≈ 7 h pretrain (≈ 2 it/s on 4 chips at batch 384 with remat) + 2 h evals + X1 ≈ 4.5 h → w2 ≈ 13.5 h; w1 ≈ 8 h; w3 ≈ 9 h. Spend band ≈ $170–230 (US 16 $13.64/h · US 8 $6.82/h · Mumbai-16 ≈ $16/h); tripwire 1.5× = $345. The 8-shape sequential ≈ 20–22 h needs a PI-approved deadline extension (+8–10 h) — frame it, never extend alone.
- **Log signatures specific to this chain (remote `runs/detached.log`, surfaced by `pod.sh status` PROGRESS snippets and the GCS markers):** sportC1's set (`PRETRAIN-START <arm> … prec=highest|default` · `STAGEA-OK` (+ `<arm>a_pretrain.tgz`, `<arm>_STAGEA_OK`) · `PRETRAIN-OK` (`<arm>_PRETRAIN_OK`, `<arm>_pretrain.tgz`, `<arm>_ckpt.pkl`) · `VALBEST <arm> A:|B:NNNNNN val step -> path` · `VB-FALLBACK-FINAL` (labeled, not an error) · `PRETRAIN-RESTORE <arm>[a]` (grids + the stage-A dir re-pulled after a node change — automatic) · `STAGEA-RESTORE-MISSING` (labeled) · `SCREEN-SKIP` · `EVAL-OK|EVAL-SKIP|CENSUS-OK|CENSUS-SKIP` · `FULL-FINAL <arm> := vsel` · `ARM-OK` · `SPORTC2-WORKER-DONE worker=N waiting` (normal on a 16) · `CHAIN-SPORTC2-COMPLETE` + `sportC2_final.tgz` + `SELF-TEARDOWN`) PLUS: `CALIB-OK|CALIB-SKIP` (the stall-calibration rows; `calib_R3_vsel_hard` and `screen_R3_vb_hard` are R3's labeled hard-feedback rows), **`PRETRAIN-OOM-RETRY-REMAT <arm>`** (a launch-time HBM exhaustion before any logged step, retried ONCE with `--remat` — labeled in `RETRY_REMAT.txt`, NOT a death; report it in the heartbeat), `RIDER-CKPT-MISS` (riders are non-blocking). **Registered outcomes, never retried:** `PRETRAIN-NAN <arm> [stage A|stage B] (rc=3)` → `AMPUTATED to ckpt_NNNNNN step N` + `STOPPED.txt` + evals on the amputated final (a stage-A death STOPs the arm: no stage B). **Stop-and-report signatures:** `AMPUTATE-FAILED` (incl. a persistent OOM), `EVAL-N-BAD`, `EVAL-SHARD-FAILED`, `EVAL-FAILED`, `CENSUS-FAILED`, `CALIB-FAILED`, `BAD-ARM`, `NPZ-MISSING`, `INIT-CKPT-MISSING R4` (R0's ckpt absent from GCS — verified present today), `SPORTC2-INCOMPLETE` (wait passes exhausted — read WHY: which `*_ARM_OK` is absent → that worker's log).
- **Decision table (this campaign):** sportC1's (§4) verbatim: preemption → the supervisor re-hunts (a second running-preemption on the 16 ⇒ demoted to the 8 for the supervisor's life); on the NEW node every arm resumes from its banked 1k-cadence `ckpt_latest` (EMA restored; `resumes.txt` records the step; R1/X1/X2 restart their carries — labeled; R4 resumes its continuation, never its init) and evals resume from 300 s partials; **re-plant the node-side guard on EVERY new node and after ANY deadline change**; the chain re-pulls grids and the stage-A dir by itself before val-selection (no manual pulls); the 8.5 h wall-ceiling recycle (`worker(s) N IDLE — relaunching`) is NORMAL; churn circuit-breaker: ≥ 2 node losses in 2 h during US-day → stop the supervisor, notify the PI, wait for the night window unless told otherwise; Mumbai = US-dry fallback ONLY on a PI go; deadline binding with arms unfinished → the guard clips = a labeled pause (banked partials make it cheap), extension = PI go only, then re-plant the guard.
- **Heartbeat (hourly, IST with UTC; even if uneventful):** node zone/shape/state · per worker: current arm + phase (PROGRESS snippet + GCS markers) · completion fraction + ETA from MEASURED pace · chip census · spend from the watchdog spans × rate (`spend_report.py` over-rates US-16 spans — hand-derive) vs the $170–230 band · deadline remaining · incidents since the last heartbeat (an OOM retry counts as one). **Ops reads NO accuracy/vote values.**
- **Completion (§6 generic + these counts):** 7 `*_ARM_OK`; pretrain dirs W0 R1 R2 R3 R4 X1 X2 (+ W0a R1a R2a R3a stage-A dirs) with `ckpt_latest.pkl`, any `STOPPED.txt` / `RETRY_REMAT.txt` / `NAN_ABORT.txt` quoted verbatim and `resumes.txt` noted; per native arm `full_vsel_t64`, `full_final_t64`, `full_vsel_t64_alt` (n == 422786 each), `retfm_t8` (512), `sxscan` (20000), screens (512), `sxcensus_*_vsel` + `_final` (census.json), `sxcalib_*_vsel` (calib.json); R3 additionally `sxscreen_psportC2R3_vb_hard` + `sxcalib_psportC2R3_vsel_hard`; X1/X2: `full_vsel_t16`, `full_final_t16`, `full_vsel_t16_alt`, `full_vsel_t64`, scan, censuses, calib; riders `sxscan_psportC1B0_vselA20k` + `…B1_vselA20k` (20000), `sxeval_psportC1B0/full_vselA20k_t64_ema` (422786), `sxrider_C3X_sel5k` + `sxrider_D4_sel5k` (5000). Sentinel in the pod log + `sportC2_final.tgz` in GCS → positive `describe` ABSENT → fleet-zero sweep (all campaign zones, nodes + QRs) → pull with `gcloud storage cp` + crc32c → extract into `runs/` → COUNTS ONLY → stop the session layers (leave the launchd watchdog) → ONE factual ops report → STOP. The analysis pass (`analyze_sportC2.py` untouched + the three freethink lenses, $0) runs in a fresh session on the PI's go.
- **Calendar:** night Sep 4–5 → analysis Sep 5–6 → sportC3 (parity at d160, ×2 seeds, the surviving levers) Sep 6–7 → ARC-d96 (+ wd 1.0 arm, commitment lens) after the champion work → draft from Sep 8; freeze ~Sep 10–12; ICLR abstract Sep 18.

## 1. First three commands (any time, and after any app restart)
```bash
bash tools/pod.sh status            # node (all zones), supervisor, deadline, GCS bank, remote job
tail -3 runs/pod_qhrrn2-pod2.log    # what the loop last saw / did
launchctl list | grep qhrrn2        # watchdog loaded? (reinstall: bash tools/install_watchdog.sh)
```
Then `git log --oneline -3` (has a close/verdict landed?) and the newest ledger §5 entry. If `supervisor: NOT RUNNING` and the campaign is incomplete: `DRY_SLEEP=150 nohup bash tools/pod.sh supervise 14 >/dev/null 2>&1 &` + `nohup caffeinate -i -s -w "$(cat runs/pod_supervisor.pid)" &` (adopts a live chain; refuses to start twice).

## 2. Re-arm the session layers after any app restart (supervisor + caffeinate + launchd watchdog survive; these do not)
(a) edge monitor: `tail -F runs/pod_qhrrn2-pod2.log` filtered to `CREATED|READY|CANARY|PREEMPT|DEMOT|PRETRAIN-NAN|EVAL-N-BAD|MERGE-N-BAD|INCOMPLETE|COMPLETE|FAILED|TEARDOWN|needs eyes|deadline` — never put a literal pgrep/pkill pattern in a watcher's text (self-match class, §7). (b) hourly heartbeat to the PI: per-process completion fraction + concrete ETA from MEASURED pace (arm markers in GCS, `PRETRAIN-OK` timestamps, monitor step rows), IST (UTC), spend vs the registered band from watchdog spans × rate (`spend_report.py` over-rates US-16 spans at the Mumbai 2.35× — hand-derive), chip census, deadline rule (extend +14h when <10h AND the PI has authorized). (c) verify the watchdog and the node-side guard (`bash tools/plant_guard.sh <hours>` → `GUARD-OK`; re-run after EVERY node change and EVERY deadline change). (d) since 2026-09-04: the 15-min measured ticker (`bash tools/ops_snapshot.sh` in a persistent Monitor loop) + the guard-planter Monitor (`CREATED in` → `plant_guard.sh 20`); (e) verify the live-bank loop on the node (the TICK `LB` line < 10 min old), restart it if not (`tools/live_bank.sh loop`, detached).

## 3. Normal log signatures
`CREATED` / `CANARY` / `LAUNCH chain` / `launch wN: detached + verified` / `RUNNING 1/1 worker(s)` progress rows / `PRETRAIN-START|OK <arm>` / `STAGEA_OK` (two-stage) / `EVAL-OK <name>` / `EVAL-SKIP` (idempotent marker) / `ARM-OK <arm>` / wall-ceiling recycle ≈ launch+8.5h: `worker(s) 0 IDLE — relaunching (attempt N/10)` then a new `launch w0` = NORMAL (resume) / `COMPILE-CACHE restored` / `VENV-RESTORE-OK` / `code from GCS archive` / `SELF-TEARDOWN` / `CHAIN-<TAG>-COMPLETE`. Multi-host (v6e-16 = 4 hosts × 4 chips): four `START worker=k/4` lines.

## 4. Decision table (the loop does the mechanics; you decide these)
`PRETRAIN-NAN <arm> -> amputate` = a REGISTERED outcome (one-shot; never retry the arm) · `AMPUTATE-FAILED` = STOP + report · `EVAL-N-BAD` / `MERGE-N-BAD` / `EVAL-SHARD-FAILED` = read that shard log AT THE NODE immediately; N-BAD = partition integrity — stop, report, delete nothing · preemption → the supervisor re-hunts (strike 2 on the 16 ⇒ demoted to v6e-8 for its life); on the NEW node partials auto-resume (≤10 min loss) — re-plant the guard + re-pull banked grids/metrics before any eval (else `select_ckpt` silently falls back to the final grid) · `<TAG>-INCOMPLETE` = wait-passes exhausted — read WHY (which `*_ARM_OK` is absent → that worker's log) before any relaunch · deadline binding → the guard clips = a labeled pause (banked partials make it cheap); extension = PI-GATED ONLY, then re-run plant_guard · churn circuit-breaker: ≥2 node losses in 2h during US-day → stop the supervisor, notify the PI, wait for the night window unless told otherwise · Mumbai/asia = US-dry fallback only (PI policy) · a deliberate node kill sweeps its own chain's claim markers in the same action.

## 5. Never (ops phase)
No analyzer runs, no accuracy/vote values read or quoted, no verdicts, no tuning, no chain/env edits beyond the registered live verbs (kill-orphan / extend-deadline-with-PI-go / pause), no second pod / on-demand / queued resources without an explicit PI go, never two supervisors, never change `ARMS` order mid-campaign, never `gsutil -m`, never judge a remote command through `| tail`.

## 6. Completion procedure (generic; the campaign registration names the counts)
Sentinel in the pod log + final tgz in GCS verified at source → node positive-describe ABSENT → fleet-zero sweep (all campaign zones, nodes + QRs) → pull final tgz (`gcloud storage cp` + crc32c) → extract into `runs/` → COUNTS ONLY (pretrain dirs with `ckpt_latest.pkl` + any `STOPPED.txt` quoted verbatim; eval dirs with `summary_all.json`: full n==422786, scan n==20000, screens/retfm n==512; `*_ARM_OK` per arm) → run the campaign's auditor if one exists (`--phase final` 0-FAIL) → stop session layers (leave the launchd watchdog) → ONE factual ops report (completion time, node timeline, incidents, spend hand-derived, "analysis pass ready") → STOP. The analysis pass carries the ledger entry + commit.

## 7. Lessons register (one line each; every one cost a bite; details in the ledger's CONSOLIDATED OPS RECORDS and the per-campaign blocks in git, §8)
- **Billing/backstops:** guest shutdown (DMS) does NOT stop TPU billing — only node deletion does; the node-side delete guard (`plant_guard.sh`, zone-discovering) is the Mac-independent backstop; the launchd watchdog hard-deletes past `runs/tpu_deadline.txt`; teardown verification = a POSITIVE `describe` (a network outage once faked "already gone" → ≤2h idle node); keep-awake (caffeinate) must outlive the supervisor whenever any watch runs (a 7h Mac sleep once blinded a wait).
- **Ssh/remote:** `--ssh-flag "-o StrictHostKeyChecking=no" --ssh-flag "-o UserKnownHostsFile=/dev/null"` (reused-IP 255 class); bound every remote call (`perl -e 'alarm N; exec @ARGV' --`); stderr visible; verify by ARTIFACT (GCS object / node log / describe), never wrapper pgrep (the "LAUNCHED" illusion); canary verifies by artifact on ssh timeout (a passed fit read FAILED through a hung channel); `nohup` over ssh hangs the session — ship remote work via the dispatcher's detach path; `mkdir` before `scp`; a payload containing a pkill pattern kills its own session (two-call pattern).
- **Data/pulls:** `gcloud storage cp` per object + crc32c against `gcloud storage ls -L` (gsutil -m stalls; serial `gsutil cp` stalled at full size with a wrong hash); tarballs of two-stage arms must include the stage-A dir + `config.json`; resumes re-split the rng (valid draw, labeled) and a death within ~3k steps of a resume is "resume-adjacent" (C1, P5) — record resume steps.
- **Chains (sportC1 analysis, 2026-09-03):** a two-stage arm's STAGE-A dir must be restored on a node change or the both-stage val-selection silently degrades to stage-B-only (B0/B1's post-preemption scans + vsel censuses ran on memorized stage-B grids while their fulls/screens ran on the right A:20k grids — the analyzer's gates did not check grid consistency across an arm's evals); fixed in `chain_sportC1.sh` (`ensure_local_pretrain` restores `${arm}a_pretrain.tgz`; harness S8) — EVERY resume path needs its own harness scenario, and the analyzer asserts one ckpt path per (arm, vsel).
- **Chains:** one-shot amputation truncates `metrics.jsonl` to the last finite grid (death step bounded by the grid census) and the trainer keeps running on NaNs (early-NaN abort owed); fixed-step screens beyond a death are garbage (243 violations) — skip; completion guard: sentinel/final only when every artifact is present (a vacuous sentinel once tore a pod down mid-campaign); merges n-gated and idempotent; `--merge` on a busy node needs `JAX_PLATFORMS=cpu`; multi-host DP needs per-host confinement (93a79d4); v6e-16 = 4×4 not 2×8; T12 at d16 needs `--remat`; the relaunch counter resets on healthy polls (a monotone counter once nearly tore down a healthy campaign at a wall recycle); static worker maps beat claim systems (claim races/TTL takeovers were the rung-2 incident class).
- **Local tooling:** exit-status masking through `| tail`/`| grep` bit ≥4 times; `grep -c` already prints 0 (`|| echo 0` fabricates); zsh does not word-split unquoted vars; macOS has no `timeout(1)`; `/bin/bash` is 3.2 (no associative arrays); a Monitor/watcher whose command text contains a `pgrep -f` pattern self-matches and can deadlock the waiter (bracket the pattern).
- **Spot weather:** us-east1-d churns in US-day hours (maintenance kills with zero progress happen); night windows land in minutes; Mumbai asia-south1-c is a reliable v6e-16 source (2.35× cost) — fallback only; QR flex-start waited ≥7h ungranted; quota buckets are zonal and `Unlimited` rows are decoration — only creates tell the truth.
- **Rates (PI 08-30):** v6e-8 US $6.82/h · v6e-16 US $13.64 · Mumbai-16 ≈$16 · Mumbai-8 ≈$8; `spend_report.py` applies the 2.35× to all ×2 spans (zone-aware fix owed); hand-derive from watchdog spans.
- **Harnesses (sportC2, 2026-09-04):** an adapted harness is UNVERIFIED until it runs — the first sportC2 run read 21/24 on sandbox construction still carrying the previous campaign's arm names (fake init ckpt / rider tarball names / assertion dirs: the PM-5 class re-entering through the copy) while the chain passed every scenario it reached; and a stub's own guard can hide the exact path a negative scenario stages (S9b) — every negative scenario asserts that the failure it stages actually fired.
- **Live banking (sportC2, 2026-09-04):** a chain without a live mid-run GCS sync loses every in-flight pretrain and eval at a preemption (chain_r0 had the 5-min loop; the sportC chains dropped it; R2's stage-A 5k grids died with a node) — `tools/live_bank.sh` (5-min serial rsync of runs/ + a fresh-node no-clobber restore with load-verified checkpoints and a labeled grid fallback) is standing from here: every chain runs `restore` at start and `loop` alongside, and the harness carries a restore scenario per resume path (S10/S10b/S10c). A checkpoint torn by the wall-recycle SIGTERM used to enter the NaN/amputation path; the sanitize step now catches it.
- **Single-chip riders (sportC2, 2026-09-04):** a k128 eval on ONE chip runs ~2.8 h per 1250 records on the d96 canvas — a 5k rider is ~11 h; anything on the completion path is sharded over the host or it never finishes before the deadline (sportC1's canvas riders never started; sportC2's would have held the node ~22 h before the sentinel).
- **Measured paces:** canvas d96 T12 3.20 it/s DP-8 (1.24 DP-4); native9 d96 T12 ≈19 it/s wall (39 printed; 50k in 43 min), T16 ≈0.83×; native full test 8 min sharded-8, 20k k128 scan ≈27 min; canvas full t64 4-way ≈2.5h; screens 19 min sharded-8 (canvas); eval bank quantum 128 records; CPU (Mac): native 20k cold eval k=0 ≈18 min, canvas ≈10× slower.

## 8. Superseded campaign blocks (verbatim in git; `git show <hash>:tools/HANDOFF.md`)
sportC1 LIVE (launch block) → 280a110 · sportC1 COMPLETE + analysis-pass STATUS → 86bf9bf, eea3c32 · sportC2 BUILT (the pre-registration pickup list) → eea3c32 · sportC0 pilot LIVE/Mumbai-switch blocks → 93850f8, dcd14de · rung-2b rider scans LIVE + COMPLETE → ee4cb1c, 29c3303 · rung 2b LIVE/COMPLETE/night-close → 3bb3079, b49c970, c60c9aa, 29c3303 · rung 2 registered/in-flight/night-pause/carriers-stopped/COMPLETE → 3a8f18a, ad08286, a30499a, 5df9769 · rung 1 tail (pause, hardened relaunch, flex-start contingency) + COMPLETE → 297351e, 5d34d03, f6e6744, 9f0454f, e068012 · rung 1 launch + Opus handoff → b1d21fe, 0db2f1e, d1be32b · wave 3a → 2f0b033, 923d546, c62c7da, 7edfd08, 541860c · wave 2 (launch, pause, resume, handoff, complete) → f164feb, d6f2574, e12a2eb · sprint S2 wave 1 (launch, drought pause, complete) → 5771779, 7978123 · rung 1c/1b (launch, weather pause, complete) → a20cdf0, 3430935, 96284dc, b5b5510.
