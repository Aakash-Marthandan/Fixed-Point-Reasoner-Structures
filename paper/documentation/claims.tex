% NOT \input by main.tex — the claim ledger the abstract and introduction draw from (from the analysis reports;
% every number cites its protocol column; "record" only with the "program-" prefix; comparator names, never nicknames).
% 1. Exact S9 symmetry on a nine-field state replaces the digit orbit (A3 - X1 = +68.8 pp on identical puzzles; A4 - A3 inside the floor).
% 2. The DEC reaches parity with the field's single-pass numbers without digit augmentation (A5/A7 95.1 @D16 / 98.0-98.1 @D64 on the full test; D128 98.6 on the 20k vs EqR 95.0) at 55 % of the parameters, 4.6x the MACs per step.
% 3. The selector law: verification-free residual selection works iff RI/NI training cleaned the attractors (40 grids incl. the field's released weights).
% 4. The two-axis decoder class (threshold/yield x commitment dynamics) is set by the loop, not the scale; the calibrated-commitment gap is class-intrinsic.
% 5. The trajectory / memorization law holds in every regime incl. the field's released grids; selection is load-bearing; the selection instrument's resolution set a 5 pp artifact.
% 6. The depth law: decimating decoders gain ~1 pp per doubling to D256 with zero regressions; the field's D16 headline undersells its own models by 6-9 pp.
% 7. The field's design choices measured on their weights: EMA +16.8 (EqR), Langevin noise .5 -10, the 60k grid -3.85, the curriculum's selector cost, the halting head a verifier (AUC >= .998), the prefix inert.
% 8. (champion night, pending) seeds x3 of the champion recipe with a 512-puzzle monitor; the orbit lever; the operator; the accuracy-per-MAC point; the commit head vs the class-intrinsic gap.
% 9. (§6, the ARC era in retrospect) retention is the basin-existence probe on both domains; the equilibrium core repaired truth-erasure on ARC (6 % -> 29 %); basins exist for a minority of ARC task pairs and no iteration, temperature or attempts help where none exists (t = 96: +0; anti-Arrhenius; 31x basin-existence conditioning).
% 10. (§6) the landscape-class law: ARC is inventory-limited, Sudoku search-limited; the same two instruments (retention; extended iteration) give opposite verdicts (the two-sided iteration figure); sharpened: the reachability wall is a property of the cell and its loop (ARC ~30 % of pairs; our Sudoku cell ~.5; the field's cell .98), not of the domain.
% 11. (§6) the verifier's worth: verified vote vs unverified majority at k = 128 on the same draws (68.8 vs 18.8; 70.7 vs 5.1) on wide-funnel maps; ~0 on init-invariant maps; ARC's conversion stack (candidate supply x basin decoding, snap-vote) is the substitute for the missing verifier.
% 12. (§6) the failure-class continuum read by one instrument (the converged-wrong rate): ARC .92 -> memorized Sudoku maps .03-.16 -> the field's RI-free base .03-.06 -> RI + FPA maps <= .001 (the DEC's A5 .0004); a memorizing Sudoku map acquires the ARC failure texture (near-valid confidently wrong grids).
% 13. (§6) the priced-code laws on ARC (seeded at three widths): the toll buys transfer to unseen rule families at a mild cold cost (Law 4); the throat declines with capacity (785 -> 459 nats, UV-led); the optimization-length tax erodes transfer at fixed price (H-40 confirmed; the beta-rescue H-41 falsified: transfer is beta-inelastic over 20x); on Sudoku the price killed the small map (H-43), then became a stability dividend (H-44) and the precondition for training the deep map (H-48), with precision and draw diversity competing for the budget (H-47).
% 14. (§6) the init-distribution law (H-37): RI pays exactly under the init distribution it trained on (Sudoku paired multi-init 38/40, p ~ 1e-10; ARC endpoint condensation), which explains EqR's multi-init lever and predicts where it transfers.
% 15. (§6) information geometry: the throat is a task-set constant on ARC and an instance-conditional meter on Sudoku (801/987/1528 nats at 50/40/30 givens); the priced profile is the knee on ARC and extreme-UV on Sudoku: the profile is a property of the task distribution, not the architecture.
% 16. (§6) portability: PORTED (retention, cold, the ladder, the throat, eta), ANALOG (thermal capture -> the verified funnel), STRUCTURAL-ARC (codebook size, the transfer gates, the vacancy floor, TTT), and the Sudoku-born back-port register (final-map retention, monitors + val-selection, answer-mode contractivity, the explosion census, the decoder lens, calibration at stalls); the ARC-d96 rung with the back-ported suite is what settles the scale-matching gap.
